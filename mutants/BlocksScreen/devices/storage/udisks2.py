import asyncio
import logging
import os
import pathlib
import shutil
import typing
import unicodedata
from collections.abc import Coroutine

import sdbus
from PyQt6 import QtCore

from .device import Device
from .udisks2_dbus_async import (
    Interfaces,
    UDisks2AsyncManager,
    UDisks2BlockAsyncInterface,
    UDisks2DriveAsyncInterface,
    UDisks2FileSystemAsyncInterface,
    UDisks2PartitionAsyncInterface,
    UDisks2PartitionTableAsyncInterface,
)

UDisks2_service: str = "org.freedesktop.UDisks2"
UDisks2_obj_path: str = "org/freedesktop/UDisks2"
AlreadyMountedException = "org.freedesktop.UDisks2.Error.AlreadyMounted"

_T = typing.TypeVar(name="_T")
from typing import Annotated, Callable, ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"]  # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg=None):  # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os  # type: ignore

    mutant_under_test = os.environ["MUTANT_UNDER_TEST"]  # type: ignore
    if mutant_under_test == "fail":  # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException  # type: ignore

        raise MutmutProgrammaticFailException("Failed programmatically")  # type: ignore
    elif mutant_under_test == "stats":  # type: ignore
        from mutmut.__main__ import record_trampoline_hit  # type: ignore

        record_trampoline_hit(orig.__module__ + "." + orig.__name__)  # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    prefix = orig.__module__ + "." + orig.__name__ + "__mutmut_"  # type: ignore
    if not mutant_under_test.startswith(prefix):  # type: ignore
        result = orig(*call_args, **call_kwargs)  # type: ignore
        return result  # type: ignore
    mutant_name = mutant_under_test.rpartition(".")[-1]  # type: ignore
    if self_arg is not None:  # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)  # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)  # type: ignore
    return result  # type: ignore


def validate_label(label: str, strict: bool = True, max_length: int = 100) -> str:
    args = [label, strict, max_length]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_validate_label__mutmut_orig,
        x_validate_label__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x_validate_label__mutmut_orig(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_1(
    label: str, strict: bool = False, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_2(
    label: str, strict: bool = True, max_length: int = 101
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_3(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_4(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return "XXXX"
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_5(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_6(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return "XXXX"
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_7(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) >= max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_8(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "XXXX" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_9(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = None
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_10(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize(None, label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_11(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", None)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_12(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize(label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_13(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize(
        "NFC",
    )
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_14(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("XXNFCXX", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_15(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("nfc", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_16(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(None):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_17(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(None) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_18(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) <= 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_19(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 33 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_20(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return "XXXX"
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_21(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = None

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_22(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(None)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_23(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "XXXX".join(
            char for char in normalized_label if ord(char) >= 32
        )

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_24(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(None) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_25(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) > 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_26(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 33)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_27(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = None
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_28(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "XX\0XX",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_29(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "XX\x00XX",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_30(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "XX/XX",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_31(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "XX\\XX",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_32(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        "XX;XX",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_33(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "XX|XX",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_34(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "XX&XX",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_35(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "XX$XX",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_36(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "XX`XX",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_37(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "XX(XX",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_38(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        "XX)XX",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_39(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "XX{XX",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_40(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "XX}XX",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_41(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "XX[XX",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_42(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "XX]XX",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_43(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "XX<XX",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_44(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        "XX>XX",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_45(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        'XX"XX',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_46(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "XX'XX",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_47(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "XX*XX",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_48(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "XX?XX",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_49(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "XX!XX",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_50(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = None
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_51(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(None)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_52(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "XXXX".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_53(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_54(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        and clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_55(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        and clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_56(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        "XX..XX" in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_57(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." not in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_58(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith(None)
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_59(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("XX/XX")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_60(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith(None)
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_61(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("XX\\XX")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_62(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            "XXXX"
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_63(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace(None, "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_64(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", None)
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_65(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_66(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "")
            .replace("/", "_")
            .replace(
                "\\",
            )
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_67(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace(None, "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_68(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", None).replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_69(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_70(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "")
            .replace(
                "/",
            )
            .replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_71(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace(None, "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_72(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", None).replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_73(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_74(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace(
                "..",
            )
            .replace("/", "_")
            .replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_75(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("XX..XX", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_76(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "XXXX").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_77(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("XX/XX", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_78(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "XX_XX").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_79(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("XX\\XX", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_80(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "XX_XX")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_81(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = None
    return final_label if final_label else ""


def x_validate_label__mutmut_82(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(None)[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_83(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip("XX .XX")[:max_length]
    return final_label if final_label else ""


def x_validate_label__mutmut_84(
    label: str, strict: bool = True, max_length: int = 100
) -> str:
    """
    Comprehensive validation for filesystem labels with security protection.

    Args:
        label: Raw input label to validate
        strict: If True, returns empty string for any invalid input
        max_length: Maximum allowed length in bytes

    Returns:
        Sanitized and validated label safe for filesystem use
    """
    if not label:
        return ""
    if not label.strip():
        return ""
    if len(label.encode("utf-8")) > max_length:
        return "" if strict else label[:max_length]
    normalized_label = unicodedata.normalize("NFC", label)
    if any(ord(char) < 32 for char in normalized_label):
        if strict:
            return ""
        normalized_label = "".join(char for char in normalized_label if ord(char) >= 32)

    dangerous_chars = {
        "\0",
        "\x00",
        "/",
        "\\",
        ";",
        "|",
        "&",
        "$",
        "`",
        "(",
        ")",
        "{",
        "}",
        "[",
        "]",
        "<",
        ">",
        '"',
        "'",
        "*",
        "?",
        "!",
    }
    clean_label = "".join(c for c in normalized_label if c not in dangerous_chars)
    if (
        ".." in clean_label
        or clean_label.startswith("/")
        or clean_label.startswith("\\")
    ):
        return (
            ""
            if strict
            else clean_label.replace("..", "").replace("/", "_").replace("\\", "_")
        )

    final_label = clean_label.strip(" .")[:max_length]
    return final_label if final_label else "XXXX"


x_validate_label__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_validate_label__mutmut_1": x_validate_label__mutmut_1,
    "x_validate_label__mutmut_2": x_validate_label__mutmut_2,
    "x_validate_label__mutmut_3": x_validate_label__mutmut_3,
    "x_validate_label__mutmut_4": x_validate_label__mutmut_4,
    "x_validate_label__mutmut_5": x_validate_label__mutmut_5,
    "x_validate_label__mutmut_6": x_validate_label__mutmut_6,
    "x_validate_label__mutmut_7": x_validate_label__mutmut_7,
    "x_validate_label__mutmut_8": x_validate_label__mutmut_8,
    "x_validate_label__mutmut_9": x_validate_label__mutmut_9,
    "x_validate_label__mutmut_10": x_validate_label__mutmut_10,
    "x_validate_label__mutmut_11": x_validate_label__mutmut_11,
    "x_validate_label__mutmut_12": x_validate_label__mutmut_12,
    "x_validate_label__mutmut_13": x_validate_label__mutmut_13,
    "x_validate_label__mutmut_14": x_validate_label__mutmut_14,
    "x_validate_label__mutmut_15": x_validate_label__mutmut_15,
    "x_validate_label__mutmut_16": x_validate_label__mutmut_16,
    "x_validate_label__mutmut_17": x_validate_label__mutmut_17,
    "x_validate_label__mutmut_18": x_validate_label__mutmut_18,
    "x_validate_label__mutmut_19": x_validate_label__mutmut_19,
    "x_validate_label__mutmut_20": x_validate_label__mutmut_20,
    "x_validate_label__mutmut_21": x_validate_label__mutmut_21,
    "x_validate_label__mutmut_22": x_validate_label__mutmut_22,
    "x_validate_label__mutmut_23": x_validate_label__mutmut_23,
    "x_validate_label__mutmut_24": x_validate_label__mutmut_24,
    "x_validate_label__mutmut_25": x_validate_label__mutmut_25,
    "x_validate_label__mutmut_26": x_validate_label__mutmut_26,
    "x_validate_label__mutmut_27": x_validate_label__mutmut_27,
    "x_validate_label__mutmut_28": x_validate_label__mutmut_28,
    "x_validate_label__mutmut_29": x_validate_label__mutmut_29,
    "x_validate_label__mutmut_30": x_validate_label__mutmut_30,
    "x_validate_label__mutmut_31": x_validate_label__mutmut_31,
    "x_validate_label__mutmut_32": x_validate_label__mutmut_32,
    "x_validate_label__mutmut_33": x_validate_label__mutmut_33,
    "x_validate_label__mutmut_34": x_validate_label__mutmut_34,
    "x_validate_label__mutmut_35": x_validate_label__mutmut_35,
    "x_validate_label__mutmut_36": x_validate_label__mutmut_36,
    "x_validate_label__mutmut_37": x_validate_label__mutmut_37,
    "x_validate_label__mutmut_38": x_validate_label__mutmut_38,
    "x_validate_label__mutmut_39": x_validate_label__mutmut_39,
    "x_validate_label__mutmut_40": x_validate_label__mutmut_40,
    "x_validate_label__mutmut_41": x_validate_label__mutmut_41,
    "x_validate_label__mutmut_42": x_validate_label__mutmut_42,
    "x_validate_label__mutmut_43": x_validate_label__mutmut_43,
    "x_validate_label__mutmut_44": x_validate_label__mutmut_44,
    "x_validate_label__mutmut_45": x_validate_label__mutmut_45,
    "x_validate_label__mutmut_46": x_validate_label__mutmut_46,
    "x_validate_label__mutmut_47": x_validate_label__mutmut_47,
    "x_validate_label__mutmut_48": x_validate_label__mutmut_48,
    "x_validate_label__mutmut_49": x_validate_label__mutmut_49,
    "x_validate_label__mutmut_50": x_validate_label__mutmut_50,
    "x_validate_label__mutmut_51": x_validate_label__mutmut_51,
    "x_validate_label__mutmut_52": x_validate_label__mutmut_52,
    "x_validate_label__mutmut_53": x_validate_label__mutmut_53,
    "x_validate_label__mutmut_54": x_validate_label__mutmut_54,
    "x_validate_label__mutmut_55": x_validate_label__mutmut_55,
    "x_validate_label__mutmut_56": x_validate_label__mutmut_56,
    "x_validate_label__mutmut_57": x_validate_label__mutmut_57,
    "x_validate_label__mutmut_58": x_validate_label__mutmut_58,
    "x_validate_label__mutmut_59": x_validate_label__mutmut_59,
    "x_validate_label__mutmut_60": x_validate_label__mutmut_60,
    "x_validate_label__mutmut_61": x_validate_label__mutmut_61,
    "x_validate_label__mutmut_62": x_validate_label__mutmut_62,
    "x_validate_label__mutmut_63": x_validate_label__mutmut_63,
    "x_validate_label__mutmut_64": x_validate_label__mutmut_64,
    "x_validate_label__mutmut_65": x_validate_label__mutmut_65,
    "x_validate_label__mutmut_66": x_validate_label__mutmut_66,
    "x_validate_label__mutmut_67": x_validate_label__mutmut_67,
    "x_validate_label__mutmut_68": x_validate_label__mutmut_68,
    "x_validate_label__mutmut_69": x_validate_label__mutmut_69,
    "x_validate_label__mutmut_70": x_validate_label__mutmut_70,
    "x_validate_label__mutmut_71": x_validate_label__mutmut_71,
    "x_validate_label__mutmut_72": x_validate_label__mutmut_72,
    "x_validate_label__mutmut_73": x_validate_label__mutmut_73,
    "x_validate_label__mutmut_74": x_validate_label__mutmut_74,
    "x_validate_label__mutmut_75": x_validate_label__mutmut_75,
    "x_validate_label__mutmut_76": x_validate_label__mutmut_76,
    "x_validate_label__mutmut_77": x_validate_label__mutmut_77,
    "x_validate_label__mutmut_78": x_validate_label__mutmut_78,
    "x_validate_label__mutmut_79": x_validate_label__mutmut_79,
    "x_validate_label__mutmut_80": x_validate_label__mutmut_80,
    "x_validate_label__mutmut_81": x_validate_label__mutmut_81,
    "x_validate_label__mutmut_82": x_validate_label__mutmut_82,
    "x_validate_label__mutmut_83": x_validate_label__mutmut_83,
    "x_validate_label__mutmut_84": x_validate_label__mutmut_84,
}
x_validate_label__mutmut_orig.__name__ = "x_validate_label"


def fire_n_forget(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    args = [coro, name, task_stack]  # type: ignore
    kwargs = {}  # type: ignore
    return _mutmut_trampoline(
        x_fire_n_forget__mutmut_orig,
        x_fire_n_forget__mutmut_mutants,
        args,
        kwargs,
        None,
    )


def x_fire_n_forget__mutmut_orig(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_1(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = None
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_2(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(None, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_3(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=None)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_4(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_5(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(
        coro,
    )
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_6(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(None)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_7(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(None)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_8(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error(None, task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_9(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", None)
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_10(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error(task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_11(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error(
                "Task %s was cancelled",
            )
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_12(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("XXTask %s was cancelledXX", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_13(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_14(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("TASK %S WAS CANCELLED", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_15(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(None, task.get_name(), e, exc_info=True)

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_16(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error("Caught exception in %s : %s", None, e, exc_info=True)

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_17(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), None, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_18(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=None
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_19(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(task.get_name(), e, exc_info=True)

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_20(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error("Caught exception in %s : %s", e, exc_info=True)

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_21(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception:
            logging.error("Caught exception in %s : %s", task.get_name(), exc_info=True)

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_22(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s",
                task.get_name(),
                e,
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_23(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "XXCaught exception in %s : %sXX", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_24(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_25(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "CAUGHT EXCEPTION IN %S : %S", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_26(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=False
            )

    task.add_done_callback(cleanup)
    return task


def x_fire_n_forget__mutmut_27(
    coro: Coroutine[typing.Any, typing.Any, typing.Any],
    name: str,
    task_stack: set[asyncio.Task[typing.Any]],
) -> asyncio.Task[typing.Any]:
    task: asyncio.Task[typing.Any] = asyncio.create_task(coro, name=name)
    task_stack.add(task)
    """Create a task for a specified coroutine and run it

    Args: 
        coro (Coroutine): coroutine to create task 
        name (str): Name for the task 
        task_stack (set): Task stack that keeps track of currently running tasks
    """

    def cleanup(task: asyncio.Task[_T]) -> None:
        """Cleanup task"""
        task_stack.discard(task)
        try:
            task.result()
        except asyncio.CancelledError:
            task.cancel()
            logging.error("Task %s was cancelled", task.get_name())
        except Exception as e:
            logging.error(
                "Caught exception in %s : %s", task.get_name(), e, exc_info=True
            )

    task.add_done_callback(None)
    return task


x_fire_n_forget__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
    "x_fire_n_forget__mutmut_1": x_fire_n_forget__mutmut_1,
    "x_fire_n_forget__mutmut_2": x_fire_n_forget__mutmut_2,
    "x_fire_n_forget__mutmut_3": x_fire_n_forget__mutmut_3,
    "x_fire_n_forget__mutmut_4": x_fire_n_forget__mutmut_4,
    "x_fire_n_forget__mutmut_5": x_fire_n_forget__mutmut_5,
    "x_fire_n_forget__mutmut_6": x_fire_n_forget__mutmut_6,
    "x_fire_n_forget__mutmut_7": x_fire_n_forget__mutmut_7,
    "x_fire_n_forget__mutmut_8": x_fire_n_forget__mutmut_8,
    "x_fire_n_forget__mutmut_9": x_fire_n_forget__mutmut_9,
    "x_fire_n_forget__mutmut_10": x_fire_n_forget__mutmut_10,
    "x_fire_n_forget__mutmut_11": x_fire_n_forget__mutmut_11,
    "x_fire_n_forget__mutmut_12": x_fire_n_forget__mutmut_12,
    "x_fire_n_forget__mutmut_13": x_fire_n_forget__mutmut_13,
    "x_fire_n_forget__mutmut_14": x_fire_n_forget__mutmut_14,
    "x_fire_n_forget__mutmut_15": x_fire_n_forget__mutmut_15,
    "x_fire_n_forget__mutmut_16": x_fire_n_forget__mutmut_16,
    "x_fire_n_forget__mutmut_17": x_fire_n_forget__mutmut_17,
    "x_fire_n_forget__mutmut_18": x_fire_n_forget__mutmut_18,
    "x_fire_n_forget__mutmut_19": x_fire_n_forget__mutmut_19,
    "x_fire_n_forget__mutmut_20": x_fire_n_forget__mutmut_20,
    "x_fire_n_forget__mutmut_21": x_fire_n_forget__mutmut_21,
    "x_fire_n_forget__mutmut_22": x_fire_n_forget__mutmut_22,
    "x_fire_n_forget__mutmut_23": x_fire_n_forget__mutmut_23,
    "x_fire_n_forget__mutmut_24": x_fire_n_forget__mutmut_24,
    "x_fire_n_forget__mutmut_25": x_fire_n_forget__mutmut_25,
    "x_fire_n_forget__mutmut_26": x_fire_n_forget__mutmut_26,
    "x_fire_n_forget__mutmut_27": x_fire_n_forget__mutmut_27,
}
x_fire_n_forget__mutmut_orig.__name__ = "x_fire_n_forget"


class UDisksDBusAsync(QtCore.QThread):
    hardware_detected: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="hardware-detected"
    )
    device_added: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, dict, name="device-added"
    )
    device_removed: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, str, name="device-removed"
    )  # device path
    hardware_removed: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="hardware-removed"
    )  # device path
    device_mounted: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, str, name="device-mounted"
    )  # device path, new symlink path
    device_unmounted: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="device-unmounted"
    )

    def __init__(self, parent: QtCore.QObject, gcodes_dir: str) -> None:
        args = [parent, gcodes_dir]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁ__init____mutmut_orig"),
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁ__init____mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁ__init____mutmut_orig(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_1(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(None)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_2(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = None
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_3(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = None
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_4(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(None)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_5(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = None
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_6(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = None
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_7(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = True
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_8(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_9(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(None)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_10(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = None
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_11(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name=None,
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_12(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path=None,
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_13(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=None,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_14(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_15(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_16(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_17(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="XXorg.freedesktop.UDisks2XX",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_18(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.udisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_19(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="ORG.FREEDESKTOP.UDISKS2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_20(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="XX/org/freedesktop/UDisks2XX",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_21(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/udisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_22(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/ORG/FREEDESKTOP/UDISKS2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_23(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = ""
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_24(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = None
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_25(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = None
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_26(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = True
        self.controlled_devs: dict[str, Device] = {}
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    def xǁUDisksDBusAsyncǁ__init____mutmut_27(
        self, parent: QtCore.QObject, gcodes_dir: str
    ) -> None:
        super().__init__(parent)
        self.task_stack: set[asyncio.Task[typing.Any]] = set()
        self.gcodes_path: pathlib.Path = pathlib.Path(gcodes_dir)
        self.system_bus: sdbus.SdBus = sdbus.sd_bus_open_system()
        self._active: bool = False
        if not self.system_bus:
            self.close()
            return
        sdbus.set_default_bus(self.system_bus)
        self.obj_manager: UDisks2AsyncManager = UDisks2AsyncManager.new_proxy(
            service_name="org.freedesktop.UDisks2",
            object_path="/org/freedesktop/UDisks2",
            bus=self.system_bus,
        )
        self.loop: asyncio.AbstractEventLoop | None = None
        self.stop_event: asyncio.Event = asyncio.Event()
        self.listener_running: bool = False
        self.controlled_devs: dict[str, Device] = None
        self._cleanup_broken_symlinks()
        self._cleanup_legacy_dir()

    xǁUDisksDBusAsyncǁ__init____mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ__init____mutmut_1": xǁUDisksDBusAsyncǁ__init____mutmut_1,
        "xǁUDisksDBusAsyncǁ__init____mutmut_2": xǁUDisksDBusAsyncǁ__init____mutmut_2,
        "xǁUDisksDBusAsyncǁ__init____mutmut_3": xǁUDisksDBusAsyncǁ__init____mutmut_3,
        "xǁUDisksDBusAsyncǁ__init____mutmut_4": xǁUDisksDBusAsyncǁ__init____mutmut_4,
        "xǁUDisksDBusAsyncǁ__init____mutmut_5": xǁUDisksDBusAsyncǁ__init____mutmut_5,
        "xǁUDisksDBusAsyncǁ__init____mutmut_6": xǁUDisksDBusAsyncǁ__init____mutmut_6,
        "xǁUDisksDBusAsyncǁ__init____mutmut_7": xǁUDisksDBusAsyncǁ__init____mutmut_7,
        "xǁUDisksDBusAsyncǁ__init____mutmut_8": xǁUDisksDBusAsyncǁ__init____mutmut_8,
        "xǁUDisksDBusAsyncǁ__init____mutmut_9": xǁUDisksDBusAsyncǁ__init____mutmut_9,
        "xǁUDisksDBusAsyncǁ__init____mutmut_10": xǁUDisksDBusAsyncǁ__init____mutmut_10,
        "xǁUDisksDBusAsyncǁ__init____mutmut_11": xǁUDisksDBusAsyncǁ__init____mutmut_11,
        "xǁUDisksDBusAsyncǁ__init____mutmut_12": xǁUDisksDBusAsyncǁ__init____mutmut_12,
        "xǁUDisksDBusAsyncǁ__init____mutmut_13": xǁUDisksDBusAsyncǁ__init____mutmut_13,
        "xǁUDisksDBusAsyncǁ__init____mutmut_14": xǁUDisksDBusAsyncǁ__init____mutmut_14,
        "xǁUDisksDBusAsyncǁ__init____mutmut_15": xǁUDisksDBusAsyncǁ__init____mutmut_15,
        "xǁUDisksDBusAsyncǁ__init____mutmut_16": xǁUDisksDBusAsyncǁ__init____mutmut_16,
        "xǁUDisksDBusAsyncǁ__init____mutmut_17": xǁUDisksDBusAsyncǁ__init____mutmut_17,
        "xǁUDisksDBusAsyncǁ__init____mutmut_18": xǁUDisksDBusAsyncǁ__init____mutmut_18,
        "xǁUDisksDBusAsyncǁ__init____mutmut_19": xǁUDisksDBusAsyncǁ__init____mutmut_19,
        "xǁUDisksDBusAsyncǁ__init____mutmut_20": xǁUDisksDBusAsyncǁ__init____mutmut_20,
        "xǁUDisksDBusAsyncǁ__init____mutmut_21": xǁUDisksDBusAsyncǁ__init____mutmut_21,
        "xǁUDisksDBusAsyncǁ__init____mutmut_22": xǁUDisksDBusAsyncǁ__init____mutmut_22,
        "xǁUDisksDBusAsyncǁ__init____mutmut_23": xǁUDisksDBusAsyncǁ__init____mutmut_23,
        "xǁUDisksDBusAsyncǁ__init____mutmut_24": xǁUDisksDBusAsyncǁ__init____mutmut_24,
        "xǁUDisksDBusAsyncǁ__init____mutmut_25": xǁUDisksDBusAsyncǁ__init____mutmut_25,
        "xǁUDisksDBusAsyncǁ__init____mutmut_26": xǁUDisksDBusAsyncǁ__init____mutmut_26,
        "xǁUDisksDBusAsyncǁ__init____mutmut_27": xǁUDisksDBusAsyncǁ__init____mutmut_27,
    }
    xǁUDisksDBusAsyncǁ__init____mutmut_orig.__name__ = "xǁUDisksDBusAsyncǁ__init__"

    @property
    def active(self) -> bool:
        return self._active

    def run(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁrun__mutmut_orig"),
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁrun__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁrun__mutmut_orig(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_1(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = None
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_2(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = None
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_3(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = False
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_4(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(None)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_5(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(None)
        except asyncio.CancelledError as err:
            logging.error("Caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_6(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error(None, err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_7(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError:
            logging.error("Caught exception on udisks2 monitor, %s", None)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_8(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error(err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_9(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError:
            logging.error(
                "Caught exception on udisks2 monitor, %s",
            )
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_10(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("XXCaught exception on udisks2 monitor, %sXX", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_11(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("caught exception on udisks2 monitor, %s", err)
            self.close()
            return

    def xǁUDisksDBusAsyncǁrun__mutmut_12(self) -> None:
        """Start UDisks2 USB monitoring"""
        self.stop_event.clear()
        try:
            self.loop = asyncio.new_event_loop()
            self._active = True
            asyncio.set_event_loop(self.loop)
            self.loop.run_until_complete(self.monitor_dbus())
        except asyncio.CancelledError as err:
            logging.error("CAUGHT EXCEPTION ON UDISKS2 MONITOR, %S", err)
            self.close()
            return

    xǁUDisksDBusAsyncǁrun__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁrun__mutmut_1": xǁUDisksDBusAsyncǁrun__mutmut_1,
        "xǁUDisksDBusAsyncǁrun__mutmut_2": xǁUDisksDBusAsyncǁrun__mutmut_2,
        "xǁUDisksDBusAsyncǁrun__mutmut_3": xǁUDisksDBusAsyncǁrun__mutmut_3,
        "xǁUDisksDBusAsyncǁrun__mutmut_4": xǁUDisksDBusAsyncǁrun__mutmut_4,
        "xǁUDisksDBusAsyncǁrun__mutmut_5": xǁUDisksDBusAsyncǁrun__mutmut_5,
        "xǁUDisksDBusAsyncǁrun__mutmut_6": xǁUDisksDBusAsyncǁrun__mutmut_6,
        "xǁUDisksDBusAsyncǁrun__mutmut_7": xǁUDisksDBusAsyncǁrun__mutmut_7,
        "xǁUDisksDBusAsyncǁrun__mutmut_8": xǁUDisksDBusAsyncǁrun__mutmut_8,
        "xǁUDisksDBusAsyncǁrun__mutmut_9": xǁUDisksDBusAsyncǁrun__mutmut_9,
        "xǁUDisksDBusAsyncǁrun__mutmut_10": xǁUDisksDBusAsyncǁrun__mutmut_10,
        "xǁUDisksDBusAsyncǁrun__mutmut_11": xǁUDisksDBusAsyncǁrun__mutmut_11,
        "xǁUDisksDBusAsyncǁrun__mutmut_12": xǁUDisksDBusAsyncǁrun__mutmut_12,
    }
    xǁUDisksDBusAsyncǁrun__mutmut_orig.__name__ = "xǁUDisksDBusAsyncǁrun"

    def close(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁclose__mutmut_orig"),
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁclose__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁclose__mutmut_orig(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_1(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_2(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(None)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_3(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = None
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_4(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = None
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_5(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = True
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_6(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = None
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_7(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(None)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_8(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(None, e)

    def xǁUDisksDBusAsyncǁclose__mutmut_9(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s", None
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_10(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(e)

    def xǁUDisksDBusAsyncǁclose__mutmut_11(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError:
            logging.error(
                "Caught exception while trying to close Udisks2 monitor: %s",
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_12(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "XXCaught exception while trying to close Udisks2 monitor: %sXX", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_13(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "caught exception while trying to close udisks2 monitor: %s", e
            )

    def xǁUDisksDBusAsyncǁclose__mutmut_14(self) -> None:
        """Close usb devices monitoring thread and run loop"""
        try:
            if not self.loop:
                return
            if self.loop.is_running():
                self.stop_event.set()
                self.loop.call_soon_threadsafe(self.loop.stop)
            _ = self.wait()
            self._active = False
            for path in self.controlled_devs.keys():
                dev: Device = self.controlled_devs.pop(path)
                dev.delete()
            self.terminate()
            self.deleteLater()
        except asyncio.CancelledError as e:
            logging.error(
                "CAUGHT EXCEPTION WHILE TRYING TO CLOSE UDISKS2 MONITOR: %S", e
            )

    xǁUDisksDBusAsyncǁclose__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁclose__mutmut_1": xǁUDisksDBusAsyncǁclose__mutmut_1,
        "xǁUDisksDBusAsyncǁclose__mutmut_2": xǁUDisksDBusAsyncǁclose__mutmut_2,
        "xǁUDisksDBusAsyncǁclose__mutmut_3": xǁUDisksDBusAsyncǁclose__mutmut_3,
        "xǁUDisksDBusAsyncǁclose__mutmut_4": xǁUDisksDBusAsyncǁclose__mutmut_4,
        "xǁUDisksDBusAsyncǁclose__mutmut_5": xǁUDisksDBusAsyncǁclose__mutmut_5,
        "xǁUDisksDBusAsyncǁclose__mutmut_6": xǁUDisksDBusAsyncǁclose__mutmut_6,
        "xǁUDisksDBusAsyncǁclose__mutmut_7": xǁUDisksDBusAsyncǁclose__mutmut_7,
        "xǁUDisksDBusAsyncǁclose__mutmut_8": xǁUDisksDBusAsyncǁclose__mutmut_8,
        "xǁUDisksDBusAsyncǁclose__mutmut_9": xǁUDisksDBusAsyncǁclose__mutmut_9,
        "xǁUDisksDBusAsyncǁclose__mutmut_10": xǁUDisksDBusAsyncǁclose__mutmut_10,
        "xǁUDisksDBusAsyncǁclose__mutmut_11": xǁUDisksDBusAsyncǁclose__mutmut_11,
        "xǁUDisksDBusAsyncǁclose__mutmut_12": xǁUDisksDBusAsyncǁclose__mutmut_12,
        "xǁUDisksDBusAsyncǁclose__mutmut_13": xǁUDisksDBusAsyncǁclose__mutmut_13,
        "xǁUDisksDBusAsyncǁclose__mutmut_14": xǁUDisksDBusAsyncǁclose__mutmut_14,
    }
    xǁUDisksDBusAsyncǁclose__mutmut_orig.__name__ = "xǁUDisksDBusAsyncǁclose"

    async def monitor_dbus(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_orig(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_1(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = None
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_2(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "XXaddXX": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_3(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "ADD": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_4(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "XXremXX": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_5(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "REM": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_6(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "XXpropXX": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_7(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "PROP": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_8(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = None
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_9(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=None,
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_10(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name=None,
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_11(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=None,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_12(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_13(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_14(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_15(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="XXMain-Restore-DiscoveryXX",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_16(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="main-restore-discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_17(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="MAIN-RESTORE-DISCOVERY",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_18(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = None
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_19(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = None
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_20(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(None, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_21(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=None)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_22(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_23(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(
                coro,
            )
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_24(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(None)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_25(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(None)
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_26(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: None)
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_27(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(None))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_28(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(None)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_29(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = None
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_30(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info(None, e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_31(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", None)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_32(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info(e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_33(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info(
                "UDisks2 Monitor stopped: %s",
            )
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_34(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("XXUDisks2 Monitor stopped: %sXX", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_35(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("udisks2 monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_36(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDISKS2 MONITOR STOPPED: %S", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_37(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = None
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_38(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = True
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_39(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error(None, e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_40(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError:
            logging.error(
                "Caught generic fatal Sdbus exception: %s", None, exc_info=True
            )
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_41(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=None)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_42(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error(e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_43(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError:
            logging.error("Caught generic fatal Sdbus exception: %s", exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_44(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error(
                "Caught generic fatal Sdbus exception: %s",
                e,
            )
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_45(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error(
                "XXCaught generic fatal Sdbus exception: %sXX", e, exc_info=True
            )
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_46(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("caught generic fatal sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_47(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("CAUGHT GENERIC FATAL SDBUS EXCEPTION: %S", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_48(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=False)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_49(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error(None, e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_50(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception:
            logging.error("Caught exception UDisks2 listeners failed: %s", None)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_51(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error(e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_52(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception:
            logging.error(
                "Caught exception UDisks2 listeners failed: %s",
            )
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_53(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("XXCaught exception UDisks2 listeners failed: %sXX", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_54(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("caught exception udisks2 listeners failed: %s", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_55(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("CAUGHT EXCEPTION UDISKS2 LISTENERS FAILED: %S", e)
            self._active = False

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_56(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = None

    async def xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_57(self) -> None:
        """Schedule coroutines for UDisks2 signals `interfaces_added`, `interfaces_removed`
        and `properties_changed`. Creates symlink upon device insertion and cleans up symlink on removal.

        """
        tasks: dict[str, Coroutine[typing.Any, typing.Any, typing.Any]] = {
            "add": self._add_interface_listener(),
            "rem": self._rem_interface_listener(),
            "prop": self._properties_changed_listener(),
        }
        _ = fire_n_forget(
            coro=self.restore_tracked(),
            name="Main-Restore-Discovery",
            task_stack=self.task_stack,
        )
        managed_tasks: list[asyncio.Task[typing.Any]] = []
        for name, coro in tasks.items():
            t = asyncio.create_task(coro, name=name)
            self.task_stack.add(t)
            t.add_done_callback(lambda _: self.task_stack.discard(t))
            managed_tasks.append(t)
        try:
            await asyncio.gather(*managed_tasks)
        except asyncio.CancelledError as e:
            for task in self.task_stack:
                _ = task.cancel()
            logging.info("UDisks2 Monitor stopped: %s", e)
            self._active = False
        except sdbus.SdBusBaseError as e:
            logging.error("Caught generic fatal Sdbus exception: %s", e, exc_info=True)
            self.close()
        except Exception as e:
            logging.error("Caught exception UDisks2 listeners failed: %s", e)
            self._active = True

    xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_1": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_1,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_2": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_2,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_3": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_3,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_4": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_4,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_5": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_5,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_6": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_6,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_7": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_7,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_8": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_8,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_9": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_9,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_10": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_10,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_11": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_11,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_12": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_12,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_13": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_13,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_14": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_14,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_15": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_15,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_16": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_16,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_17": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_17,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_18": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_18,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_19": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_19,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_20": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_20,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_21": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_21,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_22": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_22,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_23": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_23,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_24": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_24,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_25": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_25,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_26": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_26,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_27": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_27,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_28": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_28,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_29": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_29,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_30": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_30,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_31": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_31,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_32": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_32,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_33": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_33,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_34": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_34,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_35": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_35,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_36": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_36,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_37": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_37,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_38": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_38,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_39": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_39,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_40": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_40,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_41": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_41,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_42": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_42,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_43": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_43,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_44": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_44,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_45": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_45,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_46": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_46,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_47": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_47,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_48": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_48,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_49": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_49,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_50": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_50,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_51": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_51,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_52": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_52,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_53": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_53,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_54": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_54,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_55": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_55,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_56": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_56,
        "xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_57": xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_57,
    }
    xǁUDisksDBusAsyncǁmonitor_dbus__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁmonitor_dbus"
    )

    async def restore_tracked(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_orig(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_1(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = None
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_2(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=None,
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_3(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                name=None,
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_4(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                name=f"Restore-Discovery-{path}",
                task_stack=None,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_5(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_6(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_7(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, interfaces),
                name=f"Restore-Discovery-{path}",
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_8(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(None, interfaces),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_9(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(path, None),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_10(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(interfaces),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁrestore_tracked__mutmut_11(self) -> None:
        """Get and restore controlled mass storage devices"""
        info = await self.obj_manager.get_managed_objects()
        for path, interfaces in info.items():
            fire_n_forget(
                coro=self._handle_new_device(
                    path,
                ),
                name=f"Restore-Discovery-{path}",
                task_stack=self.task_stack,
            )

    xǁUDisksDBusAsyncǁrestore_tracked__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_1": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_1,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_2": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_2,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_3": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_3,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_4": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_4,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_5": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_5,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_6": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_6,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_7": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_7,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_8": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_8,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_9": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_9,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_10": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_10,
        "xǁUDisksDBusAsyncǁrestore_tracked__mutmut_11": xǁUDisksDBusAsyncǁrestore_tracked__mutmut_11,
    }
    xǁUDisksDBusAsyncǁrestore_tracked__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁrestore_tracked"
    )

    async def _add_interface_listener(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_orig(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, interfaces),
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_1(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                None,
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_2(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, interfaces),
                name=None,
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_3(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, interfaces),
                name=f"UDisks-Discovery-{path}",
                task_stack=None,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_4(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_5(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, interfaces),
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_6(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, interfaces),
                name=f"UDisks-Discovery-{path}",
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_7(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(None, interfaces),
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_8(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(path, None),
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_9(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(interfaces),
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    async def xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_10(self) -> None:
        """Handle add interface signal from UDisks2 DBus connection

        Adds the new device to internal tracking, can be retrieved with device path
        Creates symlink onto specified directory configured on the class
        """
        async for path, interfaces in self.obj_manager.interfaces_added:
            fire_n_forget(
                self._handle_new_device(
                    path,
                ),
                name=f"UDisks-Discovery-{path}",
                task_stack=self.task_stack,
            )

    xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_1": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_1,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_2": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_2,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_3": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_3,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_4": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_4,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_5": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_5,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_6": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_6,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_7": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_7,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_8": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_8,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_9": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_9,
        "xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_10": xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_10,
    }
    xǁUDisksDBusAsyncǁ_add_interface_listener__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_add_interface_listener"
    )

    async def _handle_new_device(self, path: str, interfaces) -> None:
        args = [path, interfaces]  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_orig(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_1(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value not in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_2(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = None
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_3(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=None,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_4(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=None,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_5(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=None,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_6(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_7(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_8(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_9(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = None
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_10(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    None,
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_11(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    None,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_12(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    None,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_13(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_14(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_15(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_16(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "XXNew Hardware device recognized type: %s \n  path: %sXX",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_17(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "new hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_18(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "NEW HARDWARE DEVICE RECOGNIZED TYPE: %S \n  PATH: %S",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_19(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = None
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_20(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    None, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_21(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, None, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_22(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, None
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_23(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_24(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_25(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable,
                    ddev.ejectable,
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_26(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if media_removable and ejectable and con_bus == "usb":
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_27(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable or con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_28(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable or ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_29(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus != "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_30(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "XXusbXX"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_31(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "USB"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_32(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = None
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_33(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    None, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_34(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=None, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_35(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(path, DriveInterface=ddev, symlink_path=None)
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_36(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_37(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(path, symlink_path=self.gcodes_path.as_posix())
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_38(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path,
                    DriveInterface=ddev,
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_39(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update(None)
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_40(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(None)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_41(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value not in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_42(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = None
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_43(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=None,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_44(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=None,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_45(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=None,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_46(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_47(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_48(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_49(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = None
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_50(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = None
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_51(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(None, bdev.hint_ignore)
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_52(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(bdev.hint_system, None)
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_53(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(bdev.hint_ignore)
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_54(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system,
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_55(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = None
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_56(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name and await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_57(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys and hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_58(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path not in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_59(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = None
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_60(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(None):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_61(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase not in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_62(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = None
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_63(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=None,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_64(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=None,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_65(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=None,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_66(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_67(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_68(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_69(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(None, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_70(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, None)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_71(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_72(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(
                            path,
                        )
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_73(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(None, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_74(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, None)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_75(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_76(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(
                            path,
                        )
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_77(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value not in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_78(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = None
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_79(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=None,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_80(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=None,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_81(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=None,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_82(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_83(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_84(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_85(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(None, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_86(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, None)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_87(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_88(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(
                            path,
                        )
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_89(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(None, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_90(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, None)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_91(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_92(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(
                            path,
                        )
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_93(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = None
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_94(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name and ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_95(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or "XXXX"
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_96(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(None, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_97(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, None)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_98(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(_label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_99(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(
                            dev,
                        )
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_100(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value not in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_101(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = None
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_102(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=None,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_103(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=None,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_104(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=None,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_105(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_106(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_107(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_108(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(None, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_109(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, None)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_110(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_111(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(
                            path,
                        )
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_112(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(None, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_113(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, None)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_114(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_115(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(
                        path,
                    )
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_116(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                None,
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_117(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                None,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_118(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=None,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_119(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_120(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_121(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_122(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "XXCaught exception on device inserted unknown method: %sXX",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_123(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_124(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "CAUGHT EXCEPTION ON DEVICE INSERTED UNKNOWN METHOD: %S",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_125(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=False,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_126(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                None,
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_127(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                None,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_128(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=None,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_129(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_130(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_131(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_132(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "XXCaught exception on device inserted unknown interface: %sXX",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_133(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_134(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "CAUGHT EXCEPTION ON DEVICE INSERTED UNKNOWN INTERFACE: %S",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_135(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=False,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_136(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                None,
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_137(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                None,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_138(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                None,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_139(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=None,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_140(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_141(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_142(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_143(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_144(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "XXCaught fatal exception during discovery process %s: %sXX",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_145(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_146(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "CAUGHT FATAL EXCEPTION DURING DISCOVERY PROCESS %S: %S",
                path,
                e,
                exc_info=True,
            )

    async def xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_147(
        self, path: str, interfaces
    ) -> None:
        """Handle new devices, can be used on `interfaces_added` signal and
        when recovering states from `get_managed_objects`

        """
        try:
            if Interfaces.Drive.value in interfaces:
                ddev: UDisks2DriveAsyncInterface = UDisks2DriveAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                hwbus: str = await ddev.connection_bus
                logging.debug(
                    "New Hardware device recognized type: %s \n  path: %s",
                    hwbus,
                    path,
                )
                media_removable, ejectable, con_bus = await asyncio.gather(
                    ddev.media_removable, ddev.ejectable, ddev.connection_bus
                )
                if not (media_removable and ejectable and con_bus == "usb"):
                    # Only handle usb devices and removable storage media
                    return
                device: Device = Device(
                    path, DriveInterface=ddev, symlink_path=self.gcodes_path.as_posix()
                )
                self.controlled_devs.update({path: device})
                self.hardware_detected[str].emit(path)
            if Interfaces.Block.value in interfaces:
                bdev: UDisks2BlockAsyncInterface = UDisks2BlockAsyncInterface.new_proxy(
                    service_name=UDisks2_service,
                    object_path=path,
                    bus=self.system_bus,
                )
                drv_path: str = await bdev.drive
                hint_sys, hint_ignore = await asyncio.gather(
                    bdev.hint_system, bdev.hint_ignore
                )
                dev_name = await bdev.hint_name or await bdev.id_label
                if hint_sys or hint_ignore:
                    # Always ignore device if these flags are set
                    return
                if drv_path in self.controlled_devs:
                    dev: Device = self.controlled_devs[drv_path]
                    if all(
                        phase in interfaces
                        for phase in (
                            Interfaces.PartitionTable.value,
                            Interfaces.Block.value,
                        )
                    ):
                        devpt: UDisks2PartitionTableAsyncInterface = (
                            UDisks2PartitionTableAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_raw_block(path, bdev)
                        dev.update_part_table(path, devpt)
                    if Interfaces.Filesystem.value in interfaces:
                        devfs: UDisks2FileSystemAsyncInterface = (
                            UDisks2FileSystemAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_file_system(path, devfs)
                        self.device_added.emit(path, interfaces)
                        _label = dev_name or ""
                        self.mount(dev, _label)
                    if Interfaces.Partition.value in interfaces:
                        devpart: UDisks2PartitionAsyncInterface = (
                            UDisks2PartitionAsyncInterface.new_proxy(
                                service_name=UDisks2_service,
                                object_path=path,
                                bus=self.system_bus,
                            )
                        )
                        dev.update_partitions(path, devpart)
                    dev.update_logical_blocks(path, bdev)
        except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
            logging.error(
                "Caught exception on device inserted unknown method: %s",
                e,
                exc_info=True,
            )
        except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
            logging.error(
                "Caught exception on device inserted unknown interface: %s",
                e,
                exc_info=True,
            )
        except Exception as e:
            logging.error(
                "Caught fatal exception during discovery process %s: %s",
                path,
                e,
                exc_info=False,
            )

    xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_1": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_1,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_2": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_2,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_3": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_3,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_4": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_4,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_5": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_5,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_6": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_6,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_7": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_7,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_8": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_8,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_9": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_9,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_10": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_10,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_11": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_11,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_12": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_12,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_13": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_13,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_14": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_14,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_15": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_15,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_16": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_16,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_17": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_17,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_18": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_18,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_19": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_19,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_20": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_20,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_21": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_21,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_22": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_22,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_23": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_23,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_24": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_24,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_25": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_25,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_26": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_26,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_27": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_27,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_28": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_28,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_29": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_29,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_30": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_30,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_31": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_31,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_32": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_32,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_33": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_33,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_34": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_34,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_35": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_35,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_36": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_36,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_37": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_37,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_38": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_38,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_39": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_39,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_40": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_40,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_41": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_41,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_42": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_42,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_43": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_43,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_44": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_44,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_45": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_45,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_46": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_46,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_47": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_47,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_48": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_48,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_49": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_49,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_50": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_50,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_51": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_51,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_52": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_52,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_53": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_53,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_54": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_54,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_55": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_55,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_56": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_56,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_57": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_57,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_58": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_58,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_59": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_59,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_60": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_60,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_61": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_61,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_62": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_62,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_63": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_63,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_64": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_64,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_65": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_65,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_66": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_66,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_67": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_67,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_68": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_68,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_69": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_69,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_70": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_70,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_71": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_71,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_72": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_72,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_73": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_73,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_74": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_74,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_75": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_75,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_76": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_76,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_77": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_77,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_78": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_78,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_79": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_79,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_80": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_80,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_81": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_81,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_82": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_82,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_83": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_83,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_84": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_84,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_85": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_85,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_86": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_86,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_87": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_87,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_88": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_88,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_89": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_89,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_90": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_90,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_91": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_91,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_92": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_92,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_93": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_93,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_94": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_94,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_95": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_95,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_96": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_96,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_97": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_97,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_98": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_98,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_99": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_99,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_100": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_100,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_101": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_101,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_102": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_102,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_103": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_103,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_104": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_104,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_105": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_105,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_106": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_106,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_107": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_107,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_108": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_108,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_109": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_109,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_110": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_110,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_111": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_111,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_112": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_112,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_113": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_113,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_114": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_114,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_115": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_115,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_116": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_116,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_117": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_117,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_118": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_118,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_119": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_119,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_120": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_120,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_121": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_121,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_122": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_122,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_123": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_123,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_124": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_124,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_125": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_125,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_126": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_126,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_127": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_127,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_128": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_128,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_129": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_129,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_130": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_130,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_131": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_131,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_132": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_132,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_133": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_133,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_134": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_134,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_135": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_135,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_136": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_136,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_137": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_137,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_138": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_138,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_139": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_139,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_140": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_140,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_141": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_141,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_142": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_142,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_143": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_143,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_144": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_144,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_145": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_145,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_146": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_146,
        "xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_147": xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_147,
    }
    xǁUDisksDBusAsyncǁ_handle_new_device__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_handle_new_device"
    )

    async def _properties_changed_listener(self) -> None:
        """Handle properties_changed signal from UDisks2 Dbus connection

        Updates tracked objects
        """
        async for (
            path,
            changed_properties,
            invalid_properties,
        ) in self.obj_manager.properties_changed:
            pass

    async def _rem_interface_listener(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_orig(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_1(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value not in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_2(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path not in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_3(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = None
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_4(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(None)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_5(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(None)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_6(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    None,
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_7(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    None,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_8(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=None,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_9(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_10(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_11(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_12(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "XXCaught exception on device removed unknown method: %sXX",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_13(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_14(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "CAUGHT EXCEPTION ON DEVICE REMOVED UNKNOWN METHOD: %S",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_15(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=False,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_16(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    None,
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_17(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    None,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_18(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=None,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_19(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_20(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_21(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_22(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "XXCaught exception on device removed unknown interface %sXX",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_23(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_24(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "CAUGHT EXCEPTION ON DEVICE REMOVED UNKNOWN INTERFACE %S",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_25(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=False,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_26(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    None,
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_27(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    None,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_28(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    None,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_29(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=None,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_30(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_31(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_32(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_33(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_34(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "XXCaught fatal exception on removed device: %s, %sXX",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_35(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_36(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "CAUGHT FATAL EXCEPTION ON REMOVED DEVICE: %S, %S",
                    path,
                    e,
                    exc_info=True,
                )

    async def xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_37(self) -> None:
        """Handle device removal signals from UDisks2 Dbus connection

        Removes tracked interface and cleans up any left behind data
        """
        async for path, interfaces in self.obj_manager.interfaces_removed:
            try:
                if Interfaces.Drive.value in interfaces:
                    if path in self.controlled_devs:
                        device: Device = self.controlled_devs.pop(path)
                        device.kill()
                        del device
                        self.hardware_removed[str].emit(path)
                self._cleanup_broken_symlinks()
            except sdbus.dbus_exceptions.DbusUnknownMethodError as e:
                logging.error(
                    "Caught exception on device removed unknown method: %s",
                    e,
                    exc_info=True,
                )
            except sdbus.dbus_exceptions.DbusUnknownInterfaceError as e:
                logging.error(
                    "Caught exception on device removed unknown interface %s",
                    e,
                    exc_info=True,
                )
            except Exception as e:
                logging.error(
                    "Caught fatal exception on removed device: %s, %s",
                    path,
                    e,
                    exc_info=False,
                )

    xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_1": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_1,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_2": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_2,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_3": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_3,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_4": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_4,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_5": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_5,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_6": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_6,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_7": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_7,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_8": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_8,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_9": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_9,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_10": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_10,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_11": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_11,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_12": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_12,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_13": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_13,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_14": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_14,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_15": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_15,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_16": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_16,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_17": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_17,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_18": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_18,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_19": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_19,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_20": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_20,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_21": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_21,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_22": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_22,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_23": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_23,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_24": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_24,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_25": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_25,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_26": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_26,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_27": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_27,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_28": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_28,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_29": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_29,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_30": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_30,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_31": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_31,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_32": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_32,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_33": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_33,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_34": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_34,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_35": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_35,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_36": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_36,
        "xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_37": xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_37,
    }
    xǁUDisksDBusAsyncǁ_rem_interface_listener__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_rem_interface_listener"
    )

    def mount(self, device: Device, label: str = ""):
        args = [device, label]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁmount__mutmut_orig"),
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁmount__mutmut_mutants"),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁmount__mutmut_orig(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_1(self, device: Device, label: str = "XXXX"):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_2(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = None

    def xǁUDisksDBusAsyncǁmount__mutmut_3(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=None,
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_4(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                name=None,
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_5(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                name=f"Mount-filesystem-{path}",
                task_stack=None,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_6(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_7(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_8(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, label),
                name=f"Mount-filesystem-{path}",
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_9(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(None, label),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_10(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(filesystem, None),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_11(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(label),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    def xǁUDisksDBusAsyncǁmount__mutmut_12(self, device: Device, label: str = ""):
        """Mounts the devices mountpoints"""
        for path, filesystem in device.file_systems.items():
            _ = fire_n_forget(
                coro=self._mount_filesystem(
                    filesystem,
                ),
                name=f"Mount-filesystem-{path}",
                task_stack=self.task_stack,
            )

    xǁUDisksDBusAsyncǁmount__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁmount__mutmut_1": xǁUDisksDBusAsyncǁmount__mutmut_1,
        "xǁUDisksDBusAsyncǁmount__mutmut_2": xǁUDisksDBusAsyncǁmount__mutmut_2,
        "xǁUDisksDBusAsyncǁmount__mutmut_3": xǁUDisksDBusAsyncǁmount__mutmut_3,
        "xǁUDisksDBusAsyncǁmount__mutmut_4": xǁUDisksDBusAsyncǁmount__mutmut_4,
        "xǁUDisksDBusAsyncǁmount__mutmut_5": xǁUDisksDBusAsyncǁmount__mutmut_5,
        "xǁUDisksDBusAsyncǁmount__mutmut_6": xǁUDisksDBusAsyncǁmount__mutmut_6,
        "xǁUDisksDBusAsyncǁmount__mutmut_7": xǁUDisksDBusAsyncǁmount__mutmut_7,
        "xǁUDisksDBusAsyncǁmount__mutmut_8": xǁUDisksDBusAsyncǁmount__mutmut_8,
        "xǁUDisksDBusAsyncǁmount__mutmut_9": xǁUDisksDBusAsyncǁmount__mutmut_9,
        "xǁUDisksDBusAsyncǁmount__mutmut_10": xǁUDisksDBusAsyncǁmount__mutmut_10,
        "xǁUDisksDBusAsyncǁmount__mutmut_11": xǁUDisksDBusAsyncǁmount__mutmut_11,
        "xǁUDisksDBusAsyncǁmount__mutmut_12": xǁUDisksDBusAsyncǁmount__mutmut_12,
    }
    xǁUDisksDBusAsyncǁmount__mutmut_orig.__name__ = "xǁUDisksDBusAsyncǁmount"

    async def _mount_filesystem(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        args = [filesystem, label]  # type: ignore
        kwargs = {}  # type: ignore
        return await _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_orig(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_1(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = "XXXX"
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_2(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = None
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_3(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(None)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_4(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = None
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_5(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "XXauto.no_user_interactionsXX": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_6(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "AUTO.NO_USER_INTERACTIONS": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_7(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("XXbXX", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_8(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("B", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_9(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", False),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_10(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "XXfstypeXX": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_11(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "FSTYPE": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_12(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("XXsXX", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_13(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("S", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_14(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "XXautoXX"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_15(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "AUTO"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_16(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "XXas-userXX": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_17(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "AS-USER": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_18(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("XXsXX", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_19(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("S", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_20(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get(None)),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_21(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("XXUSERXX")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_22(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("user")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_23(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "XXoptionsXX": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_24(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "OPTIONS": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_25(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("XXsXX", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_26(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("S", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_27(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "XXrw,relatime,syncXX"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_28(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "RW,RELATIME,SYNC"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_29(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = None
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_30(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(None)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_31(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=None, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_32(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=None, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_33(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(path=mnt_path, label=val_label, dst_path=None)
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_34(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_35(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(path=mnt_path, dst_path=self.gcodes_path.as_posix())
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_36(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path,
                label=val_label,
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_37(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException not in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_38(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[1]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_39(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    None,
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_40(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    None,
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_41(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_42(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_43(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "XXDevice filesystem already mounted on %s, verifying gcodes symlinkXX",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_44(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_45(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "DEVICE FILESYSTEM ALREADY MOUNTED ON %S, VERIFYING GCODES SYMLINK",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_46(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(None),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_47(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[2]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_48(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = None
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_49(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_50(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return "XXXX"
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_51(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = None
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_52(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip(None)
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_53(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode(None).strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_54(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[1].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_55(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("XXutf-8XX").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_56(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("UTF-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_57(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("XX\x00XX")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_58(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(None):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_59(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return "XXXX"
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_60(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=None,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_61(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=None,
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_62(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=None,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_63(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_64(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_65(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_66(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                None,
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_67(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                None,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_68(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                None,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_69(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=None,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_70(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_71(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_72(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_73(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_74(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "XXCaught exception while mounting file system %s : %sXX",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_75(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_76(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "CAUGHT EXCEPTION WHILE MOUNTING FILE SYSTEM %S : %S",
                filesystem,
                e,
                exc_info=True,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_77(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=False,
            )
        return ""

    async def xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_78(
        self, filesystem: UDisks2FileSystemAsyncInterface, label: str = ""
    ) -> str:
        val_label: str = validate_label(label)
        try:
            opts: dict[str, tuple[str, typing.Any]] = {
                "auto.no_user_interactions": ("b", True),
                "fstype": ("s", "auto"),
                "as-user": ("s", os.environ.get("USER")),
                "options": ("s", "rw,relatime,sync"),
            }
            mnt_path: str = await filesystem.mount(opts)
            return self.add_symlink(
                path=mnt_path, label=val_label, dst_path=self.gcodes_path.as_posix()
            )
        except sdbus.SdBusUnmappedMessageError as e:
            if AlreadyMountedException in e.args[0]:
                logging.debug(
                    "Device filesystem already mounted on %s, verifying gcodes symlink",
                    str(e.args[1]),
                )
                mount_points: list[bytes] = await filesystem.mount_points
                if not mount_points:
                    return ""
                mpoint: str = mount_points[0].decode("utf-8").strip("\x00")
                if os.path.exists(mpoint):
                    return ""
                return self.add_symlink(
                    path=mpoint,
                    dst_path=self.gcodes_path.as_posix(),
                    label=val_label,
                )
        except Exception as e:
            logging.error(
                "Caught exception while mounting file system %s : %s",
                filesystem,
                e,
                exc_info=True,
            )
        return "XXXX"

    xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_1": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_1,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_2": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_2,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_3": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_3,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_4": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_4,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_5": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_5,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_6": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_6,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_7": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_7,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_8": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_8,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_9": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_9,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_10": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_10,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_11": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_11,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_12": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_12,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_13": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_13,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_14": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_14,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_15": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_15,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_16": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_16,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_17": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_17,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_18": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_18,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_19": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_19,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_20": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_20,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_21": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_21,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_22": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_22,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_23": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_23,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_24": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_24,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_25": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_25,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_26": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_26,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_27": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_27,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_28": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_28,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_29": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_29,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_30": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_30,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_31": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_31,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_32": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_32,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_33": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_33,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_34": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_34,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_35": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_35,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_36": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_36,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_37": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_37,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_38": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_38,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_39": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_39,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_40": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_40,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_41": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_41,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_42": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_42,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_43": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_43,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_44": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_44,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_45": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_45,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_46": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_46,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_47": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_47,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_48": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_48,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_49": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_49,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_50": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_50,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_51": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_51,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_52": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_52,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_53": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_53,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_54": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_54,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_55": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_55,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_56": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_56,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_57": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_57,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_58": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_58,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_59": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_59,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_60": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_60,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_61": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_61,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_62": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_62,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_63": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_63,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_64": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_64,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_65": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_65,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_66": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_66,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_67": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_67,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_68": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_68,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_69": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_69,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_70": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_70,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_71": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_71,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_72": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_72,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_73": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_73,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_74": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_74,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_75": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_75,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_76": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_76,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_77": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_77,
        "xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_78": xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_78,
    }
    xǁUDisksDBusAsyncǁ_mount_filesystem__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_mount_filesystem"
    )

    def add_symlink(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        args = [path, dst_path, label, _index, _validated]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁadd_symlink__mutmut_orig"),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁadd_symlink__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_orig(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_1(
        self,
        path: str,
        dst_path: str,
        label: str = "XXXX",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_2(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 1,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_3(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = True,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_4(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated or label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_5(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_6(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = None
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_7(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(None, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_8(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=None)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_9(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_10(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(
                label,
            )
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_11(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=False)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_12(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = None
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_13(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" - label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_14(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "XXUSB-XX" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_15(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "usb-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_16(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = None
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_17(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "XXUSB DRIVEXX" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_18(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "usb drive" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_19(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index != 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_20(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 1 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_21(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(None)
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_22(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = None
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_23(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(None)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_24(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(None).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_25(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_26(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(None):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_27(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=None, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_28(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=None)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_29(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_30(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(
                    src=path,
                )
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_31(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(None) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_32(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else "XXXX"
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_33(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(None):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_34(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() != pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_35(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(None).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_36(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_37(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index = 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_38(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index -= 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_39(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 2
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_40(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index != 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_41(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 256:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_42(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return "XXXX"
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_43(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(None, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_44(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, None, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_45(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, None, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_46(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, None, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_47(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=None)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_48(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_49(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_50(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_51(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_52(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(
                    path,
                    dst_path,
                    label,
                    _index,
                )
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_53(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=False)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_54(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=None):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_55(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(None, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_56(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, None, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_57(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, None, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_58(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=None)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_59(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_60(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_61(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_62(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(
                    path,
                    dst_path,
                    label,
                )
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_63(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=False)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_64(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(None)
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_65(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "XXCaught fatal exception no permissions, unable to create symlink on specified pathXX"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_66(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_67(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "CAUGHT FATAL EXCEPTION NO PERMISSIONS, UNABLE TO CREATE SYMLINK ON SPECIFIED PATH"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_68(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error(None, e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_69(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError:
            logging.error("Caught fatal exception OSERROR %s", None)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_70(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error(e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_71(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError:
            logging.error(
                "Caught fatal exception OSERROR %s",
            )
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_72(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("XXCaught fatal exception OSERROR %sXX", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_73(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("caught fatal exception oserror %s", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_74(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("CAUGHT FATAL EXCEPTION OSERROR %S", e)
        return ""

    def xǁUDisksDBusAsyncǁadd_symlink__mutmut_75(
        self,
        path: str,
        dst_path: str,
        label: str = "",
        _index: int = 0,
        _validated: bool = False,
    ) -> str:
        """Create symlink on `dst_path`

        If there is a symlink created on `dst_path` with the same label,
        which points to the same `path` then it will return the `dst_path`
        as validation. If `dst_path` does not resolve to the same `path`
        then it will cleanup that symlink and create a replacement.

        In case there is no `label` then the created `symlink` on `dst_path`
        will default to **USB DRIVE**. If *USB DRIVE* symlink already exists
        then it will create a variant of that fallback **USB DRIVE [1-254]**

        Be careful with the provided directories and labels. They must come
        clean or else this method won't work.
        """
        if not _validated and label:
            label = validate_label(label, strict=True)
            label = "USB-" + label
        fallback: str = "USB DRIVE" if _index == 0 else str(f"USB DRIVE {_index}")
        dstb = pathlib.Path(dst_path).joinpath(label if label else fallback)
        try:
            if not os.path.islink(dstb):
                os.symlink(src=path, dst=dstb)
                return dstb.as_posix() if os.path.exists(dstb) else ""
            if os.path.islink(dstb):
                if dstb.resolve().as_posix() == pathlib.Path(path).as_posix():
                    return dstb.as_posix()
            if not label:
                _index += 1
                if _index == 255:
                    return ""
                return self.add_symlink(path, dst_path, label, _index, _validated=True)
            if self.rem_symlink(path=dstb.as_posix()):
                return self.add_symlink(path, dst_path, label, _validated=True)
        except PermissionError:
            logging.error(
                "Caught fatal exception no permissions, unable to create symlink on specified path"
            )
        except OSError as e:
            logging.error("Caught fatal exception OSERROR %s", e)
        return "XXXX"

    xǁUDisksDBusAsyncǁadd_symlink__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_1": xǁUDisksDBusAsyncǁadd_symlink__mutmut_1,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_2": xǁUDisksDBusAsyncǁadd_symlink__mutmut_2,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_3": xǁUDisksDBusAsyncǁadd_symlink__mutmut_3,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_4": xǁUDisksDBusAsyncǁadd_symlink__mutmut_4,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_5": xǁUDisksDBusAsyncǁadd_symlink__mutmut_5,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_6": xǁUDisksDBusAsyncǁadd_symlink__mutmut_6,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_7": xǁUDisksDBusAsyncǁadd_symlink__mutmut_7,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_8": xǁUDisksDBusAsyncǁadd_symlink__mutmut_8,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_9": xǁUDisksDBusAsyncǁadd_symlink__mutmut_9,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_10": xǁUDisksDBusAsyncǁadd_symlink__mutmut_10,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_11": xǁUDisksDBusAsyncǁadd_symlink__mutmut_11,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_12": xǁUDisksDBusAsyncǁadd_symlink__mutmut_12,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_13": xǁUDisksDBusAsyncǁadd_symlink__mutmut_13,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_14": xǁUDisksDBusAsyncǁadd_symlink__mutmut_14,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_15": xǁUDisksDBusAsyncǁadd_symlink__mutmut_15,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_16": xǁUDisksDBusAsyncǁadd_symlink__mutmut_16,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_17": xǁUDisksDBusAsyncǁadd_symlink__mutmut_17,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_18": xǁUDisksDBusAsyncǁadd_symlink__mutmut_18,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_19": xǁUDisksDBusAsyncǁadd_symlink__mutmut_19,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_20": xǁUDisksDBusAsyncǁadd_symlink__mutmut_20,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_21": xǁUDisksDBusAsyncǁadd_symlink__mutmut_21,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_22": xǁUDisksDBusAsyncǁadd_symlink__mutmut_22,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_23": xǁUDisksDBusAsyncǁadd_symlink__mutmut_23,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_24": xǁUDisksDBusAsyncǁadd_symlink__mutmut_24,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_25": xǁUDisksDBusAsyncǁadd_symlink__mutmut_25,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_26": xǁUDisksDBusAsyncǁadd_symlink__mutmut_26,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_27": xǁUDisksDBusAsyncǁadd_symlink__mutmut_27,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_28": xǁUDisksDBusAsyncǁadd_symlink__mutmut_28,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_29": xǁUDisksDBusAsyncǁadd_symlink__mutmut_29,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_30": xǁUDisksDBusAsyncǁadd_symlink__mutmut_30,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_31": xǁUDisksDBusAsyncǁadd_symlink__mutmut_31,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_32": xǁUDisksDBusAsyncǁadd_symlink__mutmut_32,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_33": xǁUDisksDBusAsyncǁadd_symlink__mutmut_33,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_34": xǁUDisksDBusAsyncǁadd_symlink__mutmut_34,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_35": xǁUDisksDBusAsyncǁadd_symlink__mutmut_35,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_36": xǁUDisksDBusAsyncǁadd_symlink__mutmut_36,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_37": xǁUDisksDBusAsyncǁadd_symlink__mutmut_37,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_38": xǁUDisksDBusAsyncǁadd_symlink__mutmut_38,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_39": xǁUDisksDBusAsyncǁadd_symlink__mutmut_39,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_40": xǁUDisksDBusAsyncǁadd_symlink__mutmut_40,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_41": xǁUDisksDBusAsyncǁadd_symlink__mutmut_41,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_42": xǁUDisksDBusAsyncǁadd_symlink__mutmut_42,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_43": xǁUDisksDBusAsyncǁadd_symlink__mutmut_43,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_44": xǁUDisksDBusAsyncǁadd_symlink__mutmut_44,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_45": xǁUDisksDBusAsyncǁadd_symlink__mutmut_45,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_46": xǁUDisksDBusAsyncǁadd_symlink__mutmut_46,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_47": xǁUDisksDBusAsyncǁadd_symlink__mutmut_47,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_48": xǁUDisksDBusAsyncǁadd_symlink__mutmut_48,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_49": xǁUDisksDBusAsyncǁadd_symlink__mutmut_49,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_50": xǁUDisksDBusAsyncǁadd_symlink__mutmut_50,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_51": xǁUDisksDBusAsyncǁadd_symlink__mutmut_51,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_52": xǁUDisksDBusAsyncǁadd_symlink__mutmut_52,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_53": xǁUDisksDBusAsyncǁadd_symlink__mutmut_53,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_54": xǁUDisksDBusAsyncǁadd_symlink__mutmut_54,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_55": xǁUDisksDBusAsyncǁadd_symlink__mutmut_55,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_56": xǁUDisksDBusAsyncǁadd_symlink__mutmut_56,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_57": xǁUDisksDBusAsyncǁadd_symlink__mutmut_57,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_58": xǁUDisksDBusAsyncǁadd_symlink__mutmut_58,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_59": xǁUDisksDBusAsyncǁadd_symlink__mutmut_59,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_60": xǁUDisksDBusAsyncǁadd_symlink__mutmut_60,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_61": xǁUDisksDBusAsyncǁadd_symlink__mutmut_61,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_62": xǁUDisksDBusAsyncǁadd_symlink__mutmut_62,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_63": xǁUDisksDBusAsyncǁadd_symlink__mutmut_63,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_64": xǁUDisksDBusAsyncǁadd_symlink__mutmut_64,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_65": xǁUDisksDBusAsyncǁadd_symlink__mutmut_65,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_66": xǁUDisksDBusAsyncǁadd_symlink__mutmut_66,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_67": xǁUDisksDBusAsyncǁadd_symlink__mutmut_67,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_68": xǁUDisksDBusAsyncǁadd_symlink__mutmut_68,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_69": xǁUDisksDBusAsyncǁadd_symlink__mutmut_69,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_70": xǁUDisksDBusAsyncǁadd_symlink__mutmut_70,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_71": xǁUDisksDBusAsyncǁadd_symlink__mutmut_71,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_72": xǁUDisksDBusAsyncǁadd_symlink__mutmut_72,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_73": xǁUDisksDBusAsyncǁadd_symlink__mutmut_73,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_74": xǁUDisksDBusAsyncǁadd_symlink__mutmut_74,
        "xǁUDisksDBusAsyncǁadd_symlink__mutmut_75": xǁUDisksDBusAsyncǁadd_symlink__mutmut_75,
    }
    xǁUDisksDBusAsyncǁadd_symlink__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁadd_symlink"
    )

    def rem_symlink(self, path: str | pathlib.Path) -> bool:
        args = [path]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(self, "xǁUDisksDBusAsyncǁrem_symlink__mutmut_orig"),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁrem_symlink__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_orig(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_1(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = None
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_2(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(None)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_3(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = None
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_4(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(None).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_5(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = None
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_6(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(None)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_7(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error(None, path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_8(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", None)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_9(self, path: str | pathlib.Path) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error(path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_10(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error(
                "Path transversal attempt in rem_symlink: %s",
            )
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_11(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("XXPath transversal attempt in rem_symlink: %sXX", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_12(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_13(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("PATH TRANSVERSAL ATTEMPT IN REM_SYMLINK: %S", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_14(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return True

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_15(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_16(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(None):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_17(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error(None, path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_18(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", None)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_19(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error(path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_20(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error(
                "Provided path %s is NOT a symlink, refusing to delete",
            )
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_21(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error(
                "XXProvided path %s is NOT a symlink, refusing to deleteXX", path
            )
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_22(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("provided path %s is not a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_23(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("PROVIDED PATH %S IS NOT A SYMLINK, REFUSING TO DELETE", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_24(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return True
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_25(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(None)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_26(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return False
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_27(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error(None, path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_28(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", None)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_29(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error(path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_30(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error(
                "Caught fatal exception failed to remove symlink %s",
            )
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_31(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error(
                "XXCaught fatal exception failed to remove symlink %sXX", path
            )
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_32(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("caught fatal exception failed to remove symlink %s", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_33(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("CAUGHT FATAL EXCEPTION FAILED TO REMOVE SYMLINK %S", path)
        return False

    def xǁUDisksDBusAsyncǁrem_symlink__mutmut_34(
        self, path: str | pathlib.Path
    ) -> bool:
        """Remove `ONLY` symlinks located in `path` if it is allowed"""
        resolved_path = pathlib.Path(path)
        resolved_gcodes_path = pathlib.Path(self.gcodes_path).as_posix()
        try:
            _ = resolved_path.relative_to(resolved_gcodes_path)
        except ValueError:
            logging.error("Path transversal attempt in rem_symlink: %s", path)
            return False

        if not os.path.islink(resolved_path):
            logging.error("Provided path %s is NOT a symlink, refusing to delete", path)
            return False
        try:
            os.remove(resolved_path)
            return True
        except (PermissionError, OSError):
            logging.error("Caught fatal exception failed to remove symlink %s", path)
        return True

    xǁUDisksDBusAsyncǁrem_symlink__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_1": xǁUDisksDBusAsyncǁrem_symlink__mutmut_1,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_2": xǁUDisksDBusAsyncǁrem_symlink__mutmut_2,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_3": xǁUDisksDBusAsyncǁrem_symlink__mutmut_3,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_4": xǁUDisksDBusAsyncǁrem_symlink__mutmut_4,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_5": xǁUDisksDBusAsyncǁrem_symlink__mutmut_5,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_6": xǁUDisksDBusAsyncǁrem_symlink__mutmut_6,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_7": xǁUDisksDBusAsyncǁrem_symlink__mutmut_7,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_8": xǁUDisksDBusAsyncǁrem_symlink__mutmut_8,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_9": xǁUDisksDBusAsyncǁrem_symlink__mutmut_9,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_10": xǁUDisksDBusAsyncǁrem_symlink__mutmut_10,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_11": xǁUDisksDBusAsyncǁrem_symlink__mutmut_11,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_12": xǁUDisksDBusAsyncǁrem_symlink__mutmut_12,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_13": xǁUDisksDBusAsyncǁrem_symlink__mutmut_13,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_14": xǁUDisksDBusAsyncǁrem_symlink__mutmut_14,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_15": xǁUDisksDBusAsyncǁrem_symlink__mutmut_15,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_16": xǁUDisksDBusAsyncǁrem_symlink__mutmut_16,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_17": xǁUDisksDBusAsyncǁrem_symlink__mutmut_17,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_18": xǁUDisksDBusAsyncǁrem_symlink__mutmut_18,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_19": xǁUDisksDBusAsyncǁrem_symlink__mutmut_19,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_20": xǁUDisksDBusAsyncǁrem_symlink__mutmut_20,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_21": xǁUDisksDBusAsyncǁrem_symlink__mutmut_21,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_22": xǁUDisksDBusAsyncǁrem_symlink__mutmut_22,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_23": xǁUDisksDBusAsyncǁrem_symlink__mutmut_23,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_24": xǁUDisksDBusAsyncǁrem_symlink__mutmut_24,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_25": xǁUDisksDBusAsyncǁrem_symlink__mutmut_25,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_26": xǁUDisksDBusAsyncǁrem_symlink__mutmut_26,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_27": xǁUDisksDBusAsyncǁrem_symlink__mutmut_27,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_28": xǁUDisksDBusAsyncǁrem_symlink__mutmut_28,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_29": xǁUDisksDBusAsyncǁrem_symlink__mutmut_29,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_30": xǁUDisksDBusAsyncǁrem_symlink__mutmut_30,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_31": xǁUDisksDBusAsyncǁrem_symlink__mutmut_31,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_32": xǁUDisksDBusAsyncǁrem_symlink__mutmut_32,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_33": xǁUDisksDBusAsyncǁrem_symlink__mutmut_33,
        "xǁUDisksDBusAsyncǁrem_symlink__mutmut_34": xǁUDisksDBusAsyncǁrem_symlink__mutmut_34,
    }
    xǁUDisksDBusAsyncǁrem_symlink__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁrem_symlink"
    )

    def _cleanup_symlinks(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_orig(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                _ = self.rem_symlink(dir.as_posix())

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_1(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob(None):
            if os.path.islink(dir):
                _ = self.rem_symlink(dir.as_posix())

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_2(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob("XX*XX"):
            if os.path.islink(dir):
                _ = self.rem_symlink(dir.as_posix())

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_3(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(None):
                _ = self.rem_symlink(dir.as_posix())

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_4(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                _ = None

    def xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_5(self) -> None:
        """Cleanup all symlinks on gcodes directory

        This method is private, if used outside of it's intended purpose
        devices will lose track of what symlinks are associated with them

        USE WITH CARE
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                _ = self.rem_symlink(None)

    xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_1": xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_1,
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_2": xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_2,
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_3": xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_3,
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_4": xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_4,
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_5": xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_5,
    }
    xǁUDisksDBusAsyncǁ_cleanup_symlinks__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_cleanup_symlinks"
    )

    def _cleanup_broken_symlinks(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_orig(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) and not os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_1(self) -> None:
        for dir in self.gcodes_path.rglob(None):
            if os.path.islink(dir) and not os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_2(self) -> None:
        for dir in self.gcodes_path.rglob("XX*XX"):
            if os.path.islink(dir) and not os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_3(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) or not os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_4(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(None) and not os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_5(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) and os.path.exists(dir):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_6(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) and not os.path.exists(None):
                _ = self.rem_symlink(dir)

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_7(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) and not os.path.exists(dir):
                _ = None

    def xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_8(self) -> None:
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir) and not os.path.exists(dir):
                _ = self.rem_symlink(None)

    xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_1": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_1,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_2": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_2,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_3": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_3,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_4": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_4,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_5": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_5,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_6": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_6,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_7": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_7,
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_8": xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_8,
    }
    xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_cleanup_broken_symlinks"
    )

    def _resolve_symlinks(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        args = [path, mount_path]  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_orig(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_1(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob(None):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_2(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("XX*XX"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_3(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(None):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_4(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(None).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_5(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    != pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_6(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(None).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_7(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return False
        return False

    def xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_8(
        self, path: str | pathlib.Path, mount_path: str | pathlib.Path
    ) -> bool:
        """Checks if `gcodes directory` already has a symlink that
        resolves to the same mount directory.


        This method returns on the first encounter. It does not
        evaluate any other symlinks after that.
        """
        for dir in self.gcodes_path.rglob("*"):
            if os.path.islink(dir):
                if (
                    pathlib.Path(path).resolve().as_posix()
                    == pathlib.Path(mount_path).as_posix()
                ):
                    # This `path` resolves to the `mount_path`
                    return True
        return True

    xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_1": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_1,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_2": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_2,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_3": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_3,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_4": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_4,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_5": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_5,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_6": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_6,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_7": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_7,
        "xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_8": xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_8,
    }
    xǁUDisksDBusAsyncǁ_resolve_symlinks__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_resolve_symlinks"
    )

    def _cleanup_legacy_dir(self) -> None:
        args = []  # type: ignore
        kwargs = {}  # type: ignore
        return _mutmut_trampoline(
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_orig"
            ),
            object.__getattribute__(
                self, "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_mutants"
            ),
            args,
            kwargs,
            self,
        )

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_orig(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_1(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = None
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_2(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath(None)
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_3(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("XXUSBXX")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_4(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("usb")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_5(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() or not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_6(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_7(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() and legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_8(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(None)
        except Exception:
            logging.debug("Failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_9(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug(None)
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_10(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("XXFailed to remove legacy dirXX")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_11(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("failed to remove legacy dir")
            return

    def xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_12(self) -> None:
        """Removes legacy directory that contained symlinks
        for all mounted USB devices on the machine
        """
        try:
            legacy_dir = self.gcodes_path.joinpath("USB")
            if legacy_dir.is_dir() and not (
                legacy_dir.is_symlink() or legacy_dir.is_file()
            ):
                shutil.rmtree(legacy_dir)
        except Exception:
            logging.debug("FAILED TO REMOVE LEGACY DIR")
            return

    xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_mutants: ClassVar[MutantDict] = {  # type: ignore
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_1": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_1,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_2": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_2,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_3": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_3,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_4": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_4,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_5": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_5,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_6": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_6,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_7": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_7,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_8": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_8,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_9": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_9,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_10": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_10,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_11": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_11,
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_12": xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_12,
    }
    xǁUDisksDBusAsyncǁ_cleanup_legacy_dir__mutmut_orig.__name__ = (
        "xǁUDisksDBusAsyncǁ_cleanup_legacy_dir"
    )
