from __future__ import annotations

import logging
import typing

import events
from lib.moonrakerComm import MoonWebSocket
from PyQt6 import QtCore, QtWidgets

logger = logging.getLogger(__name__)
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class Printer(QtCore.QObject):
    request_available_objects_signal = QtCore.pyqtSignal(name="get_available_objects")
    request_object_subscription_signal = QtCore.pyqtSignal(
        dict, name="object_subscription"
    )
    toolhead_number_signal = QtCore.pyqtSignal(int, name="toolhead_number_received")

    on_printcore_update = QtCore.pyqtSignal(dict, name="on_printcore_update")

    extruder_update = QtCore.pyqtSignal(str, str, float, name="extruder_update")
    heater_bed_update = QtCore.pyqtSignal(str, str, float, name="heater_bed_update")
    fan_update = QtCore.pyqtSignal(
        [str, str, float], [str, str, int], name="fan_update"
    )
    chamber_update = QtCore.pyqtSignal(name="chamber_update")

    idle_timeout_update = QtCore.pyqtSignal(
        [str, float], [str, str], name="idle_timeout_update"
    )

    save_variables_update = QtCore.pyqtSignal(dict, name="save_variables_update")

    gcode_move_update = QtCore.pyqtSignal(
        [str, list], [str, float], [str, bool], name="gcode_move_update"
    )
    toolhead_update = QtCore.pyqtSignal(
        [str, float], [str, list], [str, str], name="toolhead_update"
    )
    virtual_sdcard_update = QtCore.pyqtSignal(
        [str, float], [str, bool], name="virtual_sdcard_update"
    )
    print_stats_update = QtCore.pyqtSignal(
        [str, dict], [str, float], [str, str], name="print_stats_update"
    )
    mmu_update = QtCore.pyqtSignal(
        [str, dict], [str, list], [str, int], [str, str], name="mmu_update"
    )

    flowguard_update = QtCore.pyqtSignal([str, dict], name="flowguard_update")

    display_update = QtCore.pyqtSignal([str, str], [str, float], name="display_update")
    temperature_sensor_update = QtCore.pyqtSignal(
        str, str, float, name="temperature_sensor_update"
    )
    temperature_fan_update = QtCore.pyqtSignal(
        str, str, float, name="temperature_fan_update"
    )
    filament_switch_sensor_update = QtCore.pyqtSignal(
        str, str, bool, name="filament_switch_sensor_update"
    )
    filament_motion_sensor_update = QtCore.pyqtSignal(
        str, str, bool, name="filament_motion_sensor_update"
    )
    output_pin_update = QtCore.pyqtSignal(str, str, float, name="output_pin_update")
    bed_mesh_update = QtCore.pyqtSignal(
        str, list, list, list, list, name="bed_mesh_update"
    )
    gcode_macro_update = QtCore.pyqtSignal(str, dict, name="gcode_macro_update")
    webhooks_update = QtCore.pyqtSignal(str, str, name="webhooks_update")

    load_filament_update = QtCore.pyqtSignal(dict, name="load_filament_update")
    unload_filament_update = QtCore.pyqtSignal(dict, name="unload_filament_update")

    query_printer_object = QtCore.pyqtSignal(dict, name="query_printer_object")
    save_config_pending: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        name="save_config_pending"
    )
    printer_config: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        dict, name="printer_config"
    )
    configfile_update: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        dict, name="configfile_update"
    )
    z_tilt_update: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, bool, name="z_tilt_update"
    )
    config_subscription: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        [dict],
        [list],
        name="config_subscription",
    )
    manual_probe_update: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        dict, name="manual_probe_update"
    )
    available_gcode_cmds: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        dict, name="available_gcode_cmds"
    )
    gcode_response: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        list, name="gcode_response"
    )

    klippy_state_changed: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="klippy-state-changed"
    )
    object_updated: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, str, dict, name="object-updated"
    )

    extruder_number: int = 0
    available_gcode_commands: dict = {}
    available_objects: dict = {}
    configfile: dict = {}
    printing: bool = False
    printing_state: str = ""
    print_file_loaded: bool = False
    printer_busy: bool = False
    current_loaded_file: str = ""
    current_loaded_file_metadata: str = ""

    def __init__(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        args = [parent, ws]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ__init____mutmut_orig(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_1(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(None)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_2(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(None, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_3(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, None).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_4(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_5(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, ).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_6(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = None
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_7(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = None
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_8(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = "XXXX"
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_9(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = None
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_10(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = None

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_11(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = True

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_12(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = None
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_13(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "XXcurrent_temperatureXX": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_14(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "CURRENT_TEMPERATURE": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_15(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 1.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_16(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "XXtarget_temperatureXX": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_17(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "TARGET_TEMPERATURE": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_18(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 1.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_19(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "XXcan_extrudeXX": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_20(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "CAN_EXTRUDE": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_21(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": True,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_22(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = None

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_23(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "XXextruderXX": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_24(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "EXTRUDER": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_25(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "XXbedXX": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_26(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "BED": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_27(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(None)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_28(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(None)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_29(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(None)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_30(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(None)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_31(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = ""
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = {}

    def xǁPrinterǁ__init____mutmut_32(self, parent: QtCore.QObject, ws: MoonWebSocket, /) -> None:
        super(Printer, self).__init__(parent)

        self.ws = ws
        self.active_extruder_name: str = ""
        self.available_filament_sensors: dict = {}
        self.has_chamber: bool = False

        _heater_attributes: dict = {
            "current_temperature": 0.0,
            "target_temperature": 0.0,
            "can_extrude": False,
        }
        self.heaters_object: dict = {
            "extruder": _heater_attributes.copy(),
            "bed": _heater_attributes.copy(),
        }

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.request_available_objects_signal.connect(self.ws.api.get_available_objects)
        self.request_object_subscription_signal.connect(self.ws.api.object_subscription)
        self.query_printer_object.connect(self.ws.api.object_query)
        self._klippy_callback: typing.Callable[[str], None] | None = None
        self._callbacks: dict[str, typing.Callable[[dict, str], None]] = None
    
    xǁPrinterǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ__init____mutmut_1': xǁPrinterǁ__init____mutmut_1, 
        'xǁPrinterǁ__init____mutmut_2': xǁPrinterǁ__init____mutmut_2, 
        'xǁPrinterǁ__init____mutmut_3': xǁPrinterǁ__init____mutmut_3, 
        'xǁPrinterǁ__init____mutmut_4': xǁPrinterǁ__init____mutmut_4, 
        'xǁPrinterǁ__init____mutmut_5': xǁPrinterǁ__init____mutmut_5, 
        'xǁPrinterǁ__init____mutmut_6': xǁPrinterǁ__init____mutmut_6, 
        'xǁPrinterǁ__init____mutmut_7': xǁPrinterǁ__init____mutmut_7, 
        'xǁPrinterǁ__init____mutmut_8': xǁPrinterǁ__init____mutmut_8, 
        'xǁPrinterǁ__init____mutmut_9': xǁPrinterǁ__init____mutmut_9, 
        'xǁPrinterǁ__init____mutmut_10': xǁPrinterǁ__init____mutmut_10, 
        'xǁPrinterǁ__init____mutmut_11': xǁPrinterǁ__init____mutmut_11, 
        'xǁPrinterǁ__init____mutmut_12': xǁPrinterǁ__init____mutmut_12, 
        'xǁPrinterǁ__init____mutmut_13': xǁPrinterǁ__init____mutmut_13, 
        'xǁPrinterǁ__init____mutmut_14': xǁPrinterǁ__init____mutmut_14, 
        'xǁPrinterǁ__init____mutmut_15': xǁPrinterǁ__init____mutmut_15, 
        'xǁPrinterǁ__init____mutmut_16': xǁPrinterǁ__init____mutmut_16, 
        'xǁPrinterǁ__init____mutmut_17': xǁPrinterǁ__init____mutmut_17, 
        'xǁPrinterǁ__init____mutmut_18': xǁPrinterǁ__init____mutmut_18, 
        'xǁPrinterǁ__init____mutmut_19': xǁPrinterǁ__init____mutmut_19, 
        'xǁPrinterǁ__init____mutmut_20': xǁPrinterǁ__init____mutmut_20, 
        'xǁPrinterǁ__init____mutmut_21': xǁPrinterǁ__init____mutmut_21, 
        'xǁPrinterǁ__init____mutmut_22': xǁPrinterǁ__init____mutmut_22, 
        'xǁPrinterǁ__init____mutmut_23': xǁPrinterǁ__init____mutmut_23, 
        'xǁPrinterǁ__init____mutmut_24': xǁPrinterǁ__init____mutmut_24, 
        'xǁPrinterǁ__init____mutmut_25': xǁPrinterǁ__init____mutmut_25, 
        'xǁPrinterǁ__init____mutmut_26': xǁPrinterǁ__init____mutmut_26, 
        'xǁPrinterǁ__init____mutmut_27': xǁPrinterǁ__init____mutmut_27, 
        'xǁPrinterǁ__init____mutmut_28': xǁPrinterǁ__init____mutmut_28, 
        'xǁPrinterǁ__init____mutmut_29': xǁPrinterǁ__init____mutmut_29, 
        'xǁPrinterǁ__init____mutmut_30': xǁPrinterǁ__init____mutmut_30, 
        'xǁPrinterǁ__init____mutmut_31': xǁPrinterǁ__init____mutmut_31, 
        'xǁPrinterǁ__init____mutmut_32': xǁPrinterǁ__init____mutmut_32
    }
    xǁPrinterǁ__init____mutmut_orig.__name__ = 'xǁPrinterǁ__init__'

    def clear_printer_objs(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁclear_printer_objs__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁclear_printer_objs__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁclear_printer_objs__mutmut_orig(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_1(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = None
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_2(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = True
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_3(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = None
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_4(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = "XXXX"
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_5(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = None
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_6(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = True
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_7(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = None
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_8(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = True
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_9(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = None
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_10(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = "XXXX"
        self.current_loaded_file_metadata = ""

    def xǁPrinterǁclear_printer_objs__mutmut_11(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = None

    def xǁPrinterǁclear_printer_objs__mutmut_12(self) -> None:
        """Clear all tracking of printer object"""
        self.available_gcode_commands.clear()
        self.available_objects.clear()
        self.configfile.clear()
        self.printing = False
        self.printing_state = ""
        self.print_file_loaded = False
        self.printer_busy = False
        self.current_loaded_file = ""
        self.current_loaded_file_metadata = "XXXX"
    
    xǁPrinterǁclear_printer_objs__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁclear_printer_objs__mutmut_1': xǁPrinterǁclear_printer_objs__mutmut_1, 
        'xǁPrinterǁclear_printer_objs__mutmut_2': xǁPrinterǁclear_printer_objs__mutmut_2, 
        'xǁPrinterǁclear_printer_objs__mutmut_3': xǁPrinterǁclear_printer_objs__mutmut_3, 
        'xǁPrinterǁclear_printer_objs__mutmut_4': xǁPrinterǁclear_printer_objs__mutmut_4, 
        'xǁPrinterǁclear_printer_objs__mutmut_5': xǁPrinterǁclear_printer_objs__mutmut_5, 
        'xǁPrinterǁclear_printer_objs__mutmut_6': xǁPrinterǁclear_printer_objs__mutmut_6, 
        'xǁPrinterǁclear_printer_objs__mutmut_7': xǁPrinterǁclear_printer_objs__mutmut_7, 
        'xǁPrinterǁclear_printer_objs__mutmut_8': xǁPrinterǁclear_printer_objs__mutmut_8, 
        'xǁPrinterǁclear_printer_objs__mutmut_9': xǁPrinterǁclear_printer_objs__mutmut_9, 
        'xǁPrinterǁclear_printer_objs__mutmut_10': xǁPrinterǁclear_printer_objs__mutmut_10, 
        'xǁPrinterǁclear_printer_objs__mutmut_11': xǁPrinterǁclear_printer_objs__mutmut_11, 
        'xǁPrinterǁclear_printer_objs__mutmut_12': xǁPrinterǁclear_printer_objs__mutmut_12
    }
    xǁPrinterǁclear_printer_objs__mutmut_orig.__name__ = 'xǁPrinterǁclear_printer_objs'

    def __inject_callback(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        args = [object_type, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ__inject_callback__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ__inject_callback__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ__inject_callback__mutmut_orig(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_1(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type and not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_2(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_3(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_4(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(None):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_5(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                None,
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_6(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                None,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_7(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                None,
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_8(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_9(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_10(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_11(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "XXregister_callback: invalid args object_type=%r callable=%sXX",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_12(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "REGISTER_CALLBACK: INVALID ARGS OBJECT_TYPE=%R CALLABLE=%S",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_13(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(None),
            )
            return
        self._callbacks[object_type] = callback

    def xǁPrinterǁ__inject_callback__mutmut_14(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Internal: insert a validated callback into the dispatch table."""
        if not object_type or not callable(callback):
            logger.warning(
                "register_callback: invalid args object_type=%r callable=%s",
                object_type,
                callable(callback),
            )
            return
        self._callbacks[object_type] = None
    
    xǁPrinterǁ__inject_callback__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ__inject_callback__mutmut_1': xǁPrinterǁ__inject_callback__mutmut_1, 
        'xǁPrinterǁ__inject_callback__mutmut_2': xǁPrinterǁ__inject_callback__mutmut_2, 
        'xǁPrinterǁ__inject_callback__mutmut_3': xǁPrinterǁ__inject_callback__mutmut_3, 
        'xǁPrinterǁ__inject_callback__mutmut_4': xǁPrinterǁ__inject_callback__mutmut_4, 
        'xǁPrinterǁ__inject_callback__mutmut_5': xǁPrinterǁ__inject_callback__mutmut_5, 
        'xǁPrinterǁ__inject_callback__mutmut_6': xǁPrinterǁ__inject_callback__mutmut_6, 
        'xǁPrinterǁ__inject_callback__mutmut_7': xǁPrinterǁ__inject_callback__mutmut_7, 
        'xǁPrinterǁ__inject_callback__mutmut_8': xǁPrinterǁ__inject_callback__mutmut_8, 
        'xǁPrinterǁ__inject_callback__mutmut_9': xǁPrinterǁ__inject_callback__mutmut_9, 
        'xǁPrinterǁ__inject_callback__mutmut_10': xǁPrinterǁ__inject_callback__mutmut_10, 
        'xǁPrinterǁ__inject_callback__mutmut_11': xǁPrinterǁ__inject_callback__mutmut_11, 
        'xǁPrinterǁ__inject_callback__mutmut_12': xǁPrinterǁ__inject_callback__mutmut_12, 
        'xǁPrinterǁ__inject_callback__mutmut_13': xǁPrinterǁ__inject_callback__mutmut_13, 
        'xǁPrinterǁ__inject_callback__mutmut_14': xǁPrinterǁ__inject_callback__mutmut_14
    }
    xǁPrinterǁ__inject_callback__mutmut_orig.__name__ = 'xǁPrinterǁ__inject_callback'

    def register_callback(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        args = [object_type, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁregister_callback__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁregister_callback__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁregister_callback__mutmut_orig(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Register an external callback for a Moonraker object type.

        When Moonraker sends an update for ``object_type``, ``callback(values, name)``
        is called directly — no intermediate signal required.

        Args:
            object_type: Moonraker object key, e.g. ``"mmu"`` or ``"filament_switch_sensor"``.
            callback: Callable with signature ``(values: dict, name: str) -> None``.
        """
        self.__inject_callback(object_type, callback)

    def xǁPrinterǁregister_callback__mutmut_1(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Register an external callback for a Moonraker object type.

        When Moonraker sends an update for ``object_type``, ``callback(values, name)``
        is called directly — no intermediate signal required.

        Args:
            object_type: Moonraker object key, e.g. ``"mmu"`` or ``"filament_switch_sensor"``.
            callback: Callable with signature ``(values: dict, name: str) -> None``.
        """
        self.__inject_callback(None, callback)

    def xǁPrinterǁregister_callback__mutmut_2(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Register an external callback for a Moonraker object type.

        When Moonraker sends an update for ``object_type``, ``callback(values, name)``
        is called directly — no intermediate signal required.

        Args:
            object_type: Moonraker object key, e.g. ``"mmu"`` or ``"filament_switch_sensor"``.
            callback: Callable with signature ``(values: dict, name: str) -> None``.
        """
        self.__inject_callback(object_type, None)

    def xǁPrinterǁregister_callback__mutmut_3(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Register an external callback for a Moonraker object type.

        When Moonraker sends an update for ``object_type``, ``callback(values, name)``
        is called directly — no intermediate signal required.

        Args:
            object_type: Moonraker object key, e.g. ``"mmu"`` or ``"filament_switch_sensor"``.
            callback: Callable with signature ``(values: dict, name: str) -> None``.
        """
        self.__inject_callback(callback)

    def xǁPrinterǁregister_callback__mutmut_4(
        self, object_type: str, callback: typing.Callable[[dict, str], None]
    ) -> None:
        """Register an external callback for a Moonraker object type.

        When Moonraker sends an update for ``object_type``, ``callback(values, name)``
        is called directly — no intermediate signal required.

        Args:
            object_type: Moonraker object key, e.g. ``"mmu"`` or ``"filament_switch_sensor"``.
            callback: Callable with signature ``(values: dict, name: str) -> None``.
        """
        self.__inject_callback(object_type, )
    
    xǁPrinterǁregister_callback__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁregister_callback__mutmut_1': xǁPrinterǁregister_callback__mutmut_1, 
        'xǁPrinterǁregister_callback__mutmut_2': xǁPrinterǁregister_callback__mutmut_2, 
        'xǁPrinterǁregister_callback__mutmut_3': xǁPrinterǁregister_callback__mutmut_3, 
        'xǁPrinterǁregister_callback__mutmut_4': xǁPrinterǁregister_callback__mutmut_4
    }
    xǁPrinterǁregister_callback__mutmut_orig.__name__ = 'xǁPrinterǁregister_callback'

    @QtCore.pyqtSlot(str, name="on_klippy_status")
    def on_klippy_status(self, state: str):
        """Handles klippy update status

        States include `"startup", "error", "ready", "shutdown", "disconnect"`
        """
        if state.lower() == "ready":
            self.request_available_objects_signal.emit()  # request available objects
            _query_request: dict = {
                "idle_timeout": None,
                "print_stats": None,
                "virtual_sdcard": None,
            }
            self.query_printer_object.emit(_query_request)
            if self._klippy_callback is not None:
                self._klippy_callback(state)
            self.klippy_state_changed.emit(state)
            return
        if self._klippy_callback is not None:
            self._klippy_callback(state)
        self.klippy_state_changed.emit(state)
        self.clear_printer_objs()  # All other states clear it

    @QtCore.pyqtSlot(list, name="on_object_list")
    def on_object_list(self, object_list: list):
        """Handle receiving Printer object list"""
        self.available_objects = dict.fromkeys(object_list, None)
        self.request_object_subscription_signal[dict].emit(self.available_objects)

    def has_config_keyword(self, section: str) -> bool:
        args = [section]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁhas_config_keyword__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁhas_config_keyword__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁhas_config_keyword__mutmut_orig(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_1(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if section:
            return False
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_2(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return True
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_3(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = None
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_4(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get(None, None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_5(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get(None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_6(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", )
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_7(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("XXconfigXX", None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_8(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("CONFIG", None)
        if not _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_9(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", None)
        if _printer_config:
            return False
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_10(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return True
        return bool(section in _printer_config)

    def xǁPrinterǁhas_config_keyword__mutmut_11(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return False
        return bool(None)

    def xǁPrinterǁhas_config_keyword__mutmut_12(self, section: str) -> bool:
        """Check if a section exists on the printers available object configurations

            **`Does not accept prefixes`**

        Args:
            section (str): Name of the section to check its existence

        Returns:
            bool: Whether or not a configuration exists for the specified section
        """
        if not section:
            return False
        _printer_config = self.configfile.get("config", None)
        if not _printer_config:
            return False
        return bool(section not in _printer_config)
    
    xǁPrinterǁhas_config_keyword__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁhas_config_keyword__mutmut_1': xǁPrinterǁhas_config_keyword__mutmut_1, 
        'xǁPrinterǁhas_config_keyword__mutmut_2': xǁPrinterǁhas_config_keyword__mutmut_2, 
        'xǁPrinterǁhas_config_keyword__mutmut_3': xǁPrinterǁhas_config_keyword__mutmut_3, 
        'xǁPrinterǁhas_config_keyword__mutmut_4': xǁPrinterǁhas_config_keyword__mutmut_4, 
        'xǁPrinterǁhas_config_keyword__mutmut_5': xǁPrinterǁhas_config_keyword__mutmut_5, 
        'xǁPrinterǁhas_config_keyword__mutmut_6': xǁPrinterǁhas_config_keyword__mutmut_6, 
        'xǁPrinterǁhas_config_keyword__mutmut_7': xǁPrinterǁhas_config_keyword__mutmut_7, 
        'xǁPrinterǁhas_config_keyword__mutmut_8': xǁPrinterǁhas_config_keyword__mutmut_8, 
        'xǁPrinterǁhas_config_keyword__mutmut_9': xǁPrinterǁhas_config_keyword__mutmut_9, 
        'xǁPrinterǁhas_config_keyword__mutmut_10': xǁPrinterǁhas_config_keyword__mutmut_10, 
        'xǁPrinterǁhas_config_keyword__mutmut_11': xǁPrinterǁhas_config_keyword__mutmut_11, 
        'xǁPrinterǁhas_config_keyword__mutmut_12': xǁPrinterǁhas_config_keyword__mutmut_12
    }
    xǁPrinterǁhas_config_keyword__mutmut_orig.__name__ = 'xǁPrinterǁhas_config_keyword'

    def fetch_config_by_keyword(self, section: str) -> list:
        args = [section]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁfetch_config_by_keyword__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁfetch_config_by_keyword__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁfetch_config_by_keyword__mutmut_orig(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_1(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile and not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_2(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_3(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_4(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = None
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_5(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get(None)
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_6(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("XXconfigXX")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_7(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("CONFIG")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_8(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_9(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(None)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_10(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) and key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_11(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(None) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_12(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(None)) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_13(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section - " ")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_14(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + "XX XX")) or key == section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok

    def xǁPrinterǁfetch_config_by_keyword__mutmut_15(self, section: str) -> list:
        """Retrieve a section or sections from the printers configfile
        with prefix or with full section name

        Args:
            section (str): Name of the section
            name (str, optional): Name of the section object. Defaults to "".

        Returns:
            list: The entire section with the section as key or None if
                            nothing is found
        """
        if not self.configfile or not section:
            return []
        _printer_config = self.configfile.get("config")
        if not _printer_config:
            return []
        return [
            {key: _printer_config.get(key)}
            for key in _printer_config
            if key.startswith(str(section + " ")) or key != section
        ]
        # Iterates over every key and checks if it starts
        # with the prefix -> Complexity O(n*s)
        # Simple but becomes costly for large n values
        # since the dictionary is not exactly big, it should be ok
    
    xǁPrinterǁfetch_config_by_keyword__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁfetch_config_by_keyword__mutmut_1': xǁPrinterǁfetch_config_by_keyword__mutmut_1, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_2': xǁPrinterǁfetch_config_by_keyword__mutmut_2, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_3': xǁPrinterǁfetch_config_by_keyword__mutmut_3, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_4': xǁPrinterǁfetch_config_by_keyword__mutmut_4, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_5': xǁPrinterǁfetch_config_by_keyword__mutmut_5, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_6': xǁPrinterǁfetch_config_by_keyword__mutmut_6, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_7': xǁPrinterǁfetch_config_by_keyword__mutmut_7, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_8': xǁPrinterǁfetch_config_by_keyword__mutmut_8, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_9': xǁPrinterǁfetch_config_by_keyword__mutmut_9, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_10': xǁPrinterǁfetch_config_by_keyword__mutmut_10, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_11': xǁPrinterǁfetch_config_by_keyword__mutmut_11, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_12': xǁPrinterǁfetch_config_by_keyword__mutmut_12, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_13': xǁPrinterǁfetch_config_by_keyword__mutmut_13, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_14': xǁPrinterǁfetch_config_by_keyword__mutmut_14, 
        'xǁPrinterǁfetch_config_by_keyword__mutmut_15': xǁPrinterǁfetch_config_by_keyword__mutmut_15
    }
    xǁPrinterǁfetch_config_by_keyword__mutmut_orig.__name__ = 'xǁPrinterǁfetch_config_by_keyword'

    def get_config(self, section_name: str) -> dict:
        args = [section_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁget_config__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁget_config__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁget_config__mutmut_orig(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_1(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_2(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_3(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(None):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_4(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = None
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_5(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(None)
        return _config[0].get(section_name, {})

    def xǁPrinterǁget_config__mutmut_6(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(None, {})

    def xǁPrinterǁget_config__mutmut_7(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, None)

    def xǁPrinterǁget_config__mutmut_8(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get({})

    def xǁPrinterǁget_config__mutmut_9(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[0].get(section_name, )

    def xǁPrinterǁget_config__mutmut_10(self, section_name: str) -> dict:
        """Gets a printer config section, does not accept prefixes

        Args:
            section_name (str): Name of the printer object

        Returns:
            dict: The config dictionary of the printer object
        """
        if not section_name:
            return {}
        if not self.has_config_keyword(section_name):
            return {}
        _config = self.fetch_config_by_keyword(section_name)
        return _config[1].get(section_name, {})
    
    xǁPrinterǁget_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁget_config__mutmut_1': xǁPrinterǁget_config__mutmut_1, 
        'xǁPrinterǁget_config__mutmut_2': xǁPrinterǁget_config__mutmut_2, 
        'xǁPrinterǁget_config__mutmut_3': xǁPrinterǁget_config__mutmut_3, 
        'xǁPrinterǁget_config__mutmut_4': xǁPrinterǁget_config__mutmut_4, 
        'xǁPrinterǁget_config__mutmut_5': xǁPrinterǁget_config__mutmut_5, 
        'xǁPrinterǁget_config__mutmut_6': xǁPrinterǁget_config__mutmut_6, 
        'xǁPrinterǁget_config__mutmut_7': xǁPrinterǁget_config__mutmut_7, 
        'xǁPrinterǁget_config__mutmut_8': xǁPrinterǁget_config__mutmut_8, 
        'xǁPrinterǁget_config__mutmut_9': xǁPrinterǁget_config__mutmut_9, 
        'xǁPrinterǁget_config__mutmut_10': xǁPrinterǁget_config__mutmut_10
    }
    xǁPrinterǁget_config__mutmut_orig.__name__ = 'xǁPrinterǁget_config'

    def search_config_list(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        args = [search_list, _objects]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁsearch_config_list__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁsearch_config_list__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁsearch_config_list__mutmut_orig(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_1(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_2(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = None
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_3(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_4(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = None
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_5(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(None)
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_6(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(None))
        return self.search_config_list(search_list, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_7(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(None, _objects)

    def xǁPrinterǁsearch_config_list__mutmut_8(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, None)

    def xǁPrinterǁsearch_config_list__mutmut_9(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(_objects)

    def xǁPrinterǁsearch_config_list__mutmut_10(
        self, search_list: list[str], _objects: typing.Optional[list] = None
    ) -> list:
        """
        Search a list of printer objects recursively

        Args:
                search_list (list): A list of objects to search for.
                _objects (list, optional): Internal list, should not be directly set or modified.
        ---

        Returns:
            list: A list containing the found objects with their configuration
        """
        if not _objects:
            _objects = []
        if not search_list:
            return _objects
        key = search_list.pop()
        _objects.extend(self.fetch_config_by_keyword(key))
        return self.search_config_list(search_list, )
    
    xǁPrinterǁsearch_config_list__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁsearch_config_list__mutmut_1': xǁPrinterǁsearch_config_list__mutmut_1, 
        'xǁPrinterǁsearch_config_list__mutmut_2': xǁPrinterǁsearch_config_list__mutmut_2, 
        'xǁPrinterǁsearch_config_list__mutmut_3': xǁPrinterǁsearch_config_list__mutmut_3, 
        'xǁPrinterǁsearch_config_list__mutmut_4': xǁPrinterǁsearch_config_list__mutmut_4, 
        'xǁPrinterǁsearch_config_list__mutmut_5': xǁPrinterǁsearch_config_list__mutmut_5, 
        'xǁPrinterǁsearch_config_list__mutmut_6': xǁPrinterǁsearch_config_list__mutmut_6, 
        'xǁPrinterǁsearch_config_list__mutmut_7': xǁPrinterǁsearch_config_list__mutmut_7, 
        'xǁPrinterǁsearch_config_list__mutmut_8': xǁPrinterǁsearch_config_list__mutmut_8, 
        'xǁPrinterǁsearch_config_list__mutmut_9': xǁPrinterǁsearch_config_list__mutmut_9, 
        'xǁPrinterǁsearch_config_list__mutmut_10': xǁPrinterǁsearch_config_list__mutmut_10
    }
    xǁPrinterǁsearch_config_list__mutmut_orig.__name__ = 'xǁPrinterǁsearch_config_list'

    @QtCore.pyqtSlot(str, "PyQt_PyObject", name="on_subscribe_config")
    @QtCore.pyqtSlot(list, "PyQt_PyObject", name="on_subscribe_config")
    def on_subscribe_config(self, section: str | list, callback) -> None:
        """Slot that manages object config subscriptions

        Args:
            section (str | list): Config section to subscribe to
            callback (function): Callback method that is executed signaled after

        """
        if not self.configfile.get("config"):
            return
        if not section or not callable(callback):
            return
        if isinstance(section, str):
            self.config_subscription[dict].connect(callback)
            self.config_subscription[dict].emit({section: self.get_config(section)})
        elif isinstance(section, list):
            self.config_subscription[list].connect(callback)
            self.config_subscription[list].emit(self.search_config_list(section))

    def _check_callback(self, name: str, values: dict) -> bool:
        args = [name, values]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_check_callback__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_check_callback__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_check_callback__mutmut_orig(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_1(self, name: str, values: dict) -> bool:
        _split: list = None  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_2(self, name: str, values: dict) -> bool:
        _split: list = name.split(None, 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_3(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", None)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_4(self, name: str, values: dict) -> bool:
        _split: list = name.split(1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_5(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", )  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_6(self, name: str, values: dict) -> bool:
        _split: list = name.rsplit(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_7(self, name: str, values: dict) -> bool:
        _split: list = name.split("XX XX", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_8(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 2)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_9(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = None
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_10(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(None)
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_11(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split - [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_12(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] / max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_13(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + ["XXXX"] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_14(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(None, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_15(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, None))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_16(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_17(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, ))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_18(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(1, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_19(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 + len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_20(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 3 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_21(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith(None):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_22(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("XXextruderXX"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_23(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("EXTRUDER"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_24(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = None
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_25(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type not in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_26(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](None, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_27(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, None)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_28(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](_object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_29(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, )
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_30(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(None, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_31(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, None, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_32(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, None)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_33(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_34(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_35(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, )
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_36(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(None, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_37(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, None):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_38(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_39(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, ):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_40(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = None
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_41(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(None, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_42(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, None)
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_43(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_44(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, )
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_45(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(None):
                _callback(values, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_46(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(None, _object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_47(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, None)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_48(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(_object_name)
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_49(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, )
                return True
        return False

    def xǁPrinterǁ_check_callback__mutmut_50(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return False
        return False

    def xǁPrinterǁ_check_callback__mutmut_51(self, name: str, values: dict) -> bool:
        _split: list = name.split(" ", 1)  # Only need the first " " separation
        _object_type, _object_name = tuple(_split + [""] * max(0, 2 - len(_split)))
        if name.startswith("extruder"):
            _object_name = name
        if _object_type in self._callbacks:
            self._callbacks[_object_type](values, _object_name)
        self.object_updated.emit(_object_type, _object_name, values)
        if hasattr(self, f"_{_object_type}_object_updated"):
            _callback = getattr(self, f"_{_object_type}_object_updated")
            if callable(_callback):
                _callback(values, _object_name)
                return True
        return True
    
    xǁPrinterǁ_check_callback__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_check_callback__mutmut_1': xǁPrinterǁ_check_callback__mutmut_1, 
        'xǁPrinterǁ_check_callback__mutmut_2': xǁPrinterǁ_check_callback__mutmut_2, 
        'xǁPrinterǁ_check_callback__mutmut_3': xǁPrinterǁ_check_callback__mutmut_3, 
        'xǁPrinterǁ_check_callback__mutmut_4': xǁPrinterǁ_check_callback__mutmut_4, 
        'xǁPrinterǁ_check_callback__mutmut_5': xǁPrinterǁ_check_callback__mutmut_5, 
        'xǁPrinterǁ_check_callback__mutmut_6': xǁPrinterǁ_check_callback__mutmut_6, 
        'xǁPrinterǁ_check_callback__mutmut_7': xǁPrinterǁ_check_callback__mutmut_7, 
        'xǁPrinterǁ_check_callback__mutmut_8': xǁPrinterǁ_check_callback__mutmut_8, 
        'xǁPrinterǁ_check_callback__mutmut_9': xǁPrinterǁ_check_callback__mutmut_9, 
        'xǁPrinterǁ_check_callback__mutmut_10': xǁPrinterǁ_check_callback__mutmut_10, 
        'xǁPrinterǁ_check_callback__mutmut_11': xǁPrinterǁ_check_callback__mutmut_11, 
        'xǁPrinterǁ_check_callback__mutmut_12': xǁPrinterǁ_check_callback__mutmut_12, 
        'xǁPrinterǁ_check_callback__mutmut_13': xǁPrinterǁ_check_callback__mutmut_13, 
        'xǁPrinterǁ_check_callback__mutmut_14': xǁPrinterǁ_check_callback__mutmut_14, 
        'xǁPrinterǁ_check_callback__mutmut_15': xǁPrinterǁ_check_callback__mutmut_15, 
        'xǁPrinterǁ_check_callback__mutmut_16': xǁPrinterǁ_check_callback__mutmut_16, 
        'xǁPrinterǁ_check_callback__mutmut_17': xǁPrinterǁ_check_callback__mutmut_17, 
        'xǁPrinterǁ_check_callback__mutmut_18': xǁPrinterǁ_check_callback__mutmut_18, 
        'xǁPrinterǁ_check_callback__mutmut_19': xǁPrinterǁ_check_callback__mutmut_19, 
        'xǁPrinterǁ_check_callback__mutmut_20': xǁPrinterǁ_check_callback__mutmut_20, 
        'xǁPrinterǁ_check_callback__mutmut_21': xǁPrinterǁ_check_callback__mutmut_21, 
        'xǁPrinterǁ_check_callback__mutmut_22': xǁPrinterǁ_check_callback__mutmut_22, 
        'xǁPrinterǁ_check_callback__mutmut_23': xǁPrinterǁ_check_callback__mutmut_23, 
        'xǁPrinterǁ_check_callback__mutmut_24': xǁPrinterǁ_check_callback__mutmut_24, 
        'xǁPrinterǁ_check_callback__mutmut_25': xǁPrinterǁ_check_callback__mutmut_25, 
        'xǁPrinterǁ_check_callback__mutmut_26': xǁPrinterǁ_check_callback__mutmut_26, 
        'xǁPrinterǁ_check_callback__mutmut_27': xǁPrinterǁ_check_callback__mutmut_27, 
        'xǁPrinterǁ_check_callback__mutmut_28': xǁPrinterǁ_check_callback__mutmut_28, 
        'xǁPrinterǁ_check_callback__mutmut_29': xǁPrinterǁ_check_callback__mutmut_29, 
        'xǁPrinterǁ_check_callback__mutmut_30': xǁPrinterǁ_check_callback__mutmut_30, 
        'xǁPrinterǁ_check_callback__mutmut_31': xǁPrinterǁ_check_callback__mutmut_31, 
        'xǁPrinterǁ_check_callback__mutmut_32': xǁPrinterǁ_check_callback__mutmut_32, 
        'xǁPrinterǁ_check_callback__mutmut_33': xǁPrinterǁ_check_callback__mutmut_33, 
        'xǁPrinterǁ_check_callback__mutmut_34': xǁPrinterǁ_check_callback__mutmut_34, 
        'xǁPrinterǁ_check_callback__mutmut_35': xǁPrinterǁ_check_callback__mutmut_35, 
        'xǁPrinterǁ_check_callback__mutmut_36': xǁPrinterǁ_check_callback__mutmut_36, 
        'xǁPrinterǁ_check_callback__mutmut_37': xǁPrinterǁ_check_callback__mutmut_37, 
        'xǁPrinterǁ_check_callback__mutmut_38': xǁPrinterǁ_check_callback__mutmut_38, 
        'xǁPrinterǁ_check_callback__mutmut_39': xǁPrinterǁ_check_callback__mutmut_39, 
        'xǁPrinterǁ_check_callback__mutmut_40': xǁPrinterǁ_check_callback__mutmut_40, 
        'xǁPrinterǁ_check_callback__mutmut_41': xǁPrinterǁ_check_callback__mutmut_41, 
        'xǁPrinterǁ_check_callback__mutmut_42': xǁPrinterǁ_check_callback__mutmut_42, 
        'xǁPrinterǁ_check_callback__mutmut_43': xǁPrinterǁ_check_callback__mutmut_43, 
        'xǁPrinterǁ_check_callback__mutmut_44': xǁPrinterǁ_check_callback__mutmut_44, 
        'xǁPrinterǁ_check_callback__mutmut_45': xǁPrinterǁ_check_callback__mutmut_45, 
        'xǁPrinterǁ_check_callback__mutmut_46': xǁPrinterǁ_check_callback__mutmut_46, 
        'xǁPrinterǁ_check_callback__mutmut_47': xǁPrinterǁ_check_callback__mutmut_47, 
        'xǁPrinterǁ_check_callback__mutmut_48': xǁPrinterǁ_check_callback__mutmut_48, 
        'xǁPrinterǁ_check_callback__mutmut_49': xǁPrinterǁ_check_callback__mutmut_49, 
        'xǁPrinterǁ_check_callback__mutmut_50': xǁPrinterǁ_check_callback__mutmut_50, 
        'xǁPrinterǁ_check_callback__mutmut_51': xǁPrinterǁ_check_callback__mutmut_51
    }
    xǁPrinterǁ_check_callback__mutmut_orig.__name__ = 'xǁPrinterǁ_check_callback'

    @QtCore.pyqtSlot(list, name="on_object_report_received")
    def on_object_report_received(self, report: list) -> None:
        """Handles generic object updates, emits callback on specific names"""
        if not report or len(report) <= 1:
            return
        if not isinstance(report[0], dict):
            return
        _objects_updated_dict: dict = report[0]
        for name, value in _objects_updated_dict.items():
            self._check_callback(name, value)

    ####################*# Callbacks #*#####################
    @QtCore.pyqtSlot(list, name="_gcode_response")
    def _gcode_response(self, report: list) -> None:
        self.gcode_response.emit(report)

    def _webhook_printcore_updated(self, value: dict):
        args = [value]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_webhook_printcore_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_webhook_printcore_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_webhook_printcore_updated__mutmut_orig(self, value: dict):
        self.on_printcore_update[dict].emit(value)

    def xǁPrinterǁ_webhook_printcore_updated__mutmut_1(self, value: dict):
        self.on_printcore_update[dict].emit(None)
    
    xǁPrinterǁ_webhook_printcore_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_webhook_printcore_updated__mutmut_1': xǁPrinterǁ_webhook_printcore_updated__mutmut_1
    }
    xǁPrinterǁ_webhook_printcore_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_webhook_printcore_updated'

    def _webhooks_object_updated(self, value: dict, name: str = "webhooks") -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_webhooks_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_webhooks_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_webhooks_object_updated__mutmut_orig(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_1(self, value: dict, name: str = "XXwebhooksXX") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_2(self, value: dict, name: str = "WEBHOOKS") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_3(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() or "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_4(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "XXstateXX" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_5(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "STATE" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_6(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" not in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_7(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "XXstate_messageXX" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_8(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "STATE_MESSAGE" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_9(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" not in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_10(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(None, value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_11(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], None)
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_12(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_13(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], )
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_14(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["XXstateXX"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_15(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["STATE"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_16(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["XXstate_messageXX"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_17(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["STATE_MESSAGE"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_18(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug(None)
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_19(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("XXWebhooks message receivedXX")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_20(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_21(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("WEBHOOKS MESSAGE RECEIVED")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_22(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = None
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_23(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["XXstateXX"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_24(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["STATE"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_25(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = None
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_26(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].lower()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_27(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[1].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_28(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = None
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_29(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[2:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_30(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(None, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_31(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, None):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_32(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_33(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, ):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_34(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = None
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_35(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(None, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_36(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, None)
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_37(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_38(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, )
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_39(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(None):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_40(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = None
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_41(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(None, value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_42(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], None)
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_43(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_44(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], )
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_45(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["XXstateXX"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_46(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["STATE"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_47(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["XXstate_messageXX"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_48(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["STATE_MESSAGE"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_49(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = None
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_50(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None or isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_51(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_52(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(None, event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_53(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), None)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_54(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_55(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), )
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_56(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError(None)
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_57(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("XXQApplication.instance is None type.XX")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_58(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("qapplication.instance is none type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_59(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QAPPLICATION.INSTANCE IS NONE TYPE.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_60(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            None,
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_61(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            None,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_62(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            None,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_63(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_64(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_65(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "Unable to send internal Klippy %s notification : %e",
                            _state_call,
                            )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_66(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "XXUnable to send internal Klippy %s notification : %eXX",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_67(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "unable to send internal klippy %s notification : %e",
                            _state_call,
                            e,
                        )

    def xǁPrinterǁ_webhooks_object_updated__mutmut_68(self, value: dict, name: str = "webhooks") -> None:
        """Sends an event type according to the received state
            from webhooks object

        Args:
            value (dict): _description_
            name (str, optional): _description_. Defaults to "".
        """
        if "state" in value.keys() and "state_message" in value.keys():
            self.webhooks_update.emit(value["state"], value["state_message"])
            logger.debug("Webhooks message received")
            _state: str = value["state"]
            _state_upper = _state[0].upper()
            _state_call = f"{_state_upper}{_state[1:]}"
            if hasattr(events, f"Klippy{_state_call}"):
                _event_callback = getattr(events, f"Klippy{_state_call}")
                if callable(_event_callback):
                    try:
                        event = _event_callback(value["state"], value["state_message"])
                        instance = QtWidgets.QApplication.instance()
                        if instance is not None and isinstance(event, QtCore.QEvent):
                            instance.sendEvent(self.parent(), event)
                        else:
                            raise TypeError("QApplication.instance is None type.")
                    except Exception as e:
                        logger.debug(
                            "UNABLE TO SEND INTERNAL KLIPPY %S NOTIFICATION : %E",
                            _state_call,
                            e,
                        )
    
    xǁPrinterǁ_webhooks_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_webhooks_object_updated__mutmut_1': xǁPrinterǁ_webhooks_object_updated__mutmut_1, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_2': xǁPrinterǁ_webhooks_object_updated__mutmut_2, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_3': xǁPrinterǁ_webhooks_object_updated__mutmut_3, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_4': xǁPrinterǁ_webhooks_object_updated__mutmut_4, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_5': xǁPrinterǁ_webhooks_object_updated__mutmut_5, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_6': xǁPrinterǁ_webhooks_object_updated__mutmut_6, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_7': xǁPrinterǁ_webhooks_object_updated__mutmut_7, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_8': xǁPrinterǁ_webhooks_object_updated__mutmut_8, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_9': xǁPrinterǁ_webhooks_object_updated__mutmut_9, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_10': xǁPrinterǁ_webhooks_object_updated__mutmut_10, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_11': xǁPrinterǁ_webhooks_object_updated__mutmut_11, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_12': xǁPrinterǁ_webhooks_object_updated__mutmut_12, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_13': xǁPrinterǁ_webhooks_object_updated__mutmut_13, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_14': xǁPrinterǁ_webhooks_object_updated__mutmut_14, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_15': xǁPrinterǁ_webhooks_object_updated__mutmut_15, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_16': xǁPrinterǁ_webhooks_object_updated__mutmut_16, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_17': xǁPrinterǁ_webhooks_object_updated__mutmut_17, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_18': xǁPrinterǁ_webhooks_object_updated__mutmut_18, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_19': xǁPrinterǁ_webhooks_object_updated__mutmut_19, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_20': xǁPrinterǁ_webhooks_object_updated__mutmut_20, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_21': xǁPrinterǁ_webhooks_object_updated__mutmut_21, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_22': xǁPrinterǁ_webhooks_object_updated__mutmut_22, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_23': xǁPrinterǁ_webhooks_object_updated__mutmut_23, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_24': xǁPrinterǁ_webhooks_object_updated__mutmut_24, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_25': xǁPrinterǁ_webhooks_object_updated__mutmut_25, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_26': xǁPrinterǁ_webhooks_object_updated__mutmut_26, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_27': xǁPrinterǁ_webhooks_object_updated__mutmut_27, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_28': xǁPrinterǁ_webhooks_object_updated__mutmut_28, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_29': xǁPrinterǁ_webhooks_object_updated__mutmut_29, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_30': xǁPrinterǁ_webhooks_object_updated__mutmut_30, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_31': xǁPrinterǁ_webhooks_object_updated__mutmut_31, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_32': xǁPrinterǁ_webhooks_object_updated__mutmut_32, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_33': xǁPrinterǁ_webhooks_object_updated__mutmut_33, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_34': xǁPrinterǁ_webhooks_object_updated__mutmut_34, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_35': xǁPrinterǁ_webhooks_object_updated__mutmut_35, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_36': xǁPrinterǁ_webhooks_object_updated__mutmut_36, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_37': xǁPrinterǁ_webhooks_object_updated__mutmut_37, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_38': xǁPrinterǁ_webhooks_object_updated__mutmut_38, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_39': xǁPrinterǁ_webhooks_object_updated__mutmut_39, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_40': xǁPrinterǁ_webhooks_object_updated__mutmut_40, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_41': xǁPrinterǁ_webhooks_object_updated__mutmut_41, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_42': xǁPrinterǁ_webhooks_object_updated__mutmut_42, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_43': xǁPrinterǁ_webhooks_object_updated__mutmut_43, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_44': xǁPrinterǁ_webhooks_object_updated__mutmut_44, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_45': xǁPrinterǁ_webhooks_object_updated__mutmut_45, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_46': xǁPrinterǁ_webhooks_object_updated__mutmut_46, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_47': xǁPrinterǁ_webhooks_object_updated__mutmut_47, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_48': xǁPrinterǁ_webhooks_object_updated__mutmut_48, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_49': xǁPrinterǁ_webhooks_object_updated__mutmut_49, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_50': xǁPrinterǁ_webhooks_object_updated__mutmut_50, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_51': xǁPrinterǁ_webhooks_object_updated__mutmut_51, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_52': xǁPrinterǁ_webhooks_object_updated__mutmut_52, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_53': xǁPrinterǁ_webhooks_object_updated__mutmut_53, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_54': xǁPrinterǁ_webhooks_object_updated__mutmut_54, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_55': xǁPrinterǁ_webhooks_object_updated__mutmut_55, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_56': xǁPrinterǁ_webhooks_object_updated__mutmut_56, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_57': xǁPrinterǁ_webhooks_object_updated__mutmut_57, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_58': xǁPrinterǁ_webhooks_object_updated__mutmut_58, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_59': xǁPrinterǁ_webhooks_object_updated__mutmut_59, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_60': xǁPrinterǁ_webhooks_object_updated__mutmut_60, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_61': xǁPrinterǁ_webhooks_object_updated__mutmut_61, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_62': xǁPrinterǁ_webhooks_object_updated__mutmut_62, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_63': xǁPrinterǁ_webhooks_object_updated__mutmut_63, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_64': xǁPrinterǁ_webhooks_object_updated__mutmut_64, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_65': xǁPrinterǁ_webhooks_object_updated__mutmut_65, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_66': xǁPrinterǁ_webhooks_object_updated__mutmut_66, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_67': xǁPrinterǁ_webhooks_object_updated__mutmut_67, 
        'xǁPrinterǁ_webhooks_object_updated__mutmut_68': xǁPrinterǁ_webhooks_object_updated__mutmut_68
    }
    xǁPrinterǁ_webhooks_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_webhooks_object_updated'

    def _save_variables_object_updated(
        self, value: dict, name: str = "saved_variables"
    ) -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_save_variables_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_save_variables_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_save_variables_object_updated__mutmut_orig(
        self, value: dict, name: str = "saved_variables"
    ) -> None:
        self.save_variables_update.emit(value)

    def xǁPrinterǁ_save_variables_object_updated__mutmut_1(
        self, value: dict, name: str = "XXsaved_variablesXX"
    ) -> None:
        self.save_variables_update.emit(value)

    def xǁPrinterǁ_save_variables_object_updated__mutmut_2(
        self, value: dict, name: str = "SAVED_VARIABLES"
    ) -> None:
        self.save_variables_update.emit(value)

    def xǁPrinterǁ_save_variables_object_updated__mutmut_3(
        self, value: dict, name: str = "saved_variables"
    ) -> None:
        self.save_variables_update.emit(None)
    
    xǁPrinterǁ_save_variables_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_save_variables_object_updated__mutmut_1': xǁPrinterǁ_save_variables_object_updated__mutmut_1, 
        'xǁPrinterǁ_save_variables_object_updated__mutmut_2': xǁPrinterǁ_save_variables_object_updated__mutmut_2, 
        'xǁPrinterǁ_save_variables_object_updated__mutmut_3': xǁPrinterǁ_save_variables_object_updated__mutmut_3
    }
    xǁPrinterǁ_save_variables_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_save_variables_object_updated'

    def _mmu_object_updated(self, value: dict, name: str = "mmu") -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_mmu_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_mmu_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_mmu_object_updated__mutmut_orig(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_1(self, value: dict, name: str = "XXmmuXX") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_2(self, value: dict, name: str = "MMU") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_3(self, value: dict, name: str = "mmu") -> None:
        if "XXactionXX" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_4(self, value: dict, name: str = "mmu") -> None:
        if "ACTION" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_5(self, value: dict, name: str = "mmu") -> None:
        if "action" not in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_6(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit(None, value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_7(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", None)

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_8(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit(value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_9(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", )

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_10(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("XXactionXX", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_11(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("ACTION", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_12(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["XXactionXX"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_13(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["ACTION"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_14(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "XXfilamentXX" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_15(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "FILAMENT" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_16(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" not in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_17(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                None, value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_18(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", None
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_19(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_20(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_21(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "XXfilamentXX", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_22(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "FILAMENT", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_23(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["XXfilamentXX"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_24(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["FILAMENT"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_25(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "XXgateXX" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_26(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "GATE" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_27(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" not in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_28(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit(None, value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_29(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", None)

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_30(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit(value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_31(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", )

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_32(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("XXgateXX", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_33(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("GATE", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_34(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["XXgateXX"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_35(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["GATE"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_36(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "XXactive_filamentXX" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_37(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "ACTIVE_FILAMENT" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_38(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" not in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_39(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit(None, value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_40(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", None)

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_41(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit(value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_42(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", )

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_43(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("XXactive_filamentXX", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_44(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("ACTIVE_FILAMENT", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_45(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["XXactive_filamentXX"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_46(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["ACTIVE_FILAMENT"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_47(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "XXgate_statusXX" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_48(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "GATE_STATUS" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_49(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" not in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_50(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit(None, value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_51(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", None)

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_52(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit(value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_53(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", )

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_54(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("XXgate_statusXX", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_55(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("GATE_STATUS", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_56(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["XXgate_statusXX"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_57(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["GATE_STATUS"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_58(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "XXflowguardXX" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_59(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "FLOWGUARD" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_60(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" not in value:
            self.flowguard_update[str, dict].emit("flowguard", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_61(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit(None, value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_62(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", None)

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_63(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit(value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_64(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", )

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_65(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("XXflowguardXX", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_66(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("FLOWGUARD", value["flowguard"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_67(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["XXflowguardXX"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed

    def xǁPrinterǁ_mmu_object_updated__mutmut_68(self, value: dict, name: str = "mmu") -> None:
        if "action" in value:
            self.mmu_update[str, str].emit("action", value["action"])

        if "filament" in value:
            self.mmu_update[str, str].emit(
                "filament", value["filament"]
            )  # THIS IS EXTRUDER SENSOR ONLY

        if "gate" in value:
            self.mmu_update[str, int].emit("gate", value["gate"])

        if "active_filament" in value:
            self.mmu_update[str, dict].emit("active_filament", value["active_filament"])

        if "gate_status" in value:
            self.mmu_update[str, list].emit("gate_status", value["gate_status"])

        if "flowguard" in value:
            self.flowguard_update[str, dict].emit("flowguard", value["FLOWGUARD"])

        # i only putted the most relevant ones, there are some other parameters that can be added later if needed
    
    xǁPrinterǁ_mmu_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_mmu_object_updated__mutmut_1': xǁPrinterǁ_mmu_object_updated__mutmut_1, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_2': xǁPrinterǁ_mmu_object_updated__mutmut_2, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_3': xǁPrinterǁ_mmu_object_updated__mutmut_3, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_4': xǁPrinterǁ_mmu_object_updated__mutmut_4, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_5': xǁPrinterǁ_mmu_object_updated__mutmut_5, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_6': xǁPrinterǁ_mmu_object_updated__mutmut_6, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_7': xǁPrinterǁ_mmu_object_updated__mutmut_7, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_8': xǁPrinterǁ_mmu_object_updated__mutmut_8, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_9': xǁPrinterǁ_mmu_object_updated__mutmut_9, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_10': xǁPrinterǁ_mmu_object_updated__mutmut_10, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_11': xǁPrinterǁ_mmu_object_updated__mutmut_11, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_12': xǁPrinterǁ_mmu_object_updated__mutmut_12, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_13': xǁPrinterǁ_mmu_object_updated__mutmut_13, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_14': xǁPrinterǁ_mmu_object_updated__mutmut_14, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_15': xǁPrinterǁ_mmu_object_updated__mutmut_15, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_16': xǁPrinterǁ_mmu_object_updated__mutmut_16, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_17': xǁPrinterǁ_mmu_object_updated__mutmut_17, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_18': xǁPrinterǁ_mmu_object_updated__mutmut_18, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_19': xǁPrinterǁ_mmu_object_updated__mutmut_19, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_20': xǁPrinterǁ_mmu_object_updated__mutmut_20, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_21': xǁPrinterǁ_mmu_object_updated__mutmut_21, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_22': xǁPrinterǁ_mmu_object_updated__mutmut_22, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_23': xǁPrinterǁ_mmu_object_updated__mutmut_23, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_24': xǁPrinterǁ_mmu_object_updated__mutmut_24, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_25': xǁPrinterǁ_mmu_object_updated__mutmut_25, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_26': xǁPrinterǁ_mmu_object_updated__mutmut_26, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_27': xǁPrinterǁ_mmu_object_updated__mutmut_27, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_28': xǁPrinterǁ_mmu_object_updated__mutmut_28, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_29': xǁPrinterǁ_mmu_object_updated__mutmut_29, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_30': xǁPrinterǁ_mmu_object_updated__mutmut_30, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_31': xǁPrinterǁ_mmu_object_updated__mutmut_31, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_32': xǁPrinterǁ_mmu_object_updated__mutmut_32, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_33': xǁPrinterǁ_mmu_object_updated__mutmut_33, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_34': xǁPrinterǁ_mmu_object_updated__mutmut_34, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_35': xǁPrinterǁ_mmu_object_updated__mutmut_35, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_36': xǁPrinterǁ_mmu_object_updated__mutmut_36, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_37': xǁPrinterǁ_mmu_object_updated__mutmut_37, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_38': xǁPrinterǁ_mmu_object_updated__mutmut_38, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_39': xǁPrinterǁ_mmu_object_updated__mutmut_39, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_40': xǁPrinterǁ_mmu_object_updated__mutmut_40, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_41': xǁPrinterǁ_mmu_object_updated__mutmut_41, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_42': xǁPrinterǁ_mmu_object_updated__mutmut_42, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_43': xǁPrinterǁ_mmu_object_updated__mutmut_43, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_44': xǁPrinterǁ_mmu_object_updated__mutmut_44, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_45': xǁPrinterǁ_mmu_object_updated__mutmut_45, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_46': xǁPrinterǁ_mmu_object_updated__mutmut_46, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_47': xǁPrinterǁ_mmu_object_updated__mutmut_47, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_48': xǁPrinterǁ_mmu_object_updated__mutmut_48, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_49': xǁPrinterǁ_mmu_object_updated__mutmut_49, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_50': xǁPrinterǁ_mmu_object_updated__mutmut_50, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_51': xǁPrinterǁ_mmu_object_updated__mutmut_51, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_52': xǁPrinterǁ_mmu_object_updated__mutmut_52, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_53': xǁPrinterǁ_mmu_object_updated__mutmut_53, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_54': xǁPrinterǁ_mmu_object_updated__mutmut_54, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_55': xǁPrinterǁ_mmu_object_updated__mutmut_55, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_56': xǁPrinterǁ_mmu_object_updated__mutmut_56, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_57': xǁPrinterǁ_mmu_object_updated__mutmut_57, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_58': xǁPrinterǁ_mmu_object_updated__mutmut_58, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_59': xǁPrinterǁ_mmu_object_updated__mutmut_59, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_60': xǁPrinterǁ_mmu_object_updated__mutmut_60, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_61': xǁPrinterǁ_mmu_object_updated__mutmut_61, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_62': xǁPrinterǁ_mmu_object_updated__mutmut_62, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_63': xǁPrinterǁ_mmu_object_updated__mutmut_63, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_64': xǁPrinterǁ_mmu_object_updated__mutmut_64, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_65': xǁPrinterǁ_mmu_object_updated__mutmut_65, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_66': xǁPrinterǁ_mmu_object_updated__mutmut_66, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_67': xǁPrinterǁ_mmu_object_updated__mutmut_67, 
        'xǁPrinterǁ_mmu_object_updated__mutmut_68': xǁPrinterǁ_mmu_object_updated__mutmut_68
    }
    xǁPrinterǁ_mmu_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_mmu_object_updated'

    def _gcode_move_object_updated(self, value: dict, name: str = "gcode_move") -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_gcode_move_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_gcode_move_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_orig(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_1(self, value: dict, name: str = "XXgcode_moveXX") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_2(self, value: dict, name: str = "GCODE_MOVE") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_3(self, value: dict, name: str = "gcode_move") -> None:
        if "XXspeed_factorXX" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_4(self, value: dict, name: str = "gcode_move") -> None:
        if "SPEED_FACTOR" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_5(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" not in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_6(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                None, value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_7(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", None
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_8(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_9(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_10(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "XXspeed_factorXX", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_11(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "SPEED_FACTOR", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_12(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["XXspeed_factorXX"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_13(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["SPEED_FACTOR"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_14(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "XXspeedXX" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_15(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "SPEED" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_16(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" not in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_17(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit(None, value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_18(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", None)
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_19(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit(value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_20(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", )
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_21(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("XXspeedXX", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_22(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("SPEED", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_23(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["XXspeedXX"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_24(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["SPEED"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_25(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "XXextrude_factorXX" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_26(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "EXTRUDE_FACTOR" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_27(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" not in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_28(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                None, value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_29(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", None
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_30(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_31(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_32(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "XXextruder_factorXX", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_33(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "EXTRUDER_FACTOR", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_34(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["XXextrude_factorXX"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_35(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["EXTRUDE_FACTOR"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_36(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "XXabsolute_coordinatesXX" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_37(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "ABSOLUTE_COORDINATES" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_38(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" not in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_39(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                None, value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_40(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", None
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_41(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_42(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_43(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "XXabsolute_coordinatesXX", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_44(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "ABSOLUTE_COORDINATES", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_45(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["XXabsolute_coordinatesXX"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_46(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["ABSOLUTE_COORDINATES"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_47(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "XXabsolute_extrudeXX" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_48(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "ABSOLUTE_EXTRUDE" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_49(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" not in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_50(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                None, value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_51(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", None
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_52(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_53(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_54(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "XXabsolute_extrudeXX", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_55(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "ABSOLUTE_EXTRUDE", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_56(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["XXabsolute_extrudeXX"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_57(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["ABSOLUTE_EXTRUDE"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_58(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "XXhoming_originXX" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_59(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "HOMING_ORIGIN" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_60(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" not in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_61(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                None, value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_62(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", None
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_63(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_64(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_65(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "XXhoming_originXX", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_66(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "HOMING_ORIGIN", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_67(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["XXhoming_originXX"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_68(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["HOMING_ORIGIN"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_69(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "XXpositionXX" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_70(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "POSITION" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_71(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" not in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_72(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit(None, value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_73(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", None)
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_74(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit(value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_75(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", )
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_76(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("XXpositionXX", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_77(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("POSITION", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_78(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["XXpositionXX"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_79(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["POSITION"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_80(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "XXgcode_positionXX" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_81(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "GCODE_POSITION" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_82(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" not in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_83(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                None, value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_84(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", None
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_85(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_86(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_87(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "XXgcode_positionXX", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_88(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "GCODE_POSITION", value["gcode_position"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_89(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["XXgcode_positionXX"]
            )

    def xǁPrinterǁ_gcode_move_object_updated__mutmut_90(self, value: dict, name: str = "gcode_move") -> None:
        if "speed_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "speed_factor", value["speed_factor"]
            )
        if "speed" in value.keys():
            self.gcode_move_update[str, float].emit("speed", value["speed"])
        if "extrude_factor" in value.keys():
            self.gcode_move_update[str, float].emit(
                "extruder_factor", value["extrude_factor"]
            )
        if "absolute_coordinates" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_coordinates", value["absolute_coordinates"]
            )
        if "absolute_extrude" in value.keys():
            self.gcode_move_update[str, bool].emit(
                "absolute_extrude", value["absolute_extrude"]
            )
        if "homing_origin" in value.keys():
            self.gcode_move_update[str, list].emit(
                "homing_origin", value["homing_origin"]
            )
        if "position" in value.keys():
            self.gcode_move_update[str, list].emit("position", value["position"])
        if "gcode_position" in value.keys():
            self.gcode_move_update[str, list].emit(
                "gcode_position", value["GCODE_POSITION"]
            )
    
    xǁPrinterǁ_gcode_move_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_gcode_move_object_updated__mutmut_1': xǁPrinterǁ_gcode_move_object_updated__mutmut_1, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_2': xǁPrinterǁ_gcode_move_object_updated__mutmut_2, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_3': xǁPrinterǁ_gcode_move_object_updated__mutmut_3, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_4': xǁPrinterǁ_gcode_move_object_updated__mutmut_4, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_5': xǁPrinterǁ_gcode_move_object_updated__mutmut_5, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_6': xǁPrinterǁ_gcode_move_object_updated__mutmut_6, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_7': xǁPrinterǁ_gcode_move_object_updated__mutmut_7, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_8': xǁPrinterǁ_gcode_move_object_updated__mutmut_8, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_9': xǁPrinterǁ_gcode_move_object_updated__mutmut_9, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_10': xǁPrinterǁ_gcode_move_object_updated__mutmut_10, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_11': xǁPrinterǁ_gcode_move_object_updated__mutmut_11, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_12': xǁPrinterǁ_gcode_move_object_updated__mutmut_12, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_13': xǁPrinterǁ_gcode_move_object_updated__mutmut_13, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_14': xǁPrinterǁ_gcode_move_object_updated__mutmut_14, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_15': xǁPrinterǁ_gcode_move_object_updated__mutmut_15, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_16': xǁPrinterǁ_gcode_move_object_updated__mutmut_16, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_17': xǁPrinterǁ_gcode_move_object_updated__mutmut_17, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_18': xǁPrinterǁ_gcode_move_object_updated__mutmut_18, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_19': xǁPrinterǁ_gcode_move_object_updated__mutmut_19, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_20': xǁPrinterǁ_gcode_move_object_updated__mutmut_20, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_21': xǁPrinterǁ_gcode_move_object_updated__mutmut_21, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_22': xǁPrinterǁ_gcode_move_object_updated__mutmut_22, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_23': xǁPrinterǁ_gcode_move_object_updated__mutmut_23, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_24': xǁPrinterǁ_gcode_move_object_updated__mutmut_24, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_25': xǁPrinterǁ_gcode_move_object_updated__mutmut_25, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_26': xǁPrinterǁ_gcode_move_object_updated__mutmut_26, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_27': xǁPrinterǁ_gcode_move_object_updated__mutmut_27, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_28': xǁPrinterǁ_gcode_move_object_updated__mutmut_28, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_29': xǁPrinterǁ_gcode_move_object_updated__mutmut_29, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_30': xǁPrinterǁ_gcode_move_object_updated__mutmut_30, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_31': xǁPrinterǁ_gcode_move_object_updated__mutmut_31, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_32': xǁPrinterǁ_gcode_move_object_updated__mutmut_32, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_33': xǁPrinterǁ_gcode_move_object_updated__mutmut_33, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_34': xǁPrinterǁ_gcode_move_object_updated__mutmut_34, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_35': xǁPrinterǁ_gcode_move_object_updated__mutmut_35, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_36': xǁPrinterǁ_gcode_move_object_updated__mutmut_36, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_37': xǁPrinterǁ_gcode_move_object_updated__mutmut_37, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_38': xǁPrinterǁ_gcode_move_object_updated__mutmut_38, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_39': xǁPrinterǁ_gcode_move_object_updated__mutmut_39, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_40': xǁPrinterǁ_gcode_move_object_updated__mutmut_40, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_41': xǁPrinterǁ_gcode_move_object_updated__mutmut_41, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_42': xǁPrinterǁ_gcode_move_object_updated__mutmut_42, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_43': xǁPrinterǁ_gcode_move_object_updated__mutmut_43, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_44': xǁPrinterǁ_gcode_move_object_updated__mutmut_44, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_45': xǁPrinterǁ_gcode_move_object_updated__mutmut_45, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_46': xǁPrinterǁ_gcode_move_object_updated__mutmut_46, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_47': xǁPrinterǁ_gcode_move_object_updated__mutmut_47, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_48': xǁPrinterǁ_gcode_move_object_updated__mutmut_48, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_49': xǁPrinterǁ_gcode_move_object_updated__mutmut_49, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_50': xǁPrinterǁ_gcode_move_object_updated__mutmut_50, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_51': xǁPrinterǁ_gcode_move_object_updated__mutmut_51, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_52': xǁPrinterǁ_gcode_move_object_updated__mutmut_52, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_53': xǁPrinterǁ_gcode_move_object_updated__mutmut_53, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_54': xǁPrinterǁ_gcode_move_object_updated__mutmut_54, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_55': xǁPrinterǁ_gcode_move_object_updated__mutmut_55, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_56': xǁPrinterǁ_gcode_move_object_updated__mutmut_56, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_57': xǁPrinterǁ_gcode_move_object_updated__mutmut_57, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_58': xǁPrinterǁ_gcode_move_object_updated__mutmut_58, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_59': xǁPrinterǁ_gcode_move_object_updated__mutmut_59, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_60': xǁPrinterǁ_gcode_move_object_updated__mutmut_60, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_61': xǁPrinterǁ_gcode_move_object_updated__mutmut_61, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_62': xǁPrinterǁ_gcode_move_object_updated__mutmut_62, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_63': xǁPrinterǁ_gcode_move_object_updated__mutmut_63, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_64': xǁPrinterǁ_gcode_move_object_updated__mutmut_64, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_65': xǁPrinterǁ_gcode_move_object_updated__mutmut_65, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_66': xǁPrinterǁ_gcode_move_object_updated__mutmut_66, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_67': xǁPrinterǁ_gcode_move_object_updated__mutmut_67, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_68': xǁPrinterǁ_gcode_move_object_updated__mutmut_68, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_69': xǁPrinterǁ_gcode_move_object_updated__mutmut_69, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_70': xǁPrinterǁ_gcode_move_object_updated__mutmut_70, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_71': xǁPrinterǁ_gcode_move_object_updated__mutmut_71, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_72': xǁPrinterǁ_gcode_move_object_updated__mutmut_72, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_73': xǁPrinterǁ_gcode_move_object_updated__mutmut_73, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_74': xǁPrinterǁ_gcode_move_object_updated__mutmut_74, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_75': xǁPrinterǁ_gcode_move_object_updated__mutmut_75, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_76': xǁPrinterǁ_gcode_move_object_updated__mutmut_76, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_77': xǁPrinterǁ_gcode_move_object_updated__mutmut_77, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_78': xǁPrinterǁ_gcode_move_object_updated__mutmut_78, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_79': xǁPrinterǁ_gcode_move_object_updated__mutmut_79, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_80': xǁPrinterǁ_gcode_move_object_updated__mutmut_80, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_81': xǁPrinterǁ_gcode_move_object_updated__mutmut_81, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_82': xǁPrinterǁ_gcode_move_object_updated__mutmut_82, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_83': xǁPrinterǁ_gcode_move_object_updated__mutmut_83, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_84': xǁPrinterǁ_gcode_move_object_updated__mutmut_84, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_85': xǁPrinterǁ_gcode_move_object_updated__mutmut_85, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_86': xǁPrinterǁ_gcode_move_object_updated__mutmut_86, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_87': xǁPrinterǁ_gcode_move_object_updated__mutmut_87, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_88': xǁPrinterǁ_gcode_move_object_updated__mutmut_88, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_89': xǁPrinterǁ_gcode_move_object_updated__mutmut_89, 
        'xǁPrinterǁ_gcode_move_object_updated__mutmut_90': xǁPrinterǁ_gcode_move_object_updated__mutmut_90
    }
    xǁPrinterǁ_gcode_move_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_gcode_move_object_updated'

    def _toolhead_object_updated(self, values: dict, name: str = "toolhead") -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_toolhead_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_toolhead_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_toolhead_object_updated__mutmut_orig(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_1(self, values: dict, name: str = "XXtoolheadXX") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_2(self, values: dict, name: str = "TOOLHEAD") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_3(self, values: dict, name: str = "toolhead") -> None:
        if "XXhomed_axesXX" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_4(self, values: dict, name: str = "toolhead") -> None:
        if "HOMED_AXES" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_5(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" not in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_6(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit(None, values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_7(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", None)
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_8(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit(values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_9(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", )
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_10(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("XXhomed_axesXX", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_11(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("HOMED_AXES", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_12(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["XXhomed_axesXX"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_13(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["HOMED_AXES"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_14(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "XXprint_timeXX" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_15(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "PRINT_TIME" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_16(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" not in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_17(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit(None, values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_18(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", None)
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_19(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit(values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_20(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", )
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_21(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("XXprint_timeXX", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_22(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("PRINT_TIME", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_23(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["XXprint_timeXX"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_24(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["PRINT_TIME"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_25(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "XXestimated_print_timeXX" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_26(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "ESTIMATED_PRINT_TIME" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_27(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" not in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_28(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                None, values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_29(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", None
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_30(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_31(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_32(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "XXestimated_print_timeXX", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_33(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "ESTIMATED_PRINT_TIME", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_34(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["XXestimated_print_timeXX"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_35(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["ESTIMATED_PRINT_TIME"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_36(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "XXextruderXX" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_37(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "EXTRUDER" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_38(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" not in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_39(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit(None, values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_40(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", None)
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_41(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit(values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_42(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", )
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_43(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("XXextruderXX", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_44(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("EXTRUDER", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_45(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["XXextruderXX"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_46(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["EXTRUDER"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_47(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = None
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_48(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["XXextruderXX"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_49(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["EXTRUDER"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_50(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "XXpositionXX" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_51(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "POSITION" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_52(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" not in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_53(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit(None, values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_54(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", None)
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_55(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit(values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_56(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", )
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_57(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("XXpositionXX", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_58(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("POSITION", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_59(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["XXpositionXX"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_60(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["POSITION"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_61(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "XXmax_velocityXX" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_62(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "MAX_VELOCITY" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_63(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" not in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_64(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                None, values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_65(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", None
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_66(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_67(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_68(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "XXmax_velocityXX", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_69(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "MAX_VELOCITY", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_70(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["XXmax_velocityXX"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_71(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["MAX_VELOCITY"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_72(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "XXmax_accelXX" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_73(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "MAX_ACCEL" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_74(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" not in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_75(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit(None, values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_76(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", None)
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_77(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit(values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_78(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", )
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_79(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("XXmax_accelXX", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_80(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("MAX_ACCEL", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_81(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["XXmax_accelXX"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_82(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["MAX_ACCEL"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_83(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "XXmax_accel_to_decelXX" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_84(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "MAX_ACCEL_TO_DECEL" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_85(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" not in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_86(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                None, values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_87(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", None
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_88(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_89(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_90(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "XXmax_accel_to_decelXX", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_91(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "MAX_ACCEL_TO_DECEL", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_92(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["XXmax_accel_to_decelXX"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_93(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["MAX_ACCEL_TO_DECEL"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_94(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "XXsquare_corner_velocityXX" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_95(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "SQUARE_CORNER_VELOCITY" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_96(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" not in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_97(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                None, values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_98(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", None
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_99(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_100(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_101(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "XXsquare_corner_velocityXX", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_102(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "SQUARE_CORNER_VELOCITY", values["square_corner_velocity"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_103(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["XXsquare_corner_velocityXX"]
            )

    def xǁPrinterǁ_toolhead_object_updated__mutmut_104(self, values: dict, name: str = "toolhead") -> None:
        if "homed_axes" in values.keys():
            self.toolhead_update[str, str].emit("homed_axes", values["homed_axes"])
        if "print_time" in values.keys():
            self.toolhead_update[str, float].emit("print_time", values["print_time"])
        if "estimated_print_time" in values.keys():
            self.toolhead_update[str, float].emit(
                "estimated_print_time", values["estimated_print_time"]
            )
        if "extruder" in values.keys():
            self.toolhead_update[str, str].emit("extruder", values["extruder"])
            self.active_extruder_name = values["extruder"]
        if "position" in values.keys():
            self.toolhead_update[str, list].emit("position", values["position"])
        if "max_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_velocity", values["max_velocity"]
            )
        if "max_accel" in values.keys():
            self.toolhead_update[str, float].emit("max_accel", values["max_accel"])
        if "max_accel_to_decel" in values.keys():
            self.toolhead_update[str, float].emit(
                "max_accel_to_decel", values["max_accel_to_decel"]
            )
        if "square_corner_velocity" in values.keys():
            self.toolhead_update[str, float].emit(
                "square_corner_velocity", values["SQUARE_CORNER_VELOCITY"]
            )
    
    xǁPrinterǁ_toolhead_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_toolhead_object_updated__mutmut_1': xǁPrinterǁ_toolhead_object_updated__mutmut_1, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_2': xǁPrinterǁ_toolhead_object_updated__mutmut_2, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_3': xǁPrinterǁ_toolhead_object_updated__mutmut_3, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_4': xǁPrinterǁ_toolhead_object_updated__mutmut_4, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_5': xǁPrinterǁ_toolhead_object_updated__mutmut_5, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_6': xǁPrinterǁ_toolhead_object_updated__mutmut_6, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_7': xǁPrinterǁ_toolhead_object_updated__mutmut_7, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_8': xǁPrinterǁ_toolhead_object_updated__mutmut_8, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_9': xǁPrinterǁ_toolhead_object_updated__mutmut_9, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_10': xǁPrinterǁ_toolhead_object_updated__mutmut_10, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_11': xǁPrinterǁ_toolhead_object_updated__mutmut_11, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_12': xǁPrinterǁ_toolhead_object_updated__mutmut_12, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_13': xǁPrinterǁ_toolhead_object_updated__mutmut_13, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_14': xǁPrinterǁ_toolhead_object_updated__mutmut_14, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_15': xǁPrinterǁ_toolhead_object_updated__mutmut_15, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_16': xǁPrinterǁ_toolhead_object_updated__mutmut_16, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_17': xǁPrinterǁ_toolhead_object_updated__mutmut_17, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_18': xǁPrinterǁ_toolhead_object_updated__mutmut_18, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_19': xǁPrinterǁ_toolhead_object_updated__mutmut_19, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_20': xǁPrinterǁ_toolhead_object_updated__mutmut_20, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_21': xǁPrinterǁ_toolhead_object_updated__mutmut_21, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_22': xǁPrinterǁ_toolhead_object_updated__mutmut_22, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_23': xǁPrinterǁ_toolhead_object_updated__mutmut_23, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_24': xǁPrinterǁ_toolhead_object_updated__mutmut_24, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_25': xǁPrinterǁ_toolhead_object_updated__mutmut_25, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_26': xǁPrinterǁ_toolhead_object_updated__mutmut_26, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_27': xǁPrinterǁ_toolhead_object_updated__mutmut_27, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_28': xǁPrinterǁ_toolhead_object_updated__mutmut_28, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_29': xǁPrinterǁ_toolhead_object_updated__mutmut_29, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_30': xǁPrinterǁ_toolhead_object_updated__mutmut_30, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_31': xǁPrinterǁ_toolhead_object_updated__mutmut_31, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_32': xǁPrinterǁ_toolhead_object_updated__mutmut_32, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_33': xǁPrinterǁ_toolhead_object_updated__mutmut_33, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_34': xǁPrinterǁ_toolhead_object_updated__mutmut_34, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_35': xǁPrinterǁ_toolhead_object_updated__mutmut_35, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_36': xǁPrinterǁ_toolhead_object_updated__mutmut_36, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_37': xǁPrinterǁ_toolhead_object_updated__mutmut_37, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_38': xǁPrinterǁ_toolhead_object_updated__mutmut_38, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_39': xǁPrinterǁ_toolhead_object_updated__mutmut_39, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_40': xǁPrinterǁ_toolhead_object_updated__mutmut_40, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_41': xǁPrinterǁ_toolhead_object_updated__mutmut_41, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_42': xǁPrinterǁ_toolhead_object_updated__mutmut_42, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_43': xǁPrinterǁ_toolhead_object_updated__mutmut_43, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_44': xǁPrinterǁ_toolhead_object_updated__mutmut_44, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_45': xǁPrinterǁ_toolhead_object_updated__mutmut_45, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_46': xǁPrinterǁ_toolhead_object_updated__mutmut_46, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_47': xǁPrinterǁ_toolhead_object_updated__mutmut_47, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_48': xǁPrinterǁ_toolhead_object_updated__mutmut_48, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_49': xǁPrinterǁ_toolhead_object_updated__mutmut_49, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_50': xǁPrinterǁ_toolhead_object_updated__mutmut_50, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_51': xǁPrinterǁ_toolhead_object_updated__mutmut_51, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_52': xǁPrinterǁ_toolhead_object_updated__mutmut_52, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_53': xǁPrinterǁ_toolhead_object_updated__mutmut_53, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_54': xǁPrinterǁ_toolhead_object_updated__mutmut_54, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_55': xǁPrinterǁ_toolhead_object_updated__mutmut_55, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_56': xǁPrinterǁ_toolhead_object_updated__mutmut_56, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_57': xǁPrinterǁ_toolhead_object_updated__mutmut_57, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_58': xǁPrinterǁ_toolhead_object_updated__mutmut_58, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_59': xǁPrinterǁ_toolhead_object_updated__mutmut_59, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_60': xǁPrinterǁ_toolhead_object_updated__mutmut_60, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_61': xǁPrinterǁ_toolhead_object_updated__mutmut_61, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_62': xǁPrinterǁ_toolhead_object_updated__mutmut_62, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_63': xǁPrinterǁ_toolhead_object_updated__mutmut_63, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_64': xǁPrinterǁ_toolhead_object_updated__mutmut_64, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_65': xǁPrinterǁ_toolhead_object_updated__mutmut_65, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_66': xǁPrinterǁ_toolhead_object_updated__mutmut_66, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_67': xǁPrinterǁ_toolhead_object_updated__mutmut_67, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_68': xǁPrinterǁ_toolhead_object_updated__mutmut_68, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_69': xǁPrinterǁ_toolhead_object_updated__mutmut_69, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_70': xǁPrinterǁ_toolhead_object_updated__mutmut_70, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_71': xǁPrinterǁ_toolhead_object_updated__mutmut_71, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_72': xǁPrinterǁ_toolhead_object_updated__mutmut_72, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_73': xǁPrinterǁ_toolhead_object_updated__mutmut_73, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_74': xǁPrinterǁ_toolhead_object_updated__mutmut_74, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_75': xǁPrinterǁ_toolhead_object_updated__mutmut_75, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_76': xǁPrinterǁ_toolhead_object_updated__mutmut_76, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_77': xǁPrinterǁ_toolhead_object_updated__mutmut_77, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_78': xǁPrinterǁ_toolhead_object_updated__mutmut_78, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_79': xǁPrinterǁ_toolhead_object_updated__mutmut_79, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_80': xǁPrinterǁ_toolhead_object_updated__mutmut_80, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_81': xǁPrinterǁ_toolhead_object_updated__mutmut_81, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_82': xǁPrinterǁ_toolhead_object_updated__mutmut_82, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_83': xǁPrinterǁ_toolhead_object_updated__mutmut_83, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_84': xǁPrinterǁ_toolhead_object_updated__mutmut_84, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_85': xǁPrinterǁ_toolhead_object_updated__mutmut_85, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_86': xǁPrinterǁ_toolhead_object_updated__mutmut_86, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_87': xǁPrinterǁ_toolhead_object_updated__mutmut_87, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_88': xǁPrinterǁ_toolhead_object_updated__mutmut_88, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_89': xǁPrinterǁ_toolhead_object_updated__mutmut_89, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_90': xǁPrinterǁ_toolhead_object_updated__mutmut_90, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_91': xǁPrinterǁ_toolhead_object_updated__mutmut_91, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_92': xǁPrinterǁ_toolhead_object_updated__mutmut_92, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_93': xǁPrinterǁ_toolhead_object_updated__mutmut_93, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_94': xǁPrinterǁ_toolhead_object_updated__mutmut_94, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_95': xǁPrinterǁ_toolhead_object_updated__mutmut_95, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_96': xǁPrinterǁ_toolhead_object_updated__mutmut_96, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_97': xǁPrinterǁ_toolhead_object_updated__mutmut_97, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_98': xǁPrinterǁ_toolhead_object_updated__mutmut_98, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_99': xǁPrinterǁ_toolhead_object_updated__mutmut_99, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_100': xǁPrinterǁ_toolhead_object_updated__mutmut_100, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_101': xǁPrinterǁ_toolhead_object_updated__mutmut_101, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_102': xǁPrinterǁ_toolhead_object_updated__mutmut_102, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_103': xǁPrinterǁ_toolhead_object_updated__mutmut_103, 
        'xǁPrinterǁ_toolhead_object_updated__mutmut_104': xǁPrinterǁ_toolhead_object_updated__mutmut_104
    }
    xǁPrinterǁ_toolhead_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_toolhead_object_updated'

    def _extruder_object_updated(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        args = [value, extruder_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_extruder_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_extruder_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_extruder_object_updated__mutmut_orig(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_1(
        self, value: dict, extruder_name: str = "XXextruderXX"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_2(
        self, value: dict, extruder_name: str = "EXTRUDER"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_3(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "XXtemperatureXX" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_4(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "TEMPERATURE" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_5(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" not in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_6(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                None, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_7(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, None, value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_8(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", None
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_9(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_10(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_11(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_12(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "XXtemperatureXX", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_13(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "TEMPERATURE", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_14(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["XXtemperatureXX"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_15(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["TEMPERATURE"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_16(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = None
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_17(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["XXactual_temperatureXX"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_18(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["ACTUAL_TEMPERATURE"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_19(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "XXtemperatureXX"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_20(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "TEMPERATURE"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_21(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "XXtargetXX" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_22(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "TARGET" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_23(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" not in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_24(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(None, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_25(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, None, value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_26(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", None)
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_27(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit("target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_28(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_29(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", )
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_30(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "XXtargetXX", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_31(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "TARGET", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_32(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["XXtargetXX"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_33(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["TARGET"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_34(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = None
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_35(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["XXtarget_temperatureXX"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_36(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["TARGET_TEMPERATURE"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_37(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "XXtargetXX"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_38(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "TARGET"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_39(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "XXcan_extrudeXX" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_40(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "CAN_EXTRUDE" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_41(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" not in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_42(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = None
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_43(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["XXcan_extrudeXX"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_44(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["CAN_EXTRUDE"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_45(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "XXcan_extrudeXX"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_46(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "CAN_EXTRUDE"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_47(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "XXpowerXX" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_48(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "POWER" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_49(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" not in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_50(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(None, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_51(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, None, value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_52(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", None)
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_53(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit("power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_54(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_55(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", )
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_56(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "XXpowerXX", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_57(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "POWER", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_58(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["XXpowerXX"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_59(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["POWER"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_60(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "XXpressure_advanceXX" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_61(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "PRESSURE_ADVANCE" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_62(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" not in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_63(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                None, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_64(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, None, value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_65(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", None
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_66(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_67(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_68(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_69(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "XXpressure_advanceXX", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_70(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "PRESSURE_ADVANCE", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_71(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["XXpressure_advanceXX"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_72(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["PRESSURE_ADVANCE"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_73(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "XXsmooth_timeXX" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_74(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "SMOOTH_TIME" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_75(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" not in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_76(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                None, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_77(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, None, value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_78(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", None
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_79(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                "smooth_time", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_80(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_81(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_82(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "XXsmooth_timeXX", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_83(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "SMOOTH_TIME", value["smooth_time"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_84(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["XXsmooth_timeXX"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_85(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["SMOOTH_TIME"]
            )
        if "can_extrude" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_86(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "XXcan_extrudeXX" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_87(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "CAN_EXTRUDE" in value.keys():
            pass

    def xǁPrinterǁ_extruder_object_updated__mutmut_88(
        self, value: dict, extruder_name: str = "extruder"
    ) -> None:
        if "temperature" in value.keys():
            self.extruder_update.emit(
                extruder_name, "temperature", value["temperature"]
            )
            self.heaters_object[f"{extruder_name}"]["actual_temperature"] = value[
                "temperature"
            ]
        if "target" in value.keys():
            self.extruder_update.emit(extruder_name, "target", value["target"])
            self.heaters_object[f"{extruder_name}"]["target_temperature"] = value[
                "target"
            ]
        if "can_extrude" in value.keys():
            self.heaters_object[f"{extruder_name}"]["can_extrude"] = value[
                "can_extrude"
            ]
        if "power" in value.keys():
            self.extruder_update.emit(extruder_name, "power", value["power"])
        if "pressure_advance" in value.keys():
            self.extruder_update.emit(
                extruder_name, "pressure_advance", value["pressure_advance"]
            )
        if "smooth_time" in value.keys():
            self.extruder_update.emit(
                extruder_name, "smooth_time", value["smooth_time"]
            )
        if "can_extrude" not in value.keys():
            pass
    
    xǁPrinterǁ_extruder_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_extruder_object_updated__mutmut_1': xǁPrinterǁ_extruder_object_updated__mutmut_1, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_2': xǁPrinterǁ_extruder_object_updated__mutmut_2, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_3': xǁPrinterǁ_extruder_object_updated__mutmut_3, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_4': xǁPrinterǁ_extruder_object_updated__mutmut_4, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_5': xǁPrinterǁ_extruder_object_updated__mutmut_5, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_6': xǁPrinterǁ_extruder_object_updated__mutmut_6, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_7': xǁPrinterǁ_extruder_object_updated__mutmut_7, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_8': xǁPrinterǁ_extruder_object_updated__mutmut_8, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_9': xǁPrinterǁ_extruder_object_updated__mutmut_9, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_10': xǁPrinterǁ_extruder_object_updated__mutmut_10, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_11': xǁPrinterǁ_extruder_object_updated__mutmut_11, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_12': xǁPrinterǁ_extruder_object_updated__mutmut_12, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_13': xǁPrinterǁ_extruder_object_updated__mutmut_13, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_14': xǁPrinterǁ_extruder_object_updated__mutmut_14, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_15': xǁPrinterǁ_extruder_object_updated__mutmut_15, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_16': xǁPrinterǁ_extruder_object_updated__mutmut_16, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_17': xǁPrinterǁ_extruder_object_updated__mutmut_17, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_18': xǁPrinterǁ_extruder_object_updated__mutmut_18, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_19': xǁPrinterǁ_extruder_object_updated__mutmut_19, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_20': xǁPrinterǁ_extruder_object_updated__mutmut_20, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_21': xǁPrinterǁ_extruder_object_updated__mutmut_21, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_22': xǁPrinterǁ_extruder_object_updated__mutmut_22, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_23': xǁPrinterǁ_extruder_object_updated__mutmut_23, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_24': xǁPrinterǁ_extruder_object_updated__mutmut_24, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_25': xǁPrinterǁ_extruder_object_updated__mutmut_25, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_26': xǁPrinterǁ_extruder_object_updated__mutmut_26, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_27': xǁPrinterǁ_extruder_object_updated__mutmut_27, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_28': xǁPrinterǁ_extruder_object_updated__mutmut_28, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_29': xǁPrinterǁ_extruder_object_updated__mutmut_29, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_30': xǁPrinterǁ_extruder_object_updated__mutmut_30, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_31': xǁPrinterǁ_extruder_object_updated__mutmut_31, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_32': xǁPrinterǁ_extruder_object_updated__mutmut_32, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_33': xǁPrinterǁ_extruder_object_updated__mutmut_33, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_34': xǁPrinterǁ_extruder_object_updated__mutmut_34, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_35': xǁPrinterǁ_extruder_object_updated__mutmut_35, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_36': xǁPrinterǁ_extruder_object_updated__mutmut_36, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_37': xǁPrinterǁ_extruder_object_updated__mutmut_37, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_38': xǁPrinterǁ_extruder_object_updated__mutmut_38, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_39': xǁPrinterǁ_extruder_object_updated__mutmut_39, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_40': xǁPrinterǁ_extruder_object_updated__mutmut_40, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_41': xǁPrinterǁ_extruder_object_updated__mutmut_41, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_42': xǁPrinterǁ_extruder_object_updated__mutmut_42, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_43': xǁPrinterǁ_extruder_object_updated__mutmut_43, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_44': xǁPrinterǁ_extruder_object_updated__mutmut_44, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_45': xǁPrinterǁ_extruder_object_updated__mutmut_45, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_46': xǁPrinterǁ_extruder_object_updated__mutmut_46, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_47': xǁPrinterǁ_extruder_object_updated__mutmut_47, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_48': xǁPrinterǁ_extruder_object_updated__mutmut_48, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_49': xǁPrinterǁ_extruder_object_updated__mutmut_49, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_50': xǁPrinterǁ_extruder_object_updated__mutmut_50, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_51': xǁPrinterǁ_extruder_object_updated__mutmut_51, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_52': xǁPrinterǁ_extruder_object_updated__mutmut_52, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_53': xǁPrinterǁ_extruder_object_updated__mutmut_53, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_54': xǁPrinterǁ_extruder_object_updated__mutmut_54, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_55': xǁPrinterǁ_extruder_object_updated__mutmut_55, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_56': xǁPrinterǁ_extruder_object_updated__mutmut_56, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_57': xǁPrinterǁ_extruder_object_updated__mutmut_57, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_58': xǁPrinterǁ_extruder_object_updated__mutmut_58, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_59': xǁPrinterǁ_extruder_object_updated__mutmut_59, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_60': xǁPrinterǁ_extruder_object_updated__mutmut_60, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_61': xǁPrinterǁ_extruder_object_updated__mutmut_61, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_62': xǁPrinterǁ_extruder_object_updated__mutmut_62, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_63': xǁPrinterǁ_extruder_object_updated__mutmut_63, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_64': xǁPrinterǁ_extruder_object_updated__mutmut_64, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_65': xǁPrinterǁ_extruder_object_updated__mutmut_65, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_66': xǁPrinterǁ_extruder_object_updated__mutmut_66, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_67': xǁPrinterǁ_extruder_object_updated__mutmut_67, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_68': xǁPrinterǁ_extruder_object_updated__mutmut_68, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_69': xǁPrinterǁ_extruder_object_updated__mutmut_69, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_70': xǁPrinterǁ_extruder_object_updated__mutmut_70, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_71': xǁPrinterǁ_extruder_object_updated__mutmut_71, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_72': xǁPrinterǁ_extruder_object_updated__mutmut_72, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_73': xǁPrinterǁ_extruder_object_updated__mutmut_73, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_74': xǁPrinterǁ_extruder_object_updated__mutmut_74, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_75': xǁPrinterǁ_extruder_object_updated__mutmut_75, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_76': xǁPrinterǁ_extruder_object_updated__mutmut_76, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_77': xǁPrinterǁ_extruder_object_updated__mutmut_77, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_78': xǁPrinterǁ_extruder_object_updated__mutmut_78, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_79': xǁPrinterǁ_extruder_object_updated__mutmut_79, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_80': xǁPrinterǁ_extruder_object_updated__mutmut_80, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_81': xǁPrinterǁ_extruder_object_updated__mutmut_81, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_82': xǁPrinterǁ_extruder_object_updated__mutmut_82, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_83': xǁPrinterǁ_extruder_object_updated__mutmut_83, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_84': xǁPrinterǁ_extruder_object_updated__mutmut_84, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_85': xǁPrinterǁ_extruder_object_updated__mutmut_85, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_86': xǁPrinterǁ_extruder_object_updated__mutmut_86, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_87': xǁPrinterǁ_extruder_object_updated__mutmut_87, 
        'xǁPrinterǁ_extruder_object_updated__mutmut_88': xǁPrinterǁ_extruder_object_updated__mutmut_88
    }
    xǁPrinterǁ_extruder_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_extruder_object_updated'

    def _heater_bed_object_updated(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        args = [value, heater_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_heater_bed_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_heater_bed_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_orig(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_1(
        self, value: dict, heater_name: str = "XXheater_bedXX"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_2(
        self, value: dict, heater_name: str = "HEATER_BED"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_3(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "XXtemperatureXX" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_4(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "TEMPERATURE" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_5(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" not in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_6(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                None, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_7(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, None, value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_8(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", None
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_9(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_10(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_11(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_12(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "XXtemperatureXX", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_13(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "TEMPERATURE", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_14(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["XXtemperatureXX"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_15(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["TEMPERATURE"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_16(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = None
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_17(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["XXbedXX"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_18(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["BED"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_19(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["XXactual_temperatureXX"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_20(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["ACTUAL_TEMPERATURE"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_21(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["XXtemperatureXX"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_22(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["TEMPERATURE"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_23(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "XXtargetXX" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_24(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "TARGET" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_25(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" not in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_26(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(None, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_27(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, None, value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_28(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", None)
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_29(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit("target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_30(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_31(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", )
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_32(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "XXtargetXX", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_33(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "TARGET", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_34(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["XXtargetXX"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_35(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["TARGET"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_36(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = None
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_37(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["XXbedXX"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_38(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["BED"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_39(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["XXtarget_temperatureXX"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_40(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["TARGET_TEMPERATURE"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_41(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["XXtargetXX"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_42(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["TARGET"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_43(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "XXpowerXX" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_44(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "POWER" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_45(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" not in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_46(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(None, "power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_47(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, None, value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_48(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", None)

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_49(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit("power", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_50(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_51(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", )

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_52(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "XXpowerXX", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_53(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "POWER", value["power"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_54(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["XXpowerXX"])

    def xǁPrinterǁ_heater_bed_object_updated__mutmut_55(
        self, value: dict, heater_name: str = "heater_bed"
    ) -> None:
        if "temperature" in value.keys():
            self.heater_bed_update.emit(
                heater_name, "temperature", value["temperature"]
            )
            self.heaters_object["bed"]["actual_temperature"] = value["temperature"]
        if "target" in value.keys():
            self.heater_bed_update.emit(heater_name, "target", value["target"])
            self.heaters_object["bed"]["target_temperature"] = value["target"]
        if "power" in value.keys():
            self.heater_bed_update.emit(heater_name, "power", value["POWER"])
    
    xǁPrinterǁ_heater_bed_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_heater_bed_object_updated__mutmut_1': xǁPrinterǁ_heater_bed_object_updated__mutmut_1, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_2': xǁPrinterǁ_heater_bed_object_updated__mutmut_2, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_3': xǁPrinterǁ_heater_bed_object_updated__mutmut_3, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_4': xǁPrinterǁ_heater_bed_object_updated__mutmut_4, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_5': xǁPrinterǁ_heater_bed_object_updated__mutmut_5, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_6': xǁPrinterǁ_heater_bed_object_updated__mutmut_6, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_7': xǁPrinterǁ_heater_bed_object_updated__mutmut_7, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_8': xǁPrinterǁ_heater_bed_object_updated__mutmut_8, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_9': xǁPrinterǁ_heater_bed_object_updated__mutmut_9, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_10': xǁPrinterǁ_heater_bed_object_updated__mutmut_10, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_11': xǁPrinterǁ_heater_bed_object_updated__mutmut_11, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_12': xǁPrinterǁ_heater_bed_object_updated__mutmut_12, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_13': xǁPrinterǁ_heater_bed_object_updated__mutmut_13, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_14': xǁPrinterǁ_heater_bed_object_updated__mutmut_14, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_15': xǁPrinterǁ_heater_bed_object_updated__mutmut_15, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_16': xǁPrinterǁ_heater_bed_object_updated__mutmut_16, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_17': xǁPrinterǁ_heater_bed_object_updated__mutmut_17, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_18': xǁPrinterǁ_heater_bed_object_updated__mutmut_18, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_19': xǁPrinterǁ_heater_bed_object_updated__mutmut_19, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_20': xǁPrinterǁ_heater_bed_object_updated__mutmut_20, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_21': xǁPrinterǁ_heater_bed_object_updated__mutmut_21, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_22': xǁPrinterǁ_heater_bed_object_updated__mutmut_22, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_23': xǁPrinterǁ_heater_bed_object_updated__mutmut_23, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_24': xǁPrinterǁ_heater_bed_object_updated__mutmut_24, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_25': xǁPrinterǁ_heater_bed_object_updated__mutmut_25, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_26': xǁPrinterǁ_heater_bed_object_updated__mutmut_26, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_27': xǁPrinterǁ_heater_bed_object_updated__mutmut_27, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_28': xǁPrinterǁ_heater_bed_object_updated__mutmut_28, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_29': xǁPrinterǁ_heater_bed_object_updated__mutmut_29, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_30': xǁPrinterǁ_heater_bed_object_updated__mutmut_30, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_31': xǁPrinterǁ_heater_bed_object_updated__mutmut_31, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_32': xǁPrinterǁ_heater_bed_object_updated__mutmut_32, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_33': xǁPrinterǁ_heater_bed_object_updated__mutmut_33, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_34': xǁPrinterǁ_heater_bed_object_updated__mutmut_34, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_35': xǁPrinterǁ_heater_bed_object_updated__mutmut_35, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_36': xǁPrinterǁ_heater_bed_object_updated__mutmut_36, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_37': xǁPrinterǁ_heater_bed_object_updated__mutmut_37, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_38': xǁPrinterǁ_heater_bed_object_updated__mutmut_38, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_39': xǁPrinterǁ_heater_bed_object_updated__mutmut_39, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_40': xǁPrinterǁ_heater_bed_object_updated__mutmut_40, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_41': xǁPrinterǁ_heater_bed_object_updated__mutmut_41, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_42': xǁPrinterǁ_heater_bed_object_updated__mutmut_42, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_43': xǁPrinterǁ_heater_bed_object_updated__mutmut_43, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_44': xǁPrinterǁ_heater_bed_object_updated__mutmut_44, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_45': xǁPrinterǁ_heater_bed_object_updated__mutmut_45, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_46': xǁPrinterǁ_heater_bed_object_updated__mutmut_46, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_47': xǁPrinterǁ_heater_bed_object_updated__mutmut_47, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_48': xǁPrinterǁ_heater_bed_object_updated__mutmut_48, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_49': xǁPrinterǁ_heater_bed_object_updated__mutmut_49, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_50': xǁPrinterǁ_heater_bed_object_updated__mutmut_50, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_51': xǁPrinterǁ_heater_bed_object_updated__mutmut_51, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_52': xǁPrinterǁ_heater_bed_object_updated__mutmut_52, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_53': xǁPrinterǁ_heater_bed_object_updated__mutmut_53, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_54': xǁPrinterǁ_heater_bed_object_updated__mutmut_54, 
        'xǁPrinterǁ_heater_bed_object_updated__mutmut_55': xǁPrinterǁ_heater_bed_object_updated__mutmut_55
    }
    xǁPrinterǁ_heater_bed_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_heater_bed_object_updated'

    def _chamber_object_updated(self, value: dict, heater_name: str = "chamber"):
        args = [value, heater_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_chamber_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_chamber_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_chamber_object_updated__mutmut_orig(self, value: dict, heater_name: str = "chamber"):
        self.has_chamber = True

    def xǁPrinterǁ_chamber_object_updated__mutmut_1(self, value: dict, heater_name: str = "XXchamberXX"):
        self.has_chamber = True

    def xǁPrinterǁ_chamber_object_updated__mutmut_2(self, value: dict, heater_name: str = "CHAMBER"):
        self.has_chamber = True

    def xǁPrinterǁ_chamber_object_updated__mutmut_3(self, value: dict, heater_name: str = "chamber"):
        self.has_chamber = None

    def xǁPrinterǁ_chamber_object_updated__mutmut_4(self, value: dict, heater_name: str = "chamber"):
        self.has_chamber = False
    
    xǁPrinterǁ_chamber_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_chamber_object_updated__mutmut_1': xǁPrinterǁ_chamber_object_updated__mutmut_1, 
        'xǁPrinterǁ_chamber_object_updated__mutmut_2': xǁPrinterǁ_chamber_object_updated__mutmut_2, 
        'xǁPrinterǁ_chamber_object_updated__mutmut_3': xǁPrinterǁ_chamber_object_updated__mutmut_3, 
        'xǁPrinterǁ_chamber_object_updated__mutmut_4': xǁPrinterǁ_chamber_object_updated__mutmut_4
    }
    xǁPrinterǁ_chamber_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_chamber_object_updated'

    def _fan_object_updated(self, value: dict, fan_name: str = "fan") -> None:
        args = [value, fan_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_fan_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_fan_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_fan_object_updated__mutmut_orig(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_1(self, value: dict, fan_name: str = "XXfanXX") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_2(self, value: dict, fan_name: str = "FAN") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_3(self, value: dict, fan_name: str = "fan") -> None:
        if "XXspeedXX" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_4(self, value: dict, fan_name: str = "fan") -> None:
        if "SPEED" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_5(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" not in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_6(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(None, "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_7(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", None, value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_8(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", None)
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_9(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_10(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_11(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_12(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("XXfanXX", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_13(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("FAN", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_14(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "XXspeedXX", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_15(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "SPEED", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_16(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["XXspeedXX"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_17(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["SPEED"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_18(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "XXrpmXX" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_19(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "RPM" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_20(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" not in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_21(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(None, "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_22(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", None, value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_23(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", None)

    def xǁPrinterǁ_fan_object_updated__mutmut_24(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_25(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_26(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", )

    def xǁPrinterǁ_fan_object_updated__mutmut_27(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("XXfanXX", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_28(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("FAN", "rpm", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_29(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "XXrpmXX", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_30(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "RPM", value["rpm"])

    def xǁPrinterǁ_fan_object_updated__mutmut_31(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["XXrpmXX"])

    def xǁPrinterǁ_fan_object_updated__mutmut_32(self, value: dict, fan_name: str = "fan") -> None:
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit("fan", "speed", value["speed"])
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("fan", "rpm", value["RPM"])
    
    xǁPrinterǁ_fan_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_fan_object_updated__mutmut_1': xǁPrinterǁ_fan_object_updated__mutmut_1, 
        'xǁPrinterǁ_fan_object_updated__mutmut_2': xǁPrinterǁ_fan_object_updated__mutmut_2, 
        'xǁPrinterǁ_fan_object_updated__mutmut_3': xǁPrinterǁ_fan_object_updated__mutmut_3, 
        'xǁPrinterǁ_fan_object_updated__mutmut_4': xǁPrinterǁ_fan_object_updated__mutmut_4, 
        'xǁPrinterǁ_fan_object_updated__mutmut_5': xǁPrinterǁ_fan_object_updated__mutmut_5, 
        'xǁPrinterǁ_fan_object_updated__mutmut_6': xǁPrinterǁ_fan_object_updated__mutmut_6, 
        'xǁPrinterǁ_fan_object_updated__mutmut_7': xǁPrinterǁ_fan_object_updated__mutmut_7, 
        'xǁPrinterǁ_fan_object_updated__mutmut_8': xǁPrinterǁ_fan_object_updated__mutmut_8, 
        'xǁPrinterǁ_fan_object_updated__mutmut_9': xǁPrinterǁ_fan_object_updated__mutmut_9, 
        'xǁPrinterǁ_fan_object_updated__mutmut_10': xǁPrinterǁ_fan_object_updated__mutmut_10, 
        'xǁPrinterǁ_fan_object_updated__mutmut_11': xǁPrinterǁ_fan_object_updated__mutmut_11, 
        'xǁPrinterǁ_fan_object_updated__mutmut_12': xǁPrinterǁ_fan_object_updated__mutmut_12, 
        'xǁPrinterǁ_fan_object_updated__mutmut_13': xǁPrinterǁ_fan_object_updated__mutmut_13, 
        'xǁPrinterǁ_fan_object_updated__mutmut_14': xǁPrinterǁ_fan_object_updated__mutmut_14, 
        'xǁPrinterǁ_fan_object_updated__mutmut_15': xǁPrinterǁ_fan_object_updated__mutmut_15, 
        'xǁPrinterǁ_fan_object_updated__mutmut_16': xǁPrinterǁ_fan_object_updated__mutmut_16, 
        'xǁPrinterǁ_fan_object_updated__mutmut_17': xǁPrinterǁ_fan_object_updated__mutmut_17, 
        'xǁPrinterǁ_fan_object_updated__mutmut_18': xǁPrinterǁ_fan_object_updated__mutmut_18, 
        'xǁPrinterǁ_fan_object_updated__mutmut_19': xǁPrinterǁ_fan_object_updated__mutmut_19, 
        'xǁPrinterǁ_fan_object_updated__mutmut_20': xǁPrinterǁ_fan_object_updated__mutmut_20, 
        'xǁPrinterǁ_fan_object_updated__mutmut_21': xǁPrinterǁ_fan_object_updated__mutmut_21, 
        'xǁPrinterǁ_fan_object_updated__mutmut_22': xǁPrinterǁ_fan_object_updated__mutmut_22, 
        'xǁPrinterǁ_fan_object_updated__mutmut_23': xǁPrinterǁ_fan_object_updated__mutmut_23, 
        'xǁPrinterǁ_fan_object_updated__mutmut_24': xǁPrinterǁ_fan_object_updated__mutmut_24, 
        'xǁPrinterǁ_fan_object_updated__mutmut_25': xǁPrinterǁ_fan_object_updated__mutmut_25, 
        'xǁPrinterǁ_fan_object_updated__mutmut_26': xǁPrinterǁ_fan_object_updated__mutmut_26, 
        'xǁPrinterǁ_fan_object_updated__mutmut_27': xǁPrinterǁ_fan_object_updated__mutmut_27, 
        'xǁPrinterǁ_fan_object_updated__mutmut_28': xǁPrinterǁ_fan_object_updated__mutmut_28, 
        'xǁPrinterǁ_fan_object_updated__mutmut_29': xǁPrinterǁ_fan_object_updated__mutmut_29, 
        'xǁPrinterǁ_fan_object_updated__mutmut_30': xǁPrinterǁ_fan_object_updated__mutmut_30, 
        'xǁPrinterǁ_fan_object_updated__mutmut_31': xǁPrinterǁ_fan_object_updated__mutmut_31, 
        'xǁPrinterǁ_fan_object_updated__mutmut_32': xǁPrinterǁ_fan_object_updated__mutmut_32
    }
    xǁPrinterǁ_fan_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_fan_object_updated'

    def _fan_generic_object_updated(self, value: dict, fan_name: str = "") -> None:
        args = [value, fan_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_fan_generic_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_fan_generic_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_orig(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_1(self, value: dict, fan_name: str = "XXXX") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_2(self, value: dict, fan_name: str = "") -> None:
        _names = None
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_3(self, value: dict, fan_name: str = "") -> None:
        _names = ["XXfan_genericXX", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_4(self, value: dict, fan_name: str = "") -> None:
        _names = ["FAN_GENERIC", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_5(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = None
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_6(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(None)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_7(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = "XX XX".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_8(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "XXspeedXX" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_9(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "SPEED" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_10(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" not in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_11(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                None, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_12(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, None, value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_13(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", None
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_14(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_15(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_16(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_17(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "XXspeedXX", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_18(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "SPEED", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_19(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get(None)
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_20(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("XXspeedXX")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_21(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("SPEED")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_22(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "XXrpmXX" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_23(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "RPM" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_24(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" not in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_25(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(None, "rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_26(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, None, value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_27(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", None)

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_28(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit("rpm", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_29(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_30(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", )

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_31(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "XXrpmXX", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_32(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "RPM", value.get("rpm"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_33(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get(None))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_34(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("XXrpmXX"))

    def xǁPrinterǁ_fan_generic_object_updated__mutmut_35(self, value: dict, fan_name: str = "") -> None:
        _names = ["fan_generic", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        if "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("RPM"))
    
    xǁPrinterǁ_fan_generic_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_fan_generic_object_updated__mutmut_1': xǁPrinterǁ_fan_generic_object_updated__mutmut_1, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_2': xǁPrinterǁ_fan_generic_object_updated__mutmut_2, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_3': xǁPrinterǁ_fan_generic_object_updated__mutmut_3, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_4': xǁPrinterǁ_fan_generic_object_updated__mutmut_4, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_5': xǁPrinterǁ_fan_generic_object_updated__mutmut_5, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_6': xǁPrinterǁ_fan_generic_object_updated__mutmut_6, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_7': xǁPrinterǁ_fan_generic_object_updated__mutmut_7, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_8': xǁPrinterǁ_fan_generic_object_updated__mutmut_8, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_9': xǁPrinterǁ_fan_generic_object_updated__mutmut_9, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_10': xǁPrinterǁ_fan_generic_object_updated__mutmut_10, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_11': xǁPrinterǁ_fan_generic_object_updated__mutmut_11, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_12': xǁPrinterǁ_fan_generic_object_updated__mutmut_12, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_13': xǁPrinterǁ_fan_generic_object_updated__mutmut_13, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_14': xǁPrinterǁ_fan_generic_object_updated__mutmut_14, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_15': xǁPrinterǁ_fan_generic_object_updated__mutmut_15, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_16': xǁPrinterǁ_fan_generic_object_updated__mutmut_16, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_17': xǁPrinterǁ_fan_generic_object_updated__mutmut_17, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_18': xǁPrinterǁ_fan_generic_object_updated__mutmut_18, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_19': xǁPrinterǁ_fan_generic_object_updated__mutmut_19, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_20': xǁPrinterǁ_fan_generic_object_updated__mutmut_20, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_21': xǁPrinterǁ_fan_generic_object_updated__mutmut_21, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_22': xǁPrinterǁ_fan_generic_object_updated__mutmut_22, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_23': xǁPrinterǁ_fan_generic_object_updated__mutmut_23, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_24': xǁPrinterǁ_fan_generic_object_updated__mutmut_24, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_25': xǁPrinterǁ_fan_generic_object_updated__mutmut_25, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_26': xǁPrinterǁ_fan_generic_object_updated__mutmut_26, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_27': xǁPrinterǁ_fan_generic_object_updated__mutmut_27, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_28': xǁPrinterǁ_fan_generic_object_updated__mutmut_28, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_29': xǁPrinterǁ_fan_generic_object_updated__mutmut_29, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_30': xǁPrinterǁ_fan_generic_object_updated__mutmut_30, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_31': xǁPrinterǁ_fan_generic_object_updated__mutmut_31, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_32': xǁPrinterǁ_fan_generic_object_updated__mutmut_32, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_33': xǁPrinterǁ_fan_generic_object_updated__mutmut_33, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_34': xǁPrinterǁ_fan_generic_object_updated__mutmut_34, 
        'xǁPrinterǁ_fan_generic_object_updated__mutmut_35': xǁPrinterǁ_fan_generic_object_updated__mutmut_35
    }
    xǁPrinterǁ_fan_generic_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_fan_generic_object_updated'

    def _controller_fan_object_updated(self, value: dict, fan_name: str = "") -> None:
        args = [value, fan_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_controller_fan_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_controller_fan_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_orig(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_1(self, value: dict, fan_name: str = "XXXX") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_2(self, value: dict, fan_name: str = "") -> None:
        _names = None
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_3(self, value: dict, fan_name: str = "") -> None:
        _names = ["XXcontroller_fanXX", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_4(self, value: dict, fan_name: str = "") -> None:
        _names = ["CONTROLLER_FAN", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_5(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = None
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_6(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(None)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_7(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = "XX XX".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_8(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "XXspeedXX" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_9(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "SPEED" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_10(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" not in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_11(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                None, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_12(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, None, value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_13(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", None
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_14(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_15(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_16(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_17(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "XXspeedXX", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_18(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "SPEED", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_19(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get(None)
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_20(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("XXspeedXX")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_21(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("SPEED")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_22(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "XXrpmXX" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_23(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "RPM" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_24(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" not in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_25(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(None, "rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_26(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, None, value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_27(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", None)

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_28(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit("rpm", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_29(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_30(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", )

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_31(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "XXrpmXX", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_32(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "RPM", value.get("rpm"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_33(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get(None))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_34(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("XXrpmXX"))

    def xǁPrinterǁ_controller_fan_object_updated__mutmut_35(self, value: dict, fan_name: str = "") -> None:
        _names = ["controller_fan", fan_name]
        object_name = " ".join(_names)
        if "speed" in value.keys():
            self.fan_update[str, str, float].emit(
                object_name, "speed", value.get("speed")
            )
        elif "rpm" in value.keys():
            self.fan_update[str, str, int].emit(object_name, "rpm", value.get("RPM"))
    
    xǁPrinterǁ_controller_fan_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_controller_fan_object_updated__mutmut_1': xǁPrinterǁ_controller_fan_object_updated__mutmut_1, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_2': xǁPrinterǁ_controller_fan_object_updated__mutmut_2, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_3': xǁPrinterǁ_controller_fan_object_updated__mutmut_3, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_4': xǁPrinterǁ_controller_fan_object_updated__mutmut_4, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_5': xǁPrinterǁ_controller_fan_object_updated__mutmut_5, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_6': xǁPrinterǁ_controller_fan_object_updated__mutmut_6, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_7': xǁPrinterǁ_controller_fan_object_updated__mutmut_7, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_8': xǁPrinterǁ_controller_fan_object_updated__mutmut_8, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_9': xǁPrinterǁ_controller_fan_object_updated__mutmut_9, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_10': xǁPrinterǁ_controller_fan_object_updated__mutmut_10, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_11': xǁPrinterǁ_controller_fan_object_updated__mutmut_11, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_12': xǁPrinterǁ_controller_fan_object_updated__mutmut_12, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_13': xǁPrinterǁ_controller_fan_object_updated__mutmut_13, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_14': xǁPrinterǁ_controller_fan_object_updated__mutmut_14, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_15': xǁPrinterǁ_controller_fan_object_updated__mutmut_15, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_16': xǁPrinterǁ_controller_fan_object_updated__mutmut_16, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_17': xǁPrinterǁ_controller_fan_object_updated__mutmut_17, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_18': xǁPrinterǁ_controller_fan_object_updated__mutmut_18, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_19': xǁPrinterǁ_controller_fan_object_updated__mutmut_19, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_20': xǁPrinterǁ_controller_fan_object_updated__mutmut_20, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_21': xǁPrinterǁ_controller_fan_object_updated__mutmut_21, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_22': xǁPrinterǁ_controller_fan_object_updated__mutmut_22, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_23': xǁPrinterǁ_controller_fan_object_updated__mutmut_23, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_24': xǁPrinterǁ_controller_fan_object_updated__mutmut_24, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_25': xǁPrinterǁ_controller_fan_object_updated__mutmut_25, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_26': xǁPrinterǁ_controller_fan_object_updated__mutmut_26, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_27': xǁPrinterǁ_controller_fan_object_updated__mutmut_27, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_28': xǁPrinterǁ_controller_fan_object_updated__mutmut_28, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_29': xǁPrinterǁ_controller_fan_object_updated__mutmut_29, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_30': xǁPrinterǁ_controller_fan_object_updated__mutmut_30, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_31': xǁPrinterǁ_controller_fan_object_updated__mutmut_31, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_32': xǁPrinterǁ_controller_fan_object_updated__mutmut_32, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_33': xǁPrinterǁ_controller_fan_object_updated__mutmut_33, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_34': xǁPrinterǁ_controller_fan_object_updated__mutmut_34, 
        'xǁPrinterǁ_controller_fan_object_updated__mutmut_35': xǁPrinterǁ_controller_fan_object_updated__mutmut_35
    }
    xǁPrinterǁ_controller_fan_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_controller_fan_object_updated'

    def _heater_fan_object_updated(self, value: dict, fan_name: str = "") -> None:
        args = [value, fan_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_heater_fan_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_heater_fan_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_heater_fan_object_updated__mutmut_orig(self, value: dict, fan_name: str = "") -> None:
        # Associated with a heater, on when heater is active
        # Parameters same as a normal fan
        _names = ["heater_fan", fan_name]
        # object_name = " ".join(_names)

    def xǁPrinterǁ_heater_fan_object_updated__mutmut_1(self, value: dict, fan_name: str = "XXXX") -> None:
        # Associated with a heater, on when heater is active
        # Parameters same as a normal fan
        _names = ["heater_fan", fan_name]
        # object_name = " ".join(_names)

    def xǁPrinterǁ_heater_fan_object_updated__mutmut_2(self, value: dict, fan_name: str = "") -> None:
        # Associated with a heater, on when heater is active
        # Parameters same as a normal fan
        _names = None
        # object_name = " ".join(_names)

    def xǁPrinterǁ_heater_fan_object_updated__mutmut_3(self, value: dict, fan_name: str = "") -> None:
        # Associated with a heater, on when heater is active
        # Parameters same as a normal fan
        _names = ["XXheater_fanXX", fan_name]
        # object_name = " ".join(_names)

    def xǁPrinterǁ_heater_fan_object_updated__mutmut_4(self, value: dict, fan_name: str = "") -> None:
        # Associated with a heater, on when heater is active
        # Parameters same as a normal fan
        _names = ["HEATER_FAN", fan_name]
        # object_name = " ".join(_names)
    
    xǁPrinterǁ_heater_fan_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_heater_fan_object_updated__mutmut_1': xǁPrinterǁ_heater_fan_object_updated__mutmut_1, 
        'xǁPrinterǁ_heater_fan_object_updated__mutmut_2': xǁPrinterǁ_heater_fan_object_updated__mutmut_2, 
        'xǁPrinterǁ_heater_fan_object_updated__mutmut_3': xǁPrinterǁ_heater_fan_object_updated__mutmut_3, 
        'xǁPrinterǁ_heater_fan_object_updated__mutmut_4': xǁPrinterǁ_heater_fan_object_updated__mutmut_4
    }
    xǁPrinterǁ_heater_fan_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_heater_fan_object_updated'

    def _z_tilt_object_updated(self, value: dict, name: str = "") -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_z_tilt_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_z_tilt_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_orig(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_1(self, value: dict, name: str = "XXXX") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_2(self, value: dict, name: str = "") -> None:
        if value["XXappliedXX"]:
            self.z_tilt_update[str, bool].emit("applied", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_3(self, value: dict, name: str = "") -> None:
        if value["APPLIED"]:
            self.z_tilt_update[str, bool].emit("applied", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_4(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit(None, value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_5(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", None)

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_6(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit(value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_7(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", )

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_8(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("XXappliedXX", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_9(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("APPLIED", value["applied"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_10(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", value["XXappliedXX"])

    def xǁPrinterǁ_z_tilt_object_updated__mutmut_11(self, value: dict, name: str = "") -> None:
        if value["applied"]:
            self.z_tilt_update[str, bool].emit("applied", value["APPLIED"])
    
    xǁPrinterǁ_z_tilt_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_z_tilt_object_updated__mutmut_1': xǁPrinterǁ_z_tilt_object_updated__mutmut_1, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_2': xǁPrinterǁ_z_tilt_object_updated__mutmut_2, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_3': xǁPrinterǁ_z_tilt_object_updated__mutmut_3, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_4': xǁPrinterǁ_z_tilt_object_updated__mutmut_4, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_5': xǁPrinterǁ_z_tilt_object_updated__mutmut_5, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_6': xǁPrinterǁ_z_tilt_object_updated__mutmut_6, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_7': xǁPrinterǁ_z_tilt_object_updated__mutmut_7, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_8': xǁPrinterǁ_z_tilt_object_updated__mutmut_8, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_9': xǁPrinterǁ_z_tilt_object_updated__mutmut_9, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_10': xǁPrinterǁ_z_tilt_object_updated__mutmut_10, 
        'xǁPrinterǁ_z_tilt_object_updated__mutmut_11': xǁPrinterǁ_z_tilt_object_updated__mutmut_11
    }
    xǁPrinterǁ_z_tilt_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_z_tilt_object_updated'

    def _idle_timeout_object_updated(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        args = [value, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_idle_timeout_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_idle_timeout_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_orig(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_1(
        self, value: dict, name: str = "XXidle_timeoutXX"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_2(
        self, value: dict, name: str = "IDLE_TIMEOUT"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_3(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "XXstateXX" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_4(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "STATE" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_5(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" not in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_6(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit(None, value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_7(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", None)
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_8(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit(value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_9(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", )
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_10(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("XXstateXX", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_11(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("STATE", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_12(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["XXstateXX"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_13(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["STATE"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_14(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "XXprintingXX" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_15(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "PRINTING" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_16(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" not in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_17(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["XXstateXX"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_18(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["STATE"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_19(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = None
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_20(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = False
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_21(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" or value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_22(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state == "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_23(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "XXprintingXX" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_24(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "PRINTING" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_25(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["XXstateXX"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_26(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["STATE"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_27(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] == "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_28(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "XXprintingXX":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_29(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "PRINTING":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_30(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = None
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_31(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = True
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_32(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "XXprinting_timeXX" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_33(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "PRINTING_TIME" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_34(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" not in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_35(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                None, value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_36(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", None
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_37(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_38(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_39(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "XXprinting_timeXX", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_40(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "PRINTING_TIME", value["printing_time"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_41(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["XXprinting_timeXX"]
            )

    def xǁPrinterǁ_idle_timeout_object_updated__mutmut_42(
        self, value: dict, name: str = "idle_timeout"
    ) -> None:
        if "state" in value.keys():
            self.idle_timeout_update[str, str].emit("state", value["state"])
            if "printing" in value["state"]:
                self.printer_busy = True
            elif self.printing_state != "printing" and value["state"] != "printing":
                # It's also busy if the printer is printing or paused
                self.printer_busy = False
        if "printing_time" in value.keys():
            self.idle_timeout_update[str, float].emit(
                "printing_time", value["PRINTING_TIME"]
            )
    
    xǁPrinterǁ_idle_timeout_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_idle_timeout_object_updated__mutmut_1': xǁPrinterǁ_idle_timeout_object_updated__mutmut_1, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_2': xǁPrinterǁ_idle_timeout_object_updated__mutmut_2, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_3': xǁPrinterǁ_idle_timeout_object_updated__mutmut_3, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_4': xǁPrinterǁ_idle_timeout_object_updated__mutmut_4, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_5': xǁPrinterǁ_idle_timeout_object_updated__mutmut_5, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_6': xǁPrinterǁ_idle_timeout_object_updated__mutmut_6, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_7': xǁPrinterǁ_idle_timeout_object_updated__mutmut_7, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_8': xǁPrinterǁ_idle_timeout_object_updated__mutmut_8, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_9': xǁPrinterǁ_idle_timeout_object_updated__mutmut_9, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_10': xǁPrinterǁ_idle_timeout_object_updated__mutmut_10, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_11': xǁPrinterǁ_idle_timeout_object_updated__mutmut_11, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_12': xǁPrinterǁ_idle_timeout_object_updated__mutmut_12, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_13': xǁPrinterǁ_idle_timeout_object_updated__mutmut_13, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_14': xǁPrinterǁ_idle_timeout_object_updated__mutmut_14, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_15': xǁPrinterǁ_idle_timeout_object_updated__mutmut_15, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_16': xǁPrinterǁ_idle_timeout_object_updated__mutmut_16, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_17': xǁPrinterǁ_idle_timeout_object_updated__mutmut_17, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_18': xǁPrinterǁ_idle_timeout_object_updated__mutmut_18, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_19': xǁPrinterǁ_idle_timeout_object_updated__mutmut_19, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_20': xǁPrinterǁ_idle_timeout_object_updated__mutmut_20, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_21': xǁPrinterǁ_idle_timeout_object_updated__mutmut_21, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_22': xǁPrinterǁ_idle_timeout_object_updated__mutmut_22, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_23': xǁPrinterǁ_idle_timeout_object_updated__mutmut_23, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_24': xǁPrinterǁ_idle_timeout_object_updated__mutmut_24, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_25': xǁPrinterǁ_idle_timeout_object_updated__mutmut_25, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_26': xǁPrinterǁ_idle_timeout_object_updated__mutmut_26, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_27': xǁPrinterǁ_idle_timeout_object_updated__mutmut_27, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_28': xǁPrinterǁ_idle_timeout_object_updated__mutmut_28, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_29': xǁPrinterǁ_idle_timeout_object_updated__mutmut_29, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_30': xǁPrinterǁ_idle_timeout_object_updated__mutmut_30, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_31': xǁPrinterǁ_idle_timeout_object_updated__mutmut_31, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_32': xǁPrinterǁ_idle_timeout_object_updated__mutmut_32, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_33': xǁPrinterǁ_idle_timeout_object_updated__mutmut_33, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_34': xǁPrinterǁ_idle_timeout_object_updated__mutmut_34, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_35': xǁPrinterǁ_idle_timeout_object_updated__mutmut_35, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_36': xǁPrinterǁ_idle_timeout_object_updated__mutmut_36, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_37': xǁPrinterǁ_idle_timeout_object_updated__mutmut_37, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_38': xǁPrinterǁ_idle_timeout_object_updated__mutmut_38, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_39': xǁPrinterǁ_idle_timeout_object_updated__mutmut_39, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_40': xǁPrinterǁ_idle_timeout_object_updated__mutmut_40, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_41': xǁPrinterǁ_idle_timeout_object_updated__mutmut_41, 
        'xǁPrinterǁ_idle_timeout_object_updated__mutmut_42': xǁPrinterǁ_idle_timeout_object_updated__mutmut_42
    }
    xǁPrinterǁ_idle_timeout_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_idle_timeout_object_updated'

    def _virtual_sdcard_object_updated(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_orig(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_1(
        self, values: dict, name: str = "XXvirtual_sdcardXX"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_2(
        self, values: dict, name: str = "VIRTUAL_SDCARD"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_3(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "XXprogressXX" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_4(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "PROGRESS" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_5(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" not in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_6(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit(None, values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_7(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", None)
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_8(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit(values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_9(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", )
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_10(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("XXprogressXX", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_11(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("PROGRESS", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_12(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["XXprogressXX"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_13(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["PROGRESS"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_14(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "XXis_activeXX" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_15(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "IS_ACTIVE" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_16(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" not in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_17(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit(None, values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_18(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", None)
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_19(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit(values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_20(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", )
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_21(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("XXis_activeXX", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_22(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("IS_ACTIVE", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_23(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["XXis_activeXX"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_24(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["IS_ACTIVE"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_25(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "XXfile_positionXX" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_26(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "FILE_POSITION" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_27(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" not in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_28(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                None, float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_29(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", None
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_30(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_31(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_32(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "XXfile_positionXX", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_33(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "FILE_POSITION", float(values["file_position"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_34(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(None)
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_35(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["XXfile_positionXX"])
            )

    def xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_36(
        self, values: dict, name: str = "virtual_sdcard"
    ) -> None:
        if "progress" in values.keys():
            self.virtual_sdcard_update[str, float].emit("progress", values["progress"])
        if "is_active" in values.keys():
            self.virtual_sdcard_update[str, bool].emit("is_active", values["is_active"])
        if "file_position" in values.keys():
            self.virtual_sdcard_update[str, float].emit(
                "file_position", float(values["FILE_POSITION"])
            )
    
    xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_1': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_1, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_2': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_2, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_3': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_3, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_4': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_4, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_5': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_5, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_6': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_6, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_7': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_7, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_8': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_8, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_9': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_9, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_10': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_10, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_11': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_11, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_12': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_12, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_13': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_13, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_14': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_14, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_15': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_15, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_16': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_16, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_17': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_17, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_18': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_18, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_19': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_19, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_20': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_20, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_21': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_21, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_22': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_22, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_23': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_23, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_24': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_24, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_25': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_25, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_26': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_26, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_27': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_27, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_28': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_28, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_29': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_29, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_30': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_30, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_31': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_31, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_32': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_32, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_33': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_33, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_34': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_34, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_35': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_35, 
        'xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_36': xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_36
    }
    xǁPrinterǁ_virtual_sdcard_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_virtual_sdcard_object_updated'

    def send_print_event(self, event: str):
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁsend_print_event__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁsend_print_event__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁsend_print_event__mutmut_orig(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_1(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = None
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_2(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].lower()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_3(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[1].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_4(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = None
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_5(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[2:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_6(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(None, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_7(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, None):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_8(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_9(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, ):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_10(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                None,
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_11(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                None,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_12(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                None,
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_13(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_14(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_15(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_16(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "XXPrint Event Caught, print is %s, calling event %sXX",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_17(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "print event caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_18(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "PRINT EVENT CAUGHT, PRINT IS %S, CALLING EVENT %S",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_19(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = None
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_20(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                None, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_21(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, None
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_22(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_23(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_24(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(None):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_25(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = None
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_26(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(None, _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_27(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), None)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_28(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(_event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_29(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), )
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_30(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError(None)
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_31(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("XXQApplication.instance expected non None valueXX")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_32(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("qapplication.instance expected non none value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_33(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QAPPLICATION.INSTANCE EXPECTED NON NONE VALUE")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_34(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        None, e
                    )

    def xǁPrinterǁsend_print_event__mutmut_35(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", None
                    )

    def xǁPrinterǁsend_print_event__mutmut_36(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        e
                    )

    def xǁPrinterǁsend_print_event__mutmut_37(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "Unexpected error while posting print job start event: %s", )

    def xǁPrinterǁsend_print_event__mutmut_38(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "XXUnexpected error while posting print job start event: %sXX", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_39(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "unexpected error while posting print job start event: %s", e
                    )

    def xǁPrinterǁsend_print_event__mutmut_40(self, event: str):
        """Dispatches a print event throughout the gui

        Args:
            event (str): event name

        Raises:
            TypeError: Thrown when QApplication is None
        """
        _print_state_upper = event[0].upper()
        _print_state_call = f"{_print_state_upper}{event[1:]}"
        if hasattr(events, f"Print{_print_state_call}"):
            logger.debug(
                "Print Event Caught, print is %s, calling event %s",
                _print_state_call,
                f"Print{_print_state_call}",
            )
            _event_callback: QtCore.QEvent = getattr(
                events, f"Print{_print_state_call}"
            )
            if callable(_event_callback):
                try:
                    instance = QtWidgets.QApplication.instance()
                    if instance:
                        instance.postEvent(self.window(), _event_callback)
                    else:
                        raise TypeError("QApplication.instance expected non None value")
                except Exception as e:
                    logger.info(
                        "UNEXPECTED ERROR WHILE POSTING PRINT JOB START EVENT: %S", e
                    )
    
    xǁPrinterǁsend_print_event__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁsend_print_event__mutmut_1': xǁPrinterǁsend_print_event__mutmut_1, 
        'xǁPrinterǁsend_print_event__mutmut_2': xǁPrinterǁsend_print_event__mutmut_2, 
        'xǁPrinterǁsend_print_event__mutmut_3': xǁPrinterǁsend_print_event__mutmut_3, 
        'xǁPrinterǁsend_print_event__mutmut_4': xǁPrinterǁsend_print_event__mutmut_4, 
        'xǁPrinterǁsend_print_event__mutmut_5': xǁPrinterǁsend_print_event__mutmut_5, 
        'xǁPrinterǁsend_print_event__mutmut_6': xǁPrinterǁsend_print_event__mutmut_6, 
        'xǁPrinterǁsend_print_event__mutmut_7': xǁPrinterǁsend_print_event__mutmut_7, 
        'xǁPrinterǁsend_print_event__mutmut_8': xǁPrinterǁsend_print_event__mutmut_8, 
        'xǁPrinterǁsend_print_event__mutmut_9': xǁPrinterǁsend_print_event__mutmut_9, 
        'xǁPrinterǁsend_print_event__mutmut_10': xǁPrinterǁsend_print_event__mutmut_10, 
        'xǁPrinterǁsend_print_event__mutmut_11': xǁPrinterǁsend_print_event__mutmut_11, 
        'xǁPrinterǁsend_print_event__mutmut_12': xǁPrinterǁsend_print_event__mutmut_12, 
        'xǁPrinterǁsend_print_event__mutmut_13': xǁPrinterǁsend_print_event__mutmut_13, 
        'xǁPrinterǁsend_print_event__mutmut_14': xǁPrinterǁsend_print_event__mutmut_14, 
        'xǁPrinterǁsend_print_event__mutmut_15': xǁPrinterǁsend_print_event__mutmut_15, 
        'xǁPrinterǁsend_print_event__mutmut_16': xǁPrinterǁsend_print_event__mutmut_16, 
        'xǁPrinterǁsend_print_event__mutmut_17': xǁPrinterǁsend_print_event__mutmut_17, 
        'xǁPrinterǁsend_print_event__mutmut_18': xǁPrinterǁsend_print_event__mutmut_18, 
        'xǁPrinterǁsend_print_event__mutmut_19': xǁPrinterǁsend_print_event__mutmut_19, 
        'xǁPrinterǁsend_print_event__mutmut_20': xǁPrinterǁsend_print_event__mutmut_20, 
        'xǁPrinterǁsend_print_event__mutmut_21': xǁPrinterǁsend_print_event__mutmut_21, 
        'xǁPrinterǁsend_print_event__mutmut_22': xǁPrinterǁsend_print_event__mutmut_22, 
        'xǁPrinterǁsend_print_event__mutmut_23': xǁPrinterǁsend_print_event__mutmut_23, 
        'xǁPrinterǁsend_print_event__mutmut_24': xǁPrinterǁsend_print_event__mutmut_24, 
        'xǁPrinterǁsend_print_event__mutmut_25': xǁPrinterǁsend_print_event__mutmut_25, 
        'xǁPrinterǁsend_print_event__mutmut_26': xǁPrinterǁsend_print_event__mutmut_26, 
        'xǁPrinterǁsend_print_event__mutmut_27': xǁPrinterǁsend_print_event__mutmut_27, 
        'xǁPrinterǁsend_print_event__mutmut_28': xǁPrinterǁsend_print_event__mutmut_28, 
        'xǁPrinterǁsend_print_event__mutmut_29': xǁPrinterǁsend_print_event__mutmut_29, 
        'xǁPrinterǁsend_print_event__mutmut_30': xǁPrinterǁsend_print_event__mutmut_30, 
        'xǁPrinterǁsend_print_event__mutmut_31': xǁPrinterǁsend_print_event__mutmut_31, 
        'xǁPrinterǁsend_print_event__mutmut_32': xǁPrinterǁsend_print_event__mutmut_32, 
        'xǁPrinterǁsend_print_event__mutmut_33': xǁPrinterǁsend_print_event__mutmut_33, 
        'xǁPrinterǁsend_print_event__mutmut_34': xǁPrinterǁsend_print_event__mutmut_34, 
        'xǁPrinterǁsend_print_event__mutmut_35': xǁPrinterǁsend_print_event__mutmut_35, 
        'xǁPrinterǁsend_print_event__mutmut_36': xǁPrinterǁsend_print_event__mutmut_36, 
        'xǁPrinterǁsend_print_event__mutmut_37': xǁPrinterǁsend_print_event__mutmut_37, 
        'xǁPrinterǁsend_print_event__mutmut_38': xǁPrinterǁsend_print_event__mutmut_38, 
        'xǁPrinterǁsend_print_event__mutmut_39': xǁPrinterǁsend_print_event__mutmut_39, 
        'xǁPrinterǁsend_print_event__mutmut_40': xǁPrinterǁsend_print_event__mutmut_40
    }
    xǁPrinterǁsend_print_event__mutmut_orig.__name__ = 'xǁPrinterǁsend_print_event'

    def _print_stats_object_updated(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_print_stats_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_print_stats_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_print_stats_object_updated__mutmut_orig(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_1(
        self, values: dict, name: str = "XXprint_statsXX"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_2(
        self, values: dict, name: str = "PRINT_STATS"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_3(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "XXfilenameXX" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_4(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "FILENAME" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_5(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" not in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_6(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit(None, values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_7(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", None)
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_8(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit(values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_9(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", )
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_10(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("XXfilenameXX", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_11(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("FILENAME", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_12(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["XXfilenameXX"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_13(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["FILENAME"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_14(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = None
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_15(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = False
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_16(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "XXtotal_durationXX" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_17(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "TOTAL_DURATION" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_18(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" not in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_19(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                None, values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_20(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", None
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_21(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_22(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_23(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "XXtotal_durationXX", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_24(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "TOTAL_DURATION", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_25(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["XXtotal_durationXX"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_26(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["TOTAL_DURATION"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_27(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "XXprint_durationXX" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_28(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "PRINT_DURATION" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_29(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" not in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_30(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                None, values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_31(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", None
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_32(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_33(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_34(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "XXprint_durationXX", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_35(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "PRINT_DURATION", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_36(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["XXprint_durationXX"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_37(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["PRINT_DURATION"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_38(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "XXfilament_usedXX" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_39(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "FILAMENT_USED" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_40(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" not in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_41(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                None, values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_42(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", None
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_43(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_44(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_45(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "XXfilament_usedXX", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_46(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "FILAMENT_USED", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_47(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["XXfilament_usedXX"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_48(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["FILAMENT_USED"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_49(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "XXstateXX" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_50(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "STATE" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_51(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" not in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_52(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit(None, values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_53(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", None)
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_54(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit(values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_55(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", )
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_56(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("XXstateXX", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_57(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("STATE", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_58(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["XXstateXX"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_59(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["STATE"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_60(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = None
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_61(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get(None, None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_62(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get(None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_63(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", )
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_64(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("XXstateXX", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_65(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("STATE", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_66(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_67(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(None)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_68(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" and values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_69(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["XXstateXX"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_70(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["STATE"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_71(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] != "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_72(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "XXstandbyXX" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_73(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "STANDBY" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_74(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["XXstateXX"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_75(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["STATE"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_76(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] != "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_77(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "XXerrorXX":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_78(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "ERROR":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_79(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = None
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_80(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = True
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_81(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = None
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_82(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = True
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_83(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = None
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_84(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = False
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_85(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" and values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_86(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["XXstateXX"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_87(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["STATE"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_88(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] != "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_89(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "XXprintingXX" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_90(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "PRINTING" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_91(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["XXstateXX"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_92(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["STATE"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_93(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] != "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_94(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "XXpauseXX":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_95(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "PAUSE":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_96(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = None
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_97(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = False
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_98(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "XXmessageXX" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_99(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "MESSAGE" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_100(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" not in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_101(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit(None, values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_102(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", None)
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_103(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit(values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_104(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", )
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_105(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("XXmessageXX", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_106(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("MESSAGE", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_107(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["XXmessageXX"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_108(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["MESSAGE"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_109(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "XXinfoXX" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_110(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "INFO" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_111(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" not in values.keys():
            self.print_stats_update[str, dict].emit("info", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_112(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit(None, values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_113(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", None)

    def xǁPrinterǁ_print_stats_object_updated__mutmut_114(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit(values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_115(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", )

    def xǁPrinterǁ_print_stats_object_updated__mutmut_116(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("XXinfoXX", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_117(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("INFO", values["info"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_118(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["XXinfoXX"])

    def xǁPrinterǁ_print_stats_object_updated__mutmut_119(
        self, values: dict, name: str = "print_stats"
    ) -> None:
        if "filename" in values.keys():
            self.print_stats_update[str, str].emit("filename", values["filename"])
            self.print_file_loaded = True
        if "total_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "total_duration", values["total_duration"]
            )
        if "print_duration" in values.keys():
            self.print_stats_update[str, float].emit(
                "print_duration", values["print_duration"]
            )
        if "filament_used" in values.keys():
            self.print_stats_update[str, float].emit(
                "filament_used", values["filament_used"]
            )
        if "state" in values.keys():
            self.print_stats_update[str, str].emit("state", values["state"])
            self.printing_state = values.get("state", None)
            if not self.printing_state:
                return
            self.send_print_event(self.printing_state)
            if values["state"] == "standby" or values["state"] == "error":
                self.print_file_loaded = False
                self.printing = False
            else:
                self.print_file_loaded = True
                if values["state"] == "printing" or values["state"] == "pause":
                    self.printing = True
        if "message" in values.keys():
            self.print_stats_update[str, str].emit("message", values["message"])
        if "info" in values.keys():
            self.print_stats_update[str, dict].emit("info", values["INFO"])
    
    xǁPrinterǁ_print_stats_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_print_stats_object_updated__mutmut_1': xǁPrinterǁ_print_stats_object_updated__mutmut_1, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_2': xǁPrinterǁ_print_stats_object_updated__mutmut_2, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_3': xǁPrinterǁ_print_stats_object_updated__mutmut_3, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_4': xǁPrinterǁ_print_stats_object_updated__mutmut_4, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_5': xǁPrinterǁ_print_stats_object_updated__mutmut_5, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_6': xǁPrinterǁ_print_stats_object_updated__mutmut_6, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_7': xǁPrinterǁ_print_stats_object_updated__mutmut_7, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_8': xǁPrinterǁ_print_stats_object_updated__mutmut_8, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_9': xǁPrinterǁ_print_stats_object_updated__mutmut_9, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_10': xǁPrinterǁ_print_stats_object_updated__mutmut_10, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_11': xǁPrinterǁ_print_stats_object_updated__mutmut_11, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_12': xǁPrinterǁ_print_stats_object_updated__mutmut_12, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_13': xǁPrinterǁ_print_stats_object_updated__mutmut_13, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_14': xǁPrinterǁ_print_stats_object_updated__mutmut_14, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_15': xǁPrinterǁ_print_stats_object_updated__mutmut_15, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_16': xǁPrinterǁ_print_stats_object_updated__mutmut_16, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_17': xǁPrinterǁ_print_stats_object_updated__mutmut_17, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_18': xǁPrinterǁ_print_stats_object_updated__mutmut_18, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_19': xǁPrinterǁ_print_stats_object_updated__mutmut_19, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_20': xǁPrinterǁ_print_stats_object_updated__mutmut_20, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_21': xǁPrinterǁ_print_stats_object_updated__mutmut_21, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_22': xǁPrinterǁ_print_stats_object_updated__mutmut_22, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_23': xǁPrinterǁ_print_stats_object_updated__mutmut_23, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_24': xǁPrinterǁ_print_stats_object_updated__mutmut_24, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_25': xǁPrinterǁ_print_stats_object_updated__mutmut_25, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_26': xǁPrinterǁ_print_stats_object_updated__mutmut_26, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_27': xǁPrinterǁ_print_stats_object_updated__mutmut_27, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_28': xǁPrinterǁ_print_stats_object_updated__mutmut_28, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_29': xǁPrinterǁ_print_stats_object_updated__mutmut_29, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_30': xǁPrinterǁ_print_stats_object_updated__mutmut_30, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_31': xǁPrinterǁ_print_stats_object_updated__mutmut_31, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_32': xǁPrinterǁ_print_stats_object_updated__mutmut_32, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_33': xǁPrinterǁ_print_stats_object_updated__mutmut_33, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_34': xǁPrinterǁ_print_stats_object_updated__mutmut_34, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_35': xǁPrinterǁ_print_stats_object_updated__mutmut_35, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_36': xǁPrinterǁ_print_stats_object_updated__mutmut_36, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_37': xǁPrinterǁ_print_stats_object_updated__mutmut_37, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_38': xǁPrinterǁ_print_stats_object_updated__mutmut_38, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_39': xǁPrinterǁ_print_stats_object_updated__mutmut_39, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_40': xǁPrinterǁ_print_stats_object_updated__mutmut_40, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_41': xǁPrinterǁ_print_stats_object_updated__mutmut_41, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_42': xǁPrinterǁ_print_stats_object_updated__mutmut_42, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_43': xǁPrinterǁ_print_stats_object_updated__mutmut_43, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_44': xǁPrinterǁ_print_stats_object_updated__mutmut_44, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_45': xǁPrinterǁ_print_stats_object_updated__mutmut_45, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_46': xǁPrinterǁ_print_stats_object_updated__mutmut_46, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_47': xǁPrinterǁ_print_stats_object_updated__mutmut_47, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_48': xǁPrinterǁ_print_stats_object_updated__mutmut_48, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_49': xǁPrinterǁ_print_stats_object_updated__mutmut_49, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_50': xǁPrinterǁ_print_stats_object_updated__mutmut_50, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_51': xǁPrinterǁ_print_stats_object_updated__mutmut_51, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_52': xǁPrinterǁ_print_stats_object_updated__mutmut_52, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_53': xǁPrinterǁ_print_stats_object_updated__mutmut_53, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_54': xǁPrinterǁ_print_stats_object_updated__mutmut_54, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_55': xǁPrinterǁ_print_stats_object_updated__mutmut_55, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_56': xǁPrinterǁ_print_stats_object_updated__mutmut_56, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_57': xǁPrinterǁ_print_stats_object_updated__mutmut_57, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_58': xǁPrinterǁ_print_stats_object_updated__mutmut_58, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_59': xǁPrinterǁ_print_stats_object_updated__mutmut_59, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_60': xǁPrinterǁ_print_stats_object_updated__mutmut_60, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_61': xǁPrinterǁ_print_stats_object_updated__mutmut_61, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_62': xǁPrinterǁ_print_stats_object_updated__mutmut_62, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_63': xǁPrinterǁ_print_stats_object_updated__mutmut_63, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_64': xǁPrinterǁ_print_stats_object_updated__mutmut_64, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_65': xǁPrinterǁ_print_stats_object_updated__mutmut_65, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_66': xǁPrinterǁ_print_stats_object_updated__mutmut_66, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_67': xǁPrinterǁ_print_stats_object_updated__mutmut_67, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_68': xǁPrinterǁ_print_stats_object_updated__mutmut_68, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_69': xǁPrinterǁ_print_stats_object_updated__mutmut_69, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_70': xǁPrinterǁ_print_stats_object_updated__mutmut_70, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_71': xǁPrinterǁ_print_stats_object_updated__mutmut_71, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_72': xǁPrinterǁ_print_stats_object_updated__mutmut_72, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_73': xǁPrinterǁ_print_stats_object_updated__mutmut_73, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_74': xǁPrinterǁ_print_stats_object_updated__mutmut_74, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_75': xǁPrinterǁ_print_stats_object_updated__mutmut_75, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_76': xǁPrinterǁ_print_stats_object_updated__mutmut_76, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_77': xǁPrinterǁ_print_stats_object_updated__mutmut_77, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_78': xǁPrinterǁ_print_stats_object_updated__mutmut_78, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_79': xǁPrinterǁ_print_stats_object_updated__mutmut_79, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_80': xǁPrinterǁ_print_stats_object_updated__mutmut_80, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_81': xǁPrinterǁ_print_stats_object_updated__mutmut_81, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_82': xǁPrinterǁ_print_stats_object_updated__mutmut_82, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_83': xǁPrinterǁ_print_stats_object_updated__mutmut_83, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_84': xǁPrinterǁ_print_stats_object_updated__mutmut_84, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_85': xǁPrinterǁ_print_stats_object_updated__mutmut_85, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_86': xǁPrinterǁ_print_stats_object_updated__mutmut_86, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_87': xǁPrinterǁ_print_stats_object_updated__mutmut_87, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_88': xǁPrinterǁ_print_stats_object_updated__mutmut_88, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_89': xǁPrinterǁ_print_stats_object_updated__mutmut_89, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_90': xǁPrinterǁ_print_stats_object_updated__mutmut_90, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_91': xǁPrinterǁ_print_stats_object_updated__mutmut_91, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_92': xǁPrinterǁ_print_stats_object_updated__mutmut_92, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_93': xǁPrinterǁ_print_stats_object_updated__mutmut_93, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_94': xǁPrinterǁ_print_stats_object_updated__mutmut_94, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_95': xǁPrinterǁ_print_stats_object_updated__mutmut_95, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_96': xǁPrinterǁ_print_stats_object_updated__mutmut_96, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_97': xǁPrinterǁ_print_stats_object_updated__mutmut_97, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_98': xǁPrinterǁ_print_stats_object_updated__mutmut_98, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_99': xǁPrinterǁ_print_stats_object_updated__mutmut_99, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_100': xǁPrinterǁ_print_stats_object_updated__mutmut_100, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_101': xǁPrinterǁ_print_stats_object_updated__mutmut_101, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_102': xǁPrinterǁ_print_stats_object_updated__mutmut_102, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_103': xǁPrinterǁ_print_stats_object_updated__mutmut_103, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_104': xǁPrinterǁ_print_stats_object_updated__mutmut_104, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_105': xǁPrinterǁ_print_stats_object_updated__mutmut_105, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_106': xǁPrinterǁ_print_stats_object_updated__mutmut_106, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_107': xǁPrinterǁ_print_stats_object_updated__mutmut_107, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_108': xǁPrinterǁ_print_stats_object_updated__mutmut_108, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_109': xǁPrinterǁ_print_stats_object_updated__mutmut_109, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_110': xǁPrinterǁ_print_stats_object_updated__mutmut_110, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_111': xǁPrinterǁ_print_stats_object_updated__mutmut_111, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_112': xǁPrinterǁ_print_stats_object_updated__mutmut_112, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_113': xǁPrinterǁ_print_stats_object_updated__mutmut_113, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_114': xǁPrinterǁ_print_stats_object_updated__mutmut_114, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_115': xǁPrinterǁ_print_stats_object_updated__mutmut_115, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_116': xǁPrinterǁ_print_stats_object_updated__mutmut_116, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_117': xǁPrinterǁ_print_stats_object_updated__mutmut_117, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_118': xǁPrinterǁ_print_stats_object_updated__mutmut_118, 
        'xǁPrinterǁ_print_stats_object_updated__mutmut_119': xǁPrinterǁ_print_stats_object_updated__mutmut_119
    }
    xǁPrinterǁ_print_stats_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_print_stats_object_updated'

    def _display_status_object_updated(
        self, values: dict, name: str = "display_status"
    ) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_display_status_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_display_status_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_display_status_object_updated__mutmut_orig(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_1(
        self, values: dict, name: str = "XXdisplay_statusXX"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_2(
        self, values: dict, name: str = "DISPLAY_STATUS"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_3(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "XXmessageXX" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_4(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "MESSAGE" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_5(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" not in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_6(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit(None, values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_7(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", None)
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_8(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit(values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_9(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", )
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_10(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("XXmessageXX", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_11(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("MESSAGE", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_12(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["XXmessageXX"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_13(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["MESSAGE"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_14(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "XXprogressXX" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_15(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "PROGRESS" in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_16(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" not in values.keys():
            self.display_update[str, float].emit("progress", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_17(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit(None, values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_18(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", None)

    def xǁPrinterǁ_display_status_object_updated__mutmut_19(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit(values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_20(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", )

    def xǁPrinterǁ_display_status_object_updated__mutmut_21(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("XXprogressXX", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_22(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("PROGRESS", values["progress"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_23(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["XXprogressXX"])

    def xǁPrinterǁ_display_status_object_updated__mutmut_24(
        self, values: dict, name: str = "display_status"
    ) -> None:
        if "message" in values.keys():
            self.display_update[str, str].emit("message", values["message"])
        if "progress" in values.keys():
            self.display_update[str, float].emit("progress", values["PROGRESS"])
    
    xǁPrinterǁ_display_status_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_display_status_object_updated__mutmut_1': xǁPrinterǁ_display_status_object_updated__mutmut_1, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_2': xǁPrinterǁ_display_status_object_updated__mutmut_2, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_3': xǁPrinterǁ_display_status_object_updated__mutmut_3, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_4': xǁPrinterǁ_display_status_object_updated__mutmut_4, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_5': xǁPrinterǁ_display_status_object_updated__mutmut_5, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_6': xǁPrinterǁ_display_status_object_updated__mutmut_6, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_7': xǁPrinterǁ_display_status_object_updated__mutmut_7, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_8': xǁPrinterǁ_display_status_object_updated__mutmut_8, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_9': xǁPrinterǁ_display_status_object_updated__mutmut_9, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_10': xǁPrinterǁ_display_status_object_updated__mutmut_10, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_11': xǁPrinterǁ_display_status_object_updated__mutmut_11, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_12': xǁPrinterǁ_display_status_object_updated__mutmut_12, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_13': xǁPrinterǁ_display_status_object_updated__mutmut_13, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_14': xǁPrinterǁ_display_status_object_updated__mutmut_14, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_15': xǁPrinterǁ_display_status_object_updated__mutmut_15, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_16': xǁPrinterǁ_display_status_object_updated__mutmut_16, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_17': xǁPrinterǁ_display_status_object_updated__mutmut_17, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_18': xǁPrinterǁ_display_status_object_updated__mutmut_18, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_19': xǁPrinterǁ_display_status_object_updated__mutmut_19, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_20': xǁPrinterǁ_display_status_object_updated__mutmut_20, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_21': xǁPrinterǁ_display_status_object_updated__mutmut_21, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_22': xǁPrinterǁ_display_status_object_updated__mutmut_22, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_23': xǁPrinterǁ_display_status_object_updated__mutmut_23, 
        'xǁPrinterǁ_display_status_object_updated__mutmut_24': xǁPrinterǁ_display_status_object_updated__mutmut_24
    }
    xǁPrinterǁ_display_status_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_display_status_object_updated'

    def _temperature_sensor_object_updated(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        args = [values, temperature_sensor_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_orig(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_1(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "XXtemperatureXX" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_2(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "TEMPERATURE" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_3(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" not in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_4(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                None, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_5(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, None, values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_6(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", None
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_7(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_8(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_9(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_10(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "XXtemperatureXX", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_11(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "TEMPERATURE", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_12(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["XXtemperatureXX"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_13(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["TEMPERATURE"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_14(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "XXmeasured_min_tempXX" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_15(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "MEASURED_MIN_TEMP" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_16(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" not in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_17(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                None,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_18(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                None,
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_19(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                None,
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_20(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_21(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_22(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_23(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "XXmeasured_min_tempXX",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_24(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "MEASURED_MIN_TEMP",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_25(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["XXmeasured_min_tempXX"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_26(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["MEASURED_MIN_TEMP"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_27(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "XXmeasured_max_tempXX" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_28(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "MEASURED_MAX_TEMP" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_29(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" not in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_30(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                None,
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_31(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                None,
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_32(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                None,
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_33(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                "measured_max_temp",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_34(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_35(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_36(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "XXmeasured_max_tempXX",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_37(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "MEASURED_MAX_TEMP",
                values["measured_max_temp"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_38(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["XXmeasured_max_tempXX"],
            )

    def xǁPrinterǁ_temperature_sensor_object_updated__mutmut_39(
        self, values: dict, temperature_sensor_name: str
    ) -> None:
        if "temperature" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name, "temperature", values["temperature"]
            )
        if "measured_min_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_min_temp",
                values["measured_min_temp"],
            )
        if "measured_max_temp" in values.keys():
            self.temperature_sensor_update.emit(
                temperature_sensor_name,
                "measured_max_temp",
                values["MEASURED_MAX_TEMP"],
            )
    
    xǁPrinterǁ_temperature_sensor_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_1': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_1, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_2': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_2, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_3': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_3, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_4': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_4, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_5': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_5, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_6': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_6, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_7': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_7, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_8': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_8, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_9': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_9, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_10': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_10, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_11': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_11, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_12': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_12, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_13': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_13, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_14': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_14, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_15': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_15, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_16': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_16, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_17': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_17, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_18': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_18, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_19': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_19, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_20': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_20, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_21': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_21, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_22': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_22, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_23': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_23, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_24': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_24, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_25': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_25, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_26': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_26, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_27': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_27, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_28': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_28, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_29': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_29, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_30': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_30, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_31': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_31, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_32': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_32, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_33': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_33, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_34': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_34, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_35': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_35, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_36': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_36, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_37': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_37, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_38': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_38, 
        'xǁPrinterǁ_temperature_sensor_object_updated__mutmut_39': xǁPrinterǁ_temperature_sensor_object_updated__mutmut_39
    }
    xǁPrinterǁ_temperature_sensor_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_temperature_sensor_object_updated'

    def _temperature_fan_object_updated(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        args = [values, temperature_fan_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_temperature_fan_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_temperature_fan_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_orig(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_1(
        self, values: dict, temperature_fan_name: str = "XXXX"
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_2(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = None
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_3(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["XXtemperature_fanXX", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_4(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["TEMPERATURE_FAN", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_5(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = None
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_6(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(None)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_7(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = "XX XX".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_8(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "XXspeedXX" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_9(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "SPEED" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_10(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" not in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_11(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                None,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_12(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                None,
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_13(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                None,
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_14(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_15(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_16(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_17(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "XXspeedXX",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_18(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "SPEED",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_19(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["XXspeedXX"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_20(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["SPEED"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_21(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "XXtemperatureXX" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_22(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "TEMPERATURE" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_23(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" not in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_24(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                None,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_25(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                None,
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_26(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                None,
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_27(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_28(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_29(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_30(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "XXtemperatureXX",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_31(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "TEMPERATURE",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_32(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["XXtemperatureXX"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_33(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["TEMPERATURE"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_34(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "XXtargetXX" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_35(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "TARGET" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_36(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" not in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_37(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                None,
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_38(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                None,
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_39(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                None,
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_40(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                "target",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_41(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_42(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_43(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "XXtargetXX",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_44(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "TARGET",
                values["target"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_45(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["XXtargetXX"],
            )

    def xǁPrinterǁ_temperature_fan_object_updated__mutmut_46(
        self, values: dict, temperature_fan_name: str = ""
    ) -> None:
        _names = ["temperature_fan", temperature_fan_name]
        object_name = " ".join(_names)
        if "speed" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "speed",
                values["speed"],
            )
        if "temperature" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "temperature",
                values["temperature"],
            )
        if "target" in values.keys():
            self.temperature_fan_update.emit(
                object_name,
                "target",
                values["TARGET"],
            )
    
    xǁPrinterǁ_temperature_fan_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_temperature_fan_object_updated__mutmut_1': xǁPrinterǁ_temperature_fan_object_updated__mutmut_1, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_2': xǁPrinterǁ_temperature_fan_object_updated__mutmut_2, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_3': xǁPrinterǁ_temperature_fan_object_updated__mutmut_3, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_4': xǁPrinterǁ_temperature_fan_object_updated__mutmut_4, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_5': xǁPrinterǁ_temperature_fan_object_updated__mutmut_5, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_6': xǁPrinterǁ_temperature_fan_object_updated__mutmut_6, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_7': xǁPrinterǁ_temperature_fan_object_updated__mutmut_7, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_8': xǁPrinterǁ_temperature_fan_object_updated__mutmut_8, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_9': xǁPrinterǁ_temperature_fan_object_updated__mutmut_9, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_10': xǁPrinterǁ_temperature_fan_object_updated__mutmut_10, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_11': xǁPrinterǁ_temperature_fan_object_updated__mutmut_11, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_12': xǁPrinterǁ_temperature_fan_object_updated__mutmut_12, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_13': xǁPrinterǁ_temperature_fan_object_updated__mutmut_13, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_14': xǁPrinterǁ_temperature_fan_object_updated__mutmut_14, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_15': xǁPrinterǁ_temperature_fan_object_updated__mutmut_15, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_16': xǁPrinterǁ_temperature_fan_object_updated__mutmut_16, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_17': xǁPrinterǁ_temperature_fan_object_updated__mutmut_17, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_18': xǁPrinterǁ_temperature_fan_object_updated__mutmut_18, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_19': xǁPrinterǁ_temperature_fan_object_updated__mutmut_19, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_20': xǁPrinterǁ_temperature_fan_object_updated__mutmut_20, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_21': xǁPrinterǁ_temperature_fan_object_updated__mutmut_21, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_22': xǁPrinterǁ_temperature_fan_object_updated__mutmut_22, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_23': xǁPrinterǁ_temperature_fan_object_updated__mutmut_23, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_24': xǁPrinterǁ_temperature_fan_object_updated__mutmut_24, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_25': xǁPrinterǁ_temperature_fan_object_updated__mutmut_25, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_26': xǁPrinterǁ_temperature_fan_object_updated__mutmut_26, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_27': xǁPrinterǁ_temperature_fan_object_updated__mutmut_27, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_28': xǁPrinterǁ_temperature_fan_object_updated__mutmut_28, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_29': xǁPrinterǁ_temperature_fan_object_updated__mutmut_29, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_30': xǁPrinterǁ_temperature_fan_object_updated__mutmut_30, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_31': xǁPrinterǁ_temperature_fan_object_updated__mutmut_31, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_32': xǁPrinterǁ_temperature_fan_object_updated__mutmut_32, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_33': xǁPrinterǁ_temperature_fan_object_updated__mutmut_33, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_34': xǁPrinterǁ_temperature_fan_object_updated__mutmut_34, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_35': xǁPrinterǁ_temperature_fan_object_updated__mutmut_35, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_36': xǁPrinterǁ_temperature_fan_object_updated__mutmut_36, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_37': xǁPrinterǁ_temperature_fan_object_updated__mutmut_37, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_38': xǁPrinterǁ_temperature_fan_object_updated__mutmut_38, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_39': xǁPrinterǁ_temperature_fan_object_updated__mutmut_39, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_40': xǁPrinterǁ_temperature_fan_object_updated__mutmut_40, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_41': xǁPrinterǁ_temperature_fan_object_updated__mutmut_41, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_42': xǁPrinterǁ_temperature_fan_object_updated__mutmut_42, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_43': xǁPrinterǁ_temperature_fan_object_updated__mutmut_43, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_44': xǁPrinterǁ_temperature_fan_object_updated__mutmut_44, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_45': xǁPrinterǁ_temperature_fan_object_updated__mutmut_45, 
        'xǁPrinterǁ_temperature_fan_object_updated__mutmut_46': xǁPrinterǁ_temperature_fan_object_updated__mutmut_46
    }
    xǁPrinterǁ_temperature_fan_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_temperature_fan_object_updated'

    def _filament_switch_sensor_object_updated(
        self, values: dict, filament_switch_name: str
    ) -> None:
        args = [values, filament_switch_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_orig(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_1(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "XXfilament_detectedXX" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_2(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "FILAMENT_DETECTED" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_3(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" not in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_4(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                None,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_5(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                None,
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_6(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                None,
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_7(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_8(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_9(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_10(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "XXfilament_detectedXX",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_11(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "FILAMENT_DETECTED",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_12(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["XXfilament_detectedXX"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_13(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["FILAMENT_DETECTED"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_14(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(None)
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_15(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "XXenabledXX" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_16(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "ENABLED" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_17(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" not in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_18(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                None, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_19(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, None, values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_20(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", None
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_21(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_22(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_23(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_24(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "XXenabledXX", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_25(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "ENABLED", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_26(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["XXenabledXX"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_27(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["ENABLED"]
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})

    def xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_28(
        self, values: dict, filament_switch_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update({f"{filament_switch_name}": values})
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                filament_switch_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update(None)
    
    xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_1': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_1, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_2': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_2, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_3': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_3, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_4': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_4, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_5': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_5, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_6': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_6, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_7': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_7, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_8': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_8, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_9': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_9, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_10': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_10, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_11': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_11, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_12': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_12, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_13': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_13, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_14': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_14, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_15': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_15, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_16': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_16, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_17': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_17, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_18': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_18, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_19': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_19, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_20': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_20, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_21': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_21, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_22': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_22, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_23': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_23, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_24': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_24, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_25': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_25, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_26': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_26, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_27': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_27, 
        'xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_28': xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_28
    }
    xǁPrinterǁ_filament_switch_sensor_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_filament_switch_sensor_object_updated'

    def _filament_motion_sensor_object_updated(
        self, values: dict, filament_motion_name: str
    ) -> None:
        args = [values, filament_motion_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_orig(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_1(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "XXfilament_detectedXX" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_2(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "FILAMENT_DETECTED" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_3(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" not in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_4(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                None,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_5(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                None,
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_6(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                None,
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_7(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_8(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_9(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_10(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "XXfilament_detectedXX",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_11(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "FILAMENT_DETECTED",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_12(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["XXfilament_detectedXX"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_13(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["FILAMENT_DETECTED"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_14(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                None
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_15(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["XXfilament_detectedXX"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_16(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["FILAMENT_DETECTED"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_17(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "XXenabledXX" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_18(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "ENABLED" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_19(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" not in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_20(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                None, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_21(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, None, values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_22(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", None
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_23(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                "enabled", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_24(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_25(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_26(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "XXenabledXX", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_27(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "ENABLED", values["enabled"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_28(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["XXenabledXX"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_29(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["ENABLED"]
            )
            self.available_filament_sensors.update({f"{filament_motion_name}": values})

    def xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_30(
        self, values: dict, filament_motion_name: str
    ) -> None:
        if "filament_detected" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name,
                "filament_detected",
                values["filament_detected"],
            )
            self.available_filament_sensors.update(
                {f"{filament_motion_name}": values["filament_detected"]}
            )

        if "enabled" in values.keys():
            self.filament_motion_sensor_update.emit(
                filament_motion_name, "enabled", values["enabled"]
            )
            self.available_filament_sensors.update(None)
    
    xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_1': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_1, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_2': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_2, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_3': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_3, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_4': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_4, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_5': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_5, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_6': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_6, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_7': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_7, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_8': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_8, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_9': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_9, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_10': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_10, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_11': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_11, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_12': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_12, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_13': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_13, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_14': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_14, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_15': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_15, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_16': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_16, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_17': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_17, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_18': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_18, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_19': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_19, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_20': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_20, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_21': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_21, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_22': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_22, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_23': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_23, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_24': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_24, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_25': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_25, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_26': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_26, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_27': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_27, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_28': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_28, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_29': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_29, 
        'xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_30': xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_30
    }
    xǁPrinterǁ_filament_motion_sensor_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_filament_motion_sensor_object_updated'

    def _cutter_sensor_object_updated(self, values: dict, cutter_name: str) -> None:
        args = [values, cutter_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_orig(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_1(self, values: dict, cutter_name: str) -> None:
        if "XXfilament_detectedXX" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_2(self, values: dict, cutter_name: str) -> None:
        if "FILAMENT_DETECTED" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_3(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" not in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_4(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                None, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_5(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, None, values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_6(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", None
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_7(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_8(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_9(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_10(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "XXfilament_detectedXX", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_11(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "FILAMENT_DETECTED", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_12(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["XXfilament_detectedXX"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_13(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["FILAMENT_DETECTED"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_14(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "XXenabledXX" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_15(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "ENABLED" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_16(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" not in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_17(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                None, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_18(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, None, values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_19(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", None
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_20(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                "enabled", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_21(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_22(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_23(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "XXenabledXX", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_24(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "ENABLED", values["enabled"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_25(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["XXenabledXX"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_26(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["ENABLED"]
            )

        self.available_filament_sensors.update({f"{cutter_name}": values})

    def xǁPrinterǁ_cutter_sensor_object_updated__mutmut_27(self, values: dict, cutter_name: str) -> None:
        if "filament_detected" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "filament_detected", values["filament_detected"]
            )
        if "enabled" in values.keys():
            self.filament_switch_sensor_update.emit(
                cutter_name, "enabled", values["enabled"]
            )

        self.available_filament_sensors.update(None)
    
    xǁPrinterǁ_cutter_sensor_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_1': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_1, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_2': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_2, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_3': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_3, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_4': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_4, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_5': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_5, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_6': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_6, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_7': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_7, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_8': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_8, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_9': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_9, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_10': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_10, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_11': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_11, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_12': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_12, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_13': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_13, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_14': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_14, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_15': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_15, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_16': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_16, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_17': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_17, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_18': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_18, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_19': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_19, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_20': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_20, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_21': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_21, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_22': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_22, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_23': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_23, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_24': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_24, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_25': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_25, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_26': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_26, 
        'xǁPrinterǁ_cutter_sensor_object_updated__mutmut_27': xǁPrinterǁ_cutter_sensor_object_updated__mutmut_27
    }
    xǁPrinterǁ_cutter_sensor_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_cutter_sensor_object_updated'

    def _output_pin_object_updated(self, values: dict, output_pin_name: str) -> None:
        args = [values, output_pin_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_output_pin_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_output_pin_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_output_pin_object_updated__mutmut_orig(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_1(self, values: dict, output_pin_name: str) -> None:
        if "XXvalueXX" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_2(self, values: dict, output_pin_name: str) -> None:
        if "VALUE" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_3(self, values: dict, output_pin_name: str) -> None:
        if "value" not in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_4(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(None, "value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_5(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, None, values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_6(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", None)

    def xǁPrinterǁ_output_pin_object_updated__mutmut_7(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit("value", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_8(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_9(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", )

    def xǁPrinterǁ_output_pin_object_updated__mutmut_10(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "XXvalueXX", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_11(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "VALUE", values["value"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_12(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["XXvalueXX"])

    def xǁPrinterǁ_output_pin_object_updated__mutmut_13(self, values: dict, output_pin_name: str) -> None:
        if "value" in values.keys():
            self.output_pin_update.emit(output_pin_name, "value", values["VALUE"])
    
    xǁPrinterǁ_output_pin_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_output_pin_object_updated__mutmut_1': xǁPrinterǁ_output_pin_object_updated__mutmut_1, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_2': xǁPrinterǁ_output_pin_object_updated__mutmut_2, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_3': xǁPrinterǁ_output_pin_object_updated__mutmut_3, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_4': xǁPrinterǁ_output_pin_object_updated__mutmut_4, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_5': xǁPrinterǁ_output_pin_object_updated__mutmut_5, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_6': xǁPrinterǁ_output_pin_object_updated__mutmut_6, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_7': xǁPrinterǁ_output_pin_object_updated__mutmut_7, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_8': xǁPrinterǁ_output_pin_object_updated__mutmut_8, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_9': xǁPrinterǁ_output_pin_object_updated__mutmut_9, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_10': xǁPrinterǁ_output_pin_object_updated__mutmut_10, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_11': xǁPrinterǁ_output_pin_object_updated__mutmut_11, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_12': xǁPrinterǁ_output_pin_object_updated__mutmut_12, 
        'xǁPrinterǁ_output_pin_object_updated__mutmut_13': xǁPrinterǁ_output_pin_object_updated__mutmut_13
    }
    xǁPrinterǁ_output_pin_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_output_pin_object_updated'

    def _bed_mesh_object_updated(self, values: dict, name: str = "bed_mesh") -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_bed_mesh_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_bed_mesh_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_bed_mesh_object_updated__mutmut_orig(self, values: dict, name: str = "bed_mesh") -> None:
        # TODO
        pass

    def xǁPrinterǁ_bed_mesh_object_updated__mutmut_1(self, values: dict, name: str = "XXbed_meshXX") -> None:
        # TODO
        pass

    def xǁPrinterǁ_bed_mesh_object_updated__mutmut_2(self, values: dict, name: str = "BED_MESH") -> None:
        # TODO
        pass
    
    xǁPrinterǁ_bed_mesh_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_bed_mesh_object_updated__mutmut_1': xǁPrinterǁ_bed_mesh_object_updated__mutmut_1, 
        'xǁPrinterǁ_bed_mesh_object_updated__mutmut_2': xǁPrinterǁ_bed_mesh_object_updated__mutmut_2
    }
    xǁPrinterǁ_bed_mesh_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_bed_mesh_object_updated'

    def _gcode_macro_object_updated(self, values: dict, gcode_macro_name: str) -> None:
        args = [values, gcode_macro_name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_gcode_macro_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_gcode_macro_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_gcode_macro_object_updated__mutmut_orig(self, values: dict, gcode_macro_name: str) -> None:
        # * values argument can come with many different types for this macro so handle them in another place

        self.gcode_macro_update.emit(gcode_macro_name, values)
        return

    def xǁPrinterǁ_gcode_macro_object_updated__mutmut_1(self, values: dict, gcode_macro_name: str) -> None:
        # * values argument can come with many different types for this macro so handle them in another place

        self.gcode_macro_update.emit(None, values)
        return

    def xǁPrinterǁ_gcode_macro_object_updated__mutmut_2(self, values: dict, gcode_macro_name: str) -> None:
        # * values argument can come with many different types for this macro so handle them in another place

        self.gcode_macro_update.emit(gcode_macro_name, None)
        return

    def xǁPrinterǁ_gcode_macro_object_updated__mutmut_3(self, values: dict, gcode_macro_name: str) -> None:
        # * values argument can come with many different types for this macro so handle them in another place

        self.gcode_macro_update.emit(values)
        return

    def xǁPrinterǁ_gcode_macro_object_updated__mutmut_4(self, values: dict, gcode_macro_name: str) -> None:
        # * values argument can come with many different types for this macro so handle them in another place

        self.gcode_macro_update.emit(gcode_macro_name, )
        return
    
    xǁPrinterǁ_gcode_macro_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_gcode_macro_object_updated__mutmut_1': xǁPrinterǁ_gcode_macro_object_updated__mutmut_1, 
        'xǁPrinterǁ_gcode_macro_object_updated__mutmut_2': xǁPrinterǁ_gcode_macro_object_updated__mutmut_2, 
        'xǁPrinterǁ_gcode_macro_object_updated__mutmut_3': xǁPrinterǁ_gcode_macro_object_updated__mutmut_3, 
        'xǁPrinterǁ_gcode_macro_object_updated__mutmut_4': xǁPrinterǁ_gcode_macro_object_updated__mutmut_4
    }
    xǁPrinterǁ_gcode_macro_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_gcode_macro_object_updated'

    def _configfile_object_updated(
        self, values: dict, name: str = "configfile"
    ) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_configfile_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_configfile_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_configfile_object_updated__mutmut_orig(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_1(
        self, values: dict, name: str = "XXconfigfileXX"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_2(
        self, values: dict, name: str = "CONFIGFILE"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_3(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(None)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_4(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "XXconfigXX" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_5(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "CONFIG" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_6(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" not in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_7(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(None)
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_8(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["XXconfigXX"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_9(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["CONFIG"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_10(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "XXsettingsXX" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_11(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "SETTINGS" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_12(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" not in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_13(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "XXsave_config_pendingXX" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_14(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "SAVE_CONFIG_PENDING" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_15(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" not in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_16(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "XXsave_config_pending_itemsXX" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_17(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "SAVE_CONFIG_PENDING_ITEMS" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_18(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" not in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_19(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "XXwarningsXX" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_20(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "WARNINGS" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_21(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" not in values.keys():
            # TODO
            ...

        self.configfile_update.emit(values)  # Signal config update

        return

    def xǁPrinterǁ_configfile_object_updated__mutmut_22(
        self, values: dict, name: str = "configfile"
    ) -> None:
        self.configfile.update(values)
        if "config" in values.keys():
            self.printer_config.emit(values["config"])
        if "settings" in values.keys():
            # TODO
            ...
        if "save_config_pending" in values.keys():
            self.save_config_pending.emit()
        if "save_config_pending_items" in values.keys():
            # TODO
            ...
        if "warnings" in values.keys():
            # TODO
            ...

        self.configfile_update.emit(None)  # Signal config update

        return
    
    xǁPrinterǁ_configfile_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_configfile_object_updated__mutmut_1': xǁPrinterǁ_configfile_object_updated__mutmut_1, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_2': xǁPrinterǁ_configfile_object_updated__mutmut_2, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_3': xǁPrinterǁ_configfile_object_updated__mutmut_3, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_4': xǁPrinterǁ_configfile_object_updated__mutmut_4, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_5': xǁPrinterǁ_configfile_object_updated__mutmut_5, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_6': xǁPrinterǁ_configfile_object_updated__mutmut_6, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_7': xǁPrinterǁ_configfile_object_updated__mutmut_7, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_8': xǁPrinterǁ_configfile_object_updated__mutmut_8, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_9': xǁPrinterǁ_configfile_object_updated__mutmut_9, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_10': xǁPrinterǁ_configfile_object_updated__mutmut_10, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_11': xǁPrinterǁ_configfile_object_updated__mutmut_11, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_12': xǁPrinterǁ_configfile_object_updated__mutmut_12, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_13': xǁPrinterǁ_configfile_object_updated__mutmut_13, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_14': xǁPrinterǁ_configfile_object_updated__mutmut_14, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_15': xǁPrinterǁ_configfile_object_updated__mutmut_15, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_16': xǁPrinterǁ_configfile_object_updated__mutmut_16, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_17': xǁPrinterǁ_configfile_object_updated__mutmut_17, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_18': xǁPrinterǁ_configfile_object_updated__mutmut_18, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_19': xǁPrinterǁ_configfile_object_updated__mutmut_19, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_20': xǁPrinterǁ_configfile_object_updated__mutmut_20, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_21': xǁPrinterǁ_configfile_object_updated__mutmut_21, 
        'xǁPrinterǁ_configfile_object_updated__mutmut_22': xǁPrinterǁ_configfile_object_updated__mutmut_22
    }
    xǁPrinterǁ_configfile_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_configfile_object_updated'

    def _gcode_object_updated(self, values: dict, name: str = "gcode_object") -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_gcode_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_gcode_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_gcode_object_updated__mutmut_orig(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_1(self, values: dict, name: str = "XXgcode_objectXX") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_2(self, values: dict, name: str = "GCODE_OBJECT") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_3(self, values: dict, name: str = "gcode_object") -> None:
        if values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_4(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get(None):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_5(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("XXcommandsXX"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_6(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("COMMANDS"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_7(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(None)  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_8(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get(None))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_9(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("XXcommandsXX"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_10(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("COMMANDS"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("commands"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_11(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(None)
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_12(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get(None))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_13(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("XXcommandsXX"))
        return

    def xǁPrinterǁ_gcode_object_updated__mutmut_14(self, values: dict, name: str = "gcode_object") -> None:
        if not values.get("commands"):
            return
        self.available_gcode_commands.update(values.get("commands"))  # type: ignore
        self.available_gcode_cmds.emit(values.get("COMMANDS"))
        return
    
    xǁPrinterǁ_gcode_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_gcode_object_updated__mutmut_1': xǁPrinterǁ_gcode_object_updated__mutmut_1, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_2': xǁPrinterǁ_gcode_object_updated__mutmut_2, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_3': xǁPrinterǁ_gcode_object_updated__mutmut_3, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_4': xǁPrinterǁ_gcode_object_updated__mutmut_4, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_5': xǁPrinterǁ_gcode_object_updated__mutmut_5, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_6': xǁPrinterǁ_gcode_object_updated__mutmut_6, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_7': xǁPrinterǁ_gcode_object_updated__mutmut_7, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_8': xǁPrinterǁ_gcode_object_updated__mutmut_8, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_9': xǁPrinterǁ_gcode_object_updated__mutmut_9, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_10': xǁPrinterǁ_gcode_object_updated__mutmut_10, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_11': xǁPrinterǁ_gcode_object_updated__mutmut_11, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_12': xǁPrinterǁ_gcode_object_updated__mutmut_12, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_13': xǁPrinterǁ_gcode_object_updated__mutmut_13, 
        'xǁPrinterǁ_gcode_object_updated__mutmut_14': xǁPrinterǁ_gcode_object_updated__mutmut_14
    }
    xǁPrinterǁ_gcode_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_gcode_object_updated'

    def _manual_probe_object_updated(self, values: dict, name: str) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_manual_probe_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_manual_probe_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_manual_probe_object_updated__mutmut_orig(self, values: dict, name: str) -> None:
        self.manual_probe_update[dict].emit(values)
        return

    def xǁPrinterǁ_manual_probe_object_updated__mutmut_1(self, values: dict, name: str) -> None:
        self.manual_probe_update[dict].emit(None)
        return
    
    xǁPrinterǁ_manual_probe_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_manual_probe_object_updated__mutmut_1': xǁPrinterǁ_manual_probe_object_updated__mutmut_1
    }
    xǁPrinterǁ_manual_probe_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_manual_probe_object_updated'

    def _probe_object_updated(self, values: dict, name: str) -> None:
        # TODO
        ...

    def _bltouch_object_updated(self, values: dict, name: str) -> None:
        # TODO:
        ...

    def _probe_eddy_current_object_updated(self, values: dict, name: str) -> None:
        # TODO
        pass

    def _axis_twist_compensation_object_updated(self, values: dict, name: str) -> None:
        # TODO
        ...

    def _temperature_probe_object_updated(self, values: dict, name: str) -> None:
        pass

    # TODO: testing needed here idk if does work
    def _unload_filament_object_updated(self, values: dict, name: str) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_unload_filament_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_unload_filament_object_updated__mutmut_mutants'), args, kwargs, self)

    # TODO: testing needed here idk if does work
    def xǁPrinterǁ_unload_filament_object_updated__mutmut_orig(self, values: dict, name: str) -> None:
        self.unload_filament_update[dict].emit(values)

    # TODO: testing needed here idk if does work
    def xǁPrinterǁ_unload_filament_object_updated__mutmut_1(self, values: dict, name: str) -> None:
        self.unload_filament_update[dict].emit(None)
    
    xǁPrinterǁ_unload_filament_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_unload_filament_object_updated__mutmut_1': xǁPrinterǁ_unload_filament_object_updated__mutmut_1
    }
    xǁPrinterǁ_unload_filament_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_unload_filament_object_updated'

    def _load_filament_object_updated(self, values: dict, name: str) -> None:
        args = [values, name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁPrinterǁ_load_filament_object_updated__mutmut_orig'), object.__getattribute__(self, 'xǁPrinterǁ_load_filament_object_updated__mutmut_mutants'), args, kwargs, self)

    def xǁPrinterǁ_load_filament_object_updated__mutmut_orig(self, values: dict, name: str) -> None:
        if "state" in values.keys():
            self.load_filament_update[dict].emit(values)

    def xǁPrinterǁ_load_filament_object_updated__mutmut_1(self, values: dict, name: str) -> None:
        if "XXstateXX" in values.keys():
            self.load_filament_update[dict].emit(values)

    def xǁPrinterǁ_load_filament_object_updated__mutmut_2(self, values: dict, name: str) -> None:
        if "STATE" in values.keys():
            self.load_filament_update[dict].emit(values)

    def xǁPrinterǁ_load_filament_object_updated__mutmut_3(self, values: dict, name: str) -> None:
        if "state" not in values.keys():
            self.load_filament_update[dict].emit(values)

    def xǁPrinterǁ_load_filament_object_updated__mutmut_4(self, values: dict, name: str) -> None:
        if "state" in values.keys():
            self.load_filament_update[dict].emit(None)
    
    xǁPrinterǁ_load_filament_object_updated__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁPrinterǁ_load_filament_object_updated__mutmut_1': xǁPrinterǁ_load_filament_object_updated__mutmut_1, 
        'xǁPrinterǁ_load_filament_object_updated__mutmut_2': xǁPrinterǁ_load_filament_object_updated__mutmut_2, 
        'xǁPrinterǁ_load_filament_object_updated__mutmut_3': xǁPrinterǁ_load_filament_object_updated__mutmut_3, 
        'xǁPrinterǁ_load_filament_object_updated__mutmut_4': xǁPrinterǁ_load_filament_object_updated__mutmut_4
    }
    xǁPrinterǁ_load_filament_object_updated__mutmut_orig.__name__ = 'xǁPrinterǁ_load_filament_object_updated'
