from __future__ import annotations
import re
import typing
from functools import partial
import logging

from helper_methods import normalize
from lib.moonrakerComm import MoonWebSocket
from lib.panels.widgets.numpadPage import CustomNumpad
from lib.panels.widgets.optionCardWidget import OptionCard
from lib.panels.widgets.popupDialogWidget import Popup
from lib.panels.widgets.printcorePage import SwapPrintcorePage
from lib.panels.widgets.probeHelperPage import ProbeHelper
from lib.panels.widgets.slider_selector_page import SliderPage
from lib.printer import Printer
from lib.ui.controlStackedWidget_ui import Ui_controlStackedWidget
from lib.utils.display_button import DisplayButton
from PyQt6 import QtCore, QtGui, QtWidgets


_logger = logging.getLogger(__name__)
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class ControlTab(QtWidgets.QStackedWidget):
    """Printer Control Stacked Widget"""

    request_back_button = QtCore.pyqtSignal(name="request-back-button")
    request_change_page = QtCore.pyqtSignal(int, int, name="request-change-page")
    request_numpad_signal = QtCore.pyqtSignal(
        int,
        str,
        str,
        "PyQt_PyObject",
        QtWidgets.QStackedWidget,
        name="request-numpad",
    )
    run_gcode_signal: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="run-gcode"
    )
    disable_popups: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        bool, name="disable-popups"
    )
    lock_ui: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        bool, name="lock-ui"
    )
    request_numpad: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        [str, int, "PyQt_PyObject"],
        [str, int, "PyQt_PyObject", int, int],
        name="request-numpad",
    )
    request_file_info: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="request-file-info"
    )
    call_load_panel = QtCore.pyqtSignal(bool, str, name="call-load-panel")
    toggle_conn_page = QtCore.pyqtSignal(bool, name="call-load-panel")
    tune_display_buttons: dict = {}
    card_options: dict = {}

    def __init__(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        args = [parent, ws, printer]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁ__init____mutmut_orig(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_1(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(None)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_2(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = None
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_3(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(None)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_4(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = None

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_5(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(None)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_6(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = None
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_7(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = None
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_8(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(None)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_9(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = None
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_10(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = None
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_11(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = True
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_12(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = None
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_13(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = None
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_14(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = None
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_15(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = None
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_16(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 11
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_17(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = None
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_18(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 3
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_19(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = None
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_20(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = "XXXX"
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_21(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = None
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_22(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 2.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_23(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = None
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_24(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 26.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_25(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = None
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_26(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(None)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_27(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(None)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_28(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(None)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_29(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(None)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_30(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(None)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_31(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(None)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_32(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = None
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_33(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(None)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_34(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(None)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_35(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = None
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_36(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(None)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_37(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(None)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_38(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(None)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_39(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            None
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_40(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(None, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_41(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, None)
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_42(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_43(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, )
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_44(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_45(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(None)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_46(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(None)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_47(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(None)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_48(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            None
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_49(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            None
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_50(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            None
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_51(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            None
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_52(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            None
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_53(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "XXPyQt_PyObjectXX"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_54(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "pyqt_pyobject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_55(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PYQT_PYOBJECT"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_56(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            None
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_57(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "XXPyQt_PyObjectXX"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_58(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "pyqt_pyobject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_59(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PYQT_PYOBJECT"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_60(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(None)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_61(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            None
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_62(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            None
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_63(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(None)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_64(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            None
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_65(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(None)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_66(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(None)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_67(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(None)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_68(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            None
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_69(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(None, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_70(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_71(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_72(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_73(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_74(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            None
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_75(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(None, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_76(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_77(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_78(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_79(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_80(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            None
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_81(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(None, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_82(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_83(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_84(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_85(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_86(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(None)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_87(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(None)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_88(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            None
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_89(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(None, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_90(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_91(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_92(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_93(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_94(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            None
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_95(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(None, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_96(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_97(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_98(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_99(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_100(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            None
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_101(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(None, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_102(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, None)
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_103(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_104(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, )
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_105(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(None))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_106(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(None)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_107(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            None
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_108(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(None, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_109(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_110(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial("G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_111(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_112(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXG28\nM400XX")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_113(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "g28\nm400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_114(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            None
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_115(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(None, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_116(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_117(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial("M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_118(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_119(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXM84\nM400XX")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_120(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "m84\nm400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_121(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(None)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_122(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            None
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_123(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_124(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=None,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_125(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=None,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_126(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_127(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_128(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_129(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=11,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_130(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            None
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_131(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_132(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=None,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_133(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=None,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_134(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_135(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_136(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_137(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=51,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_138(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            None
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_139(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_140(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=None,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_141(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=None,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_142(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_143(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_144(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_145(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=101,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_146(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            None
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_147(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_148(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=None,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_149(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=None,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_150(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_151(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_152(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_153(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=3,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_154(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            None
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_155(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_156(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=None,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_157(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=None,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_158(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_159(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_160(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_161(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=6,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_162(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            None
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_163(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                None,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_164(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=None,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_165(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=None,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_166(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_167(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_168(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_169(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=11,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_170(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            None
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_171(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(None, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_172(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=None)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_173(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_174(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, )
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_175(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=2.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_176(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            None
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_177(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(None, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_178(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=None)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_179(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_180(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, )
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_181(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=11.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_182(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            None
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_183(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(None, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_184(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=None)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_185(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_186(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, )
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_187(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=101.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_188(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            None
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_189(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(None, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_190(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=None)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_191(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_192(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, )
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_193(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=26.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_194(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            None
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_195(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(None, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_196(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=None)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_197(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_198(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, )
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_199(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=51.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_200(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            None
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_201(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(None, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_202(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=None)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_203(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_204(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, )
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_205(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=101.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_206(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            None
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_207(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(None, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_208(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, None)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_209(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_210(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, )
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_211(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_212(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            None
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_213(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(None, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_214(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, None)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_215(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_216(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, )
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_217(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_218(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(None)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_219(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            None
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_220(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(None, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_221(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_222(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial("G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_223(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_224(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXG28 X\nM400XX")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_225(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "g28 x\nm400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_226(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            None
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_227(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(None, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_228(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_229(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial("G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_230(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_231(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXG28 Y\nM400XX")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_232(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "g28 y\nm400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_233(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            None
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_234(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(None, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_235(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_236(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial("G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_237(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_238(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXG28 Z\nM400XX")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_239(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "g28 z\nm400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_240(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            None
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_241(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(None, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_242(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, None)
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_243(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial("G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_244(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, )
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_245(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "XXG28\nM400XX")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_246(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "g28\nm400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_247(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(None)
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_248(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(None, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_249(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, None))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_250(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial("Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_251(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, ))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_252(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "XXYXX"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_253(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_254(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(None)
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_255(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(None, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_256(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, None))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_257(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial("Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_258(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, ))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_259(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "XXY-XX"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_260(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_261(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(None)
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_262(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(None, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_263(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, None))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_264(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial("X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_265(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, ))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_266(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "XXXXX"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_267(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "x"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_268(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(None)
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_269(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(None, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_270(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, None))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_271(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial("X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_272(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, ))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_273(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "XXX-XX"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_274(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "x-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_275(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            None  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_276(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(None, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_277(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, None)  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_278(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial("Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_279(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, )  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_280(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "XXZ-XX")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_281(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_282(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            None  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_283(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(None, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_284(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, None)  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_285(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial("Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_286(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, )  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_287(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "XXZXX")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_288(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_289(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(None)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_290(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(None)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_291(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(None)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_292(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = None
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_293(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(None)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_294(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(None)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_295(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(None)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_296(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            None
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_297(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "XXPyQt_PyObjectXX", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_298(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "pyqt_pyobject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_299(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PYQT_PYOBJECT", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_300(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            None
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_301(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: None
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_302(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                None
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_303(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "XXSET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0XX"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_304(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "set_heater_temperature heater=heater_bed target=0\n\
                set_heater_temperature heater=extruder target=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_305(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=HEATER_BED TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=EXTRUDER TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_306(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = None

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_307(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "XXfan_cageXX": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_308(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "FAN_CAGE": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_309(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(None),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_310(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap("XX:/fan_related/media/btn_icons/fan_cage.svgXX"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_311(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/FAN_RELATED/MEDIA/BTN_ICONS/FAN_CAGE.SVG"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_312(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "XXblowerXX": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_313(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "BLOWER": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_314(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(None),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_315(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap("XX:/fan_related/media/btn_icons/blower.svgXX"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_316(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/FAN_RELATED/MEDIA/BTN_ICONS/BLOWER.SVG"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_317(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "XXfanXX": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_318(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "FAN": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_319(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(None),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_320(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap("XX:/fan_related/media/btn_icons/fan.svgXX"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_321(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/FAN_RELATED/MEDIA/BTN_ICONS/FAN.SVG"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_322(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(None)

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_323(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: None)

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_324(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(None)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_325(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(None)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_326(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(None)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_327(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(None)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_328(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(None)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_329(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(None)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_330(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(None)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_331(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(None)

        self.printer.printer_config.connect(self.on_printer_config)

    def xǁControlTabǁ__init____mutmut_332(
        self,
        parent: QtWidgets.QWidget,
        ws: MoonWebSocket,
        printer: Printer,
        /,
    ) -> None:
        super().__init__(parent)
        self.panel = Ui_controlStackedWidget()
        self.panel.setupUi(self)

        self.popup = Popup(self)

        self.ws: MoonWebSocket = ws
        self.printer: Printer = printer
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.timers = []
        self.ztilt_state = False
        self.extruder_info: dict = {}
        self.bed_info: dict = {}
        self.toolhead_info: dict = {}
        self.extrude_length: int = 10
        self.extrude_feedrate: int = 2
        self.extrude_page_message: str = ""
        self.move_length: float = 1.0
        self.move_speed: float = 25.0
        self.probe_helper_page = ProbeHelper(self)
        self.probe_helper_page.toggle_conn_page.connect(self.toggle_conn_page)
        self.probe_helper_page.disable_popups.connect(self.disable_popups)
        self.probe_helper_page.lock_ui.connect(self.lock_ui)
        self.addWidget(self.probe_helper_page)
        self.probe_helper_page.call_load_panel.connect(self.call_load_panel)
        self.printcores_page = SwapPrintcorePage(self)
        self.addWidget(self.printcores_page)

        self.sliderPage = SliderPage(self)
        self.addWidget(self.sliderPage)
        self.sliderPage.request_back.connect(self.back_button)

        self.probe_helper_page.request_page_view.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.probe_helper_page.query_printer_object.connect(self.ws.api.object_query)
        self.probe_helper_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.probe_helper_page.request_back.connect(self.back_button)
        self.printer.print_stats_update[str, str].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, dict].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.print_stats_update[str, float].connect(
            self.probe_helper_page.on_print_stats_update
        )
        self.printer.available_gcode_cmds.connect(
            self.probe_helper_page.on_available_gcode_cmds
        )
        self.probe_helper_page.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.probe_helper_page.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.extruder_update.connect(self.probe_helper_page.on_extruder_update)
        self.printer.gcode_move_update.connect(
            self.probe_helper_page.on_gcode_move_update
        )
        self.printer.manual_probe_update.connect(
            self.probe_helper_page.on_manual_probe_update
        )
        self.printer.printer_config.connect(self.probe_helper_page.on_printer_config)
        self.printer.gcode_response.connect(
            self.probe_helper_page.handle_gcode_response
        )
        self.printer.toolhead_update[str, list].connect(self.on_toolhead_update)
        self.printer.extruder_update.connect(self.on_extruder_update)
        self.printer.heater_bed_update.connect(self.on_heater_bed_update)
        self.panel.cp_motion_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.motion_page))
        )
        self.panel.cp_temperature_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.temperature_page))
        )
        self.panel.cp_fans_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.fans_page))
        )
        self.panel.fans_back_btn.clicked.connect(self.back_button)
        self.panel.cp_switch_print_core_btn.clicked.connect(self.show_swapcore)
        self.panel.cp_nozzles_calibration_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.probe_helper_page))
        )
        self.panel.motion_extrude_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.extrude_page))
        )
        self.panel.motion_move_axis_btn.clicked.connect(
            partial(self.change_page, self.indexOf(self.panel.move_axis_page))
        )
        self.panel.mp_back_btn.clicked.connect(self.back_button)
        self.panel.motion_auto_home_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.motion_disable_steppers_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "M84\nM400")
        )
        self.panel.exp_back_btn.clicked.connect(self.back_button)
        self.panel.extrude_select_length_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_10_btn,
                value=10,
            )
        )
        self.panel.extrude_select_length_50_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_50_btn,
                value=50,
            )
        )
        self.panel.extrude_select_length_100_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_length,
                caller=self.panel.extrude_select_length_100_btn,
                value=100,
            )
        )
        self.panel.extrude_select_feedrate_2_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_2_btn,
                value=2,
            )
        )
        self.panel.extrude_select_feedrate_5_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_5_btn,
                value=5,
            )
        )
        self.panel.extrude_select_feedrate_10_btn.toggled.connect(
            partial(
                self.handle_toggle_extrude_feedrate,
                caller=self.panel.extrude_select_feedrate_10_btn,
                value=10,
            )
        )
        self.panel.mva_select_length_1_btn.toggled.connect(
            partial(self.handle_select_move_length, value=1.0)
        )
        self.panel.mva_select_length_10_btn.toggled.connect(
            partial(self.handle_select_move_length, value=10.0)
        )
        self.panel.mva_select_length_100_btn.toggled.connect(
            partial(self.handle_select_move_length, value=100.0)
        )
        self.panel.mva_select_speed_25_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=25.0)
        )
        self.panel.mva_select_speed_50_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=50.0)
        )
        self.panel.mva_select_speed_100_btn.toggled.connect(
            partial(self.handle_select_move_speed, value=100.0)
        )
        self.panel.exp_extrude_btn.clicked.connect(
            partial(self.handle_extrusion, True)
        )  # True for extrusion
        self.panel.exp_unextrude_btn.clicked.connect(
            partial(self.handle_extrusion, False)
        )  # False for retraction
        # Move Axis
        self.panel.mva_back_btn.clicked.connect(self.back_button)
        self.panel.mva_home_x_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 X\nM400")
        )
        self.panel.mva_home_y_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Y\nM400")
        )
        self.panel.mva_home_z_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28 Z\nM400")
        )
        self.panel.mva_home_all_btn.clicked.connect(
            partial(self.run_gcode_signal.emit, "G28\nM400")
        )
        self.panel.mva_up_btn.clicked.connect(partial(self.handle_move_axis, "Y"))
        self.panel.mva_down_btn.clicked.connect(partial(self.handle_move_axis, "Y-"))
        self.panel.mva_right_btn.clicked.connect(partial(self.handle_move_axis, "X"))
        self.panel.mva_left_btn.clicked.connect(partial(self.handle_move_axis, "X-"))
        self.panel.mva_z_up.clicked.connect(
            partial(self.handle_move_axis, "Z-")  # Move nozzle closer to bed
        )
        self.panel.mva_z_down.clicked.connect(
            partial(self.handle_move_axis, "Z")  # Move nozzle away from bed
        )
        self.panel.temp_back_button.clicked.connect(self.back_button)
        self.panel.printer_settings_back_btn.clicked.connect(self.back_button)
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        # @ object temperature change clicked
        self.numpadPage = CustomNumpad(self)
        self.numpadPage.request_back.connect(self.request_back_button)
        self.addWidget(self.numpadPage)

        self.request_numpad[str, int, "PyQt_PyObject", int, int].connect(
            self.on_numpad_request
        )

        self.panel.cooldown_btn.clicked.connect(
            lambda: self.run_gcode_signal.emit(
                "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=0\n\
                SET_HEATER_TEMPERATURE HEATER=extruder TARGET=0"
            )
        )

        self.path = {
            "fan_cage": QtGui.QPixmap(":/fan_related/media/btn_icons/fan_cage.svg"),
            "blower": QtGui.QPixmap(":/fan_related/media/btn_icons/blower.svg"),
            "fan": QtGui.QPixmap(":/fan_related/media/btn_icons/fan.svg"),
        }

        self.panel.cp_z_tilt_btn.clicked.connect(lambda: self.handle_ztilt())

        self.printcores_page.pc_accept.clicked.connect(self.handle_swapcore)

        self.ws.klippy_state_signal.connect(self.on_klippy_status)
        self.ws.klippy_state_signal.connect(self.probe_helper_page.on_klippy_status)
        self.printer.on_printcore_update.connect(self.handle_printcoreupdate)
        self.printer.gcode_response.connect(self._handle_gcode_response)
        self.printer.z_tilt_update.connect(self._handle_z_tilt_object_update)
        # self.panel.cp_printer_settings_btn.hide()
        self.panel.temperature_cooldown_btn.hide()
        self.panel.cooldown_btn.hide()
        self.panel.cp_switch_print_core_btn.hide()

        self.printer.fan_update[str, str, float].connect(self.on_fan_object_update)
        self.printer.fan_update[str, str, int].connect(self.on_fan_object_update)

        self.printer.printer_config.connect(None)
    
    xǁControlTabǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁ__init____mutmut_1': xǁControlTabǁ__init____mutmut_1, 
        'xǁControlTabǁ__init____mutmut_2': xǁControlTabǁ__init____mutmut_2, 
        'xǁControlTabǁ__init____mutmut_3': xǁControlTabǁ__init____mutmut_3, 
        'xǁControlTabǁ__init____mutmut_4': xǁControlTabǁ__init____mutmut_4, 
        'xǁControlTabǁ__init____mutmut_5': xǁControlTabǁ__init____mutmut_5, 
        'xǁControlTabǁ__init____mutmut_6': xǁControlTabǁ__init____mutmut_6, 
        'xǁControlTabǁ__init____mutmut_7': xǁControlTabǁ__init____mutmut_7, 
        'xǁControlTabǁ__init____mutmut_8': xǁControlTabǁ__init____mutmut_8, 
        'xǁControlTabǁ__init____mutmut_319': xǁControlTabǁ__init____mutmut_319, 
        'xǁControlTabǁ__init____mutmut_320': xǁControlTabǁ__init____mutmut_320, 
        'xǁControlTabǁ__init____mutmut_321': xǁControlTabǁ__init____mutmut_321, 
        'xǁControlTabǁ__init____mutmut_322': xǁControlTabǁ__init____mutmut_322, 
        'xǁControlTabǁ__init____mutmut_323': xǁControlTabǁ__init____mutmut_323, 
        'xǁControlTabǁ__init____mutmut_324': xǁControlTabǁ__init____mutmut_324, 
        'xǁControlTabǁ__init____mutmut_325': xǁControlTabǁ__init____mutmut_325, 
        'xǁControlTabǁ__init____mutmut_326': xǁControlTabǁ__init____mutmut_326, 
        'xǁControlTabǁ__init____mutmut_327': xǁControlTabǁ__init____mutmut_327, 
        'xǁControlTabǁ__init____mutmut_328': xǁControlTabǁ__init____mutmut_328, 
        'xǁControlTabǁ__init____mutmut_329': xǁControlTabǁ__init____mutmut_329, 
        'xǁControlTabǁ__init____mutmut_330': xǁControlTabǁ__init____mutmut_330, 
        'xǁControlTabǁ__init____mutmut_331': xǁControlTabǁ__init____mutmut_331, 
        'xǁControlTabǁ__init____mutmut_332': xǁControlTabǁ__init____mutmut_332
    }
    xǁControlTabǁ__init____mutmut_orig.__name__ = 'xǁControlTabǁ__init__'

    def _handle_z_tilt_object_update(self, value, state):
        args = [value, state]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_orig(self, value, state):
        if state:
            self.call_load_panel.emit(False, "")

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_1(self, value, state):
        if state:
            self.call_load_panel.emit(None, "")

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_2(self, value, state):
        if state:
            self.call_load_panel.emit(False, None)

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_3(self, value, state):
        if state:
            self.call_load_panel.emit("")

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_4(self, value, state):
        if state:
            self.call_load_panel.emit(False, )

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_5(self, value, state):
        if state:
            self.call_load_panel.emit(True, "")

    def xǁControlTabǁ_handle_z_tilt_object_update__mutmut_6(self, value, state):
        if state:
            self.call_load_panel.emit(False, "XXXX")
    
    xǁControlTabǁ_handle_z_tilt_object_update__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_1': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_1, 
        'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_2': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_2, 
        'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_3': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_3, 
        'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_4': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_4, 
        'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_5': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_5, 
        'xǁControlTabǁ_handle_z_tilt_object_update__mutmut_6': xǁControlTabǁ_handle_z_tilt_object_update__mutmut_6
    }
    xǁControlTabǁ_handle_z_tilt_object_update__mutmut_orig.__name__ = 'xǁControlTabǁ_handle_z_tilt_object_update'

    @QtCore.pyqtSlot(str, str, float, name="on_fan_update")
    @QtCore.pyqtSlot(str, str, int, name="on_fan_update")
    def on_fan_object_update(
        self, name: str, field: str, new_value: int | float
    ) -> None:
        """Slot that receives updates from fan objects.

        Args:
            name (str): Fan object name
            field (str): Field name
            new_value (int | float): New value for the field
        """
        if "speed" not in field:
            return

        fields = name.split()
        first_field = fields[0]
        second_field = fields[1] if len(fields) > 1 else None
        name = second_field.replace("_", " ") if second_field else name

        fan_card = self.tune_display_buttons.get(name)
        if fan_card is None and first_field in (
            "fan",
            "fan_generic",
        ):
            icon = self.path.get("fan")
            if second_field:
                second_field = second_field.lower()
                pattern_blower = r"(?:^|_)(?:blower|auxiliary)(?:_|$)"
                pattern_exhaust = r"(?:^|_)exhaust(?:_|$)"
                if re.search(pattern_blower, second_field):
                    icon = self.path.get("blower")
                elif re.search(pattern_exhaust, second_field):
                    icon = self.path.get("fan_cage")

            card = OptionCard(self, name, str(name), icon)  # type: ignore
            card.setObjectName(str(name))

            # Add card to layout and record reference
            self.card_options[name] = card
            self.panel.fans_content_layout.addWidget(card)

            # If the card doesn't have expected UI properties, discard it
            if not hasattr(card, "continue_clicked"):
                del card
                self.card_options.pop(name, None)
                return

            card.setMode(True)
            card.secondtext.setText(f"{new_value}%")
            card.continue_clicked.connect(
                lambda: self.on_slidePage_request(
                    str(name),
                    card.secondtext.text().replace("%", ""),
                    self.on_slider_change,
                    0,
                    100,
                )
            )

            self.tune_display_buttons[name] = card
            self.update()
            fan_card = card

        if fan_card:
            value_percent = new_value * 100 if new_value <= 1 else new_value
            fan_card.secondtext.setText(f"{value_percent:.0f}%")

    @QtCore.pyqtSlot(str, int, "PyQt_PyObject", name="on_slidePage_request")
    @QtCore.pyqtSlot(str, int, "PyQt_PyObject", int, int, name="on_slidePage_request")
    def on_slidePage_request(
        self,
        name: str,
        current_value: int,
        callback,
        min_value: int = 0,
        max_value: int = 100,
    ) -> None:
        self.sliderPage.value_selected.connect(callback)
        self.sliderPage.set_name(name)
        self.sliderPage.set_slider_position(int(current_value))
        self.sliderPage.set_slider_minimum(min_value)
        self.sliderPage.set_slider_maximum(max_value)
        self.change_page(self.indexOf(self.sliderPage))

    @QtCore.pyqtSlot(str, int, name="on_slider_change")
    def on_slider_change(self, name: str, new_value: int) -> None:
        if "speed" in name.lower():
            self.speed_factor_override = new_value / 100
            self.run_gcode_signal.emit(f"M220 S{new_value}")
        if name.lower() == "fan":
            self.run_gcode_signal.emit(
                f"M106 S{int(round((normalize(float(new_value / 100), 0.0, 1.0, 0, 255))))}"
            )  # [0, 255] Range
        else:
            name = name.replace(" ", "_")
            self.run_gcode_signal.emit(
                f'SET_FAN_SPEED FAN="{name}" SPEED={float(new_value / 100.00)}'
            )  # [0.0, 1.0] Range

    def create_display_button(self, name: str) -> DisplayButton:
        args = [name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁcreate_display_button__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁcreate_display_button__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁcreate_display_button__mutmut_orig(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_1(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = None
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_2(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(None)
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_3(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(None))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_4(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name - "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_5(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "XX_displayXX"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_6(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_DISPLAY"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_7(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(None)
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_8(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(None, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_9(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, None))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_10(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_11(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, ))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_12(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(151, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_13(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 51))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_14(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(None)
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_15(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(None, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_16(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, None))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_17(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_18(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, ))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_19(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(151, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_20(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 81))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_21(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = None
        font.setPointSize(16)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_22(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(None)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_23(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(17)
        display_button.setFont(font)
        return display_button

    def xǁControlTabǁcreate_display_button__mutmut_24(self, name: str) -> DisplayButton:
        """Create and return a DisplayButton

        Args:
            name (str): Name for the display button

        Returns:
            DisplayButton: The created DisplayButton object
        """
        display_button = DisplayButton()
        display_button.setObjectName(str(name + "_display"))
        display_button.setMinimumSize(QtCore.QSize(150, 50))
        display_button.setMaximumSize(QtCore.QSize(150, 80))
        font = QtGui.QFont()
        font.setPointSize(16)
        display_button.setFont(None)
        return display_button
    
    xǁControlTabǁcreate_display_button__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁcreate_display_button__mutmut_1': xǁControlTabǁcreate_display_button__mutmut_1, 
        'xǁControlTabǁcreate_display_button__mutmut_2': xǁControlTabǁcreate_display_button__mutmut_2, 
        'xǁControlTabǁcreate_display_button__mutmut_3': xǁControlTabǁcreate_display_button__mutmut_3, 
        'xǁControlTabǁcreate_display_button__mutmut_4': xǁControlTabǁcreate_display_button__mutmut_4, 
        'xǁControlTabǁcreate_display_button__mutmut_5': xǁControlTabǁcreate_display_button__mutmut_5, 
        'xǁControlTabǁcreate_display_button__mutmut_6': xǁControlTabǁcreate_display_button__mutmut_6, 
        'xǁControlTabǁcreate_display_button__mutmut_7': xǁControlTabǁcreate_display_button__mutmut_7, 
        'xǁControlTabǁcreate_display_button__mutmut_8': xǁControlTabǁcreate_display_button__mutmut_8, 
        'xǁControlTabǁcreate_display_button__mutmut_9': xǁControlTabǁcreate_display_button__mutmut_9, 
        'xǁControlTabǁcreate_display_button__mutmut_10': xǁControlTabǁcreate_display_button__mutmut_10, 
        'xǁControlTabǁcreate_display_button__mutmut_11': xǁControlTabǁcreate_display_button__mutmut_11, 
        'xǁControlTabǁcreate_display_button__mutmut_12': xǁControlTabǁcreate_display_button__mutmut_12, 
        'xǁControlTabǁcreate_display_button__mutmut_13': xǁControlTabǁcreate_display_button__mutmut_13, 
        'xǁControlTabǁcreate_display_button__mutmut_14': xǁControlTabǁcreate_display_button__mutmut_14, 
        'xǁControlTabǁcreate_display_button__mutmut_15': xǁControlTabǁcreate_display_button__mutmut_15, 
        'xǁControlTabǁcreate_display_button__mutmut_16': xǁControlTabǁcreate_display_button__mutmut_16, 
        'xǁControlTabǁcreate_display_button__mutmut_17': xǁControlTabǁcreate_display_button__mutmut_17, 
        'xǁControlTabǁcreate_display_button__mutmut_18': xǁControlTabǁcreate_display_button__mutmut_18, 
        'xǁControlTabǁcreate_display_button__mutmut_19': xǁControlTabǁcreate_display_button__mutmut_19, 
        'xǁControlTabǁcreate_display_button__mutmut_20': xǁControlTabǁcreate_display_button__mutmut_20, 
        'xǁControlTabǁcreate_display_button__mutmut_21': xǁControlTabǁcreate_display_button__mutmut_21, 
        'xǁControlTabǁcreate_display_button__mutmut_22': xǁControlTabǁcreate_display_button__mutmut_22, 
        'xǁControlTabǁcreate_display_button__mutmut_23': xǁControlTabǁcreate_display_button__mutmut_23, 
        'xǁControlTabǁcreate_display_button__mutmut_24': xǁControlTabǁcreate_display_button__mutmut_24
    }
    xǁControlTabǁcreate_display_button__mutmut_orig.__name__ = 'xǁControlTabǁcreate_display_button'

    def handle_printcoreupdate(self, value: dict):
        args = [value]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁhandle_printcoreupdate__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁhandle_printcoreupdate__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁhandle_printcoreupdate__mutmut_orig(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_1(self, value: dict):
        if value["XXswappingXX"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_2(self, value: dict):
        if value["SWAPPING"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_3(self, value: dict):
        if value["swapping"] != "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_4(self, value: dict):
        if value["swapping"] == "XXidleXX":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_5(self, value: dict):
        if value["swapping"] == "IDLE":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_6(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["XXswappingXX"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_7(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["SWAPPING"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_8(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] != "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_9(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "XXin_posXX":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_10(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "IN_POS":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_11(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(None, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_12(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, None)
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_13(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit("")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_14(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, )
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_15(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(True, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_16(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "XXXX")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_17(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(None)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_18(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(False)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_19(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                None
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_20(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "XXPlease Insert Print Core \n \n Afterwards click continueXX"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_21(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "please insert print core \n \n afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_22(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "PLEASE INSERT PRINT CORE \n \n AFTERWARDS CLICK CONTINUE"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_23(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["XXswappingXX"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_24(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["SWAPPING"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_25(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] != "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_26(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "XXunloadingXX":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_27(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "UNLOADING":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_28(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(None, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_29(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, None)
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_30(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit("Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_31(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, )
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_32(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(False, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_33(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "XXUnloading print coreXX")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_34(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_35(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "UNLOADING PRINT CORE")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_36(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["XXswappingXX"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_37(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["SWAPPING"] == "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_38(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] != "cleaning":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_39(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "XXcleaningXX":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_40(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "CLEANING":
            self.call_load_panel.emit(True, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_41(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(None, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_42(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, None)

    def xǁControlTabǁhandle_printcoreupdate__mutmut_43(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit("Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_44(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, )

    def xǁControlTabǁhandle_printcoreupdate__mutmut_45(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(False, "Cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_46(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "XXCleaning print coreXX")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_47(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "cleaning print core")

    def xǁControlTabǁhandle_printcoreupdate__mutmut_48(self, value: dict):
        if value["swapping"] == "idle":
            return

        if value["swapping"] == "in_pos":
            self.call_load_panel.emit(False, "")
            self.printcores_page.show()
            self.disable_popups.emit(True)
            self.printcores_page.setText(
                "Please Insert Print Core \n \n Afterwards click continue"
            )
        if value["swapping"] == "unloading":
            self.call_load_panel.emit(True, "Unloading print core")
        if value["swapping"] == "cleaning":
            self.call_load_panel.emit(True, "CLEANING PRINT CORE")
    
    xǁControlTabǁhandle_printcoreupdate__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'x�
 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_43': xǁControlTabǁhandle_printcoreupdate__mutmut_43, 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_44': xǁControlTabǁhandle_printcoreupdate__mutmut_44, 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_45': xǁControlTabǁhandle_printcoreupdate__mutmut_45, 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_46': xǁControlTabǁhandle_printcoreupdate__mutmut_46, 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_47': xǁControlTabǁhandle_printcoreupdate__mutmut_47, 
        'xǁControlTabǁhandle_printcoreupdate__mutmut_48': xǁControlTabǁhandle_printcoreupdate__mutmut_48
    }
    xǁControlTabǁhandle_printcoreupdate__mutmut_orig.__name__ = 'xǁControlTabǁhandle_printcoreupdate'

    def _handle_gcode_response(self, messages: list):
        args = [messages]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁ_handle_gcode_response__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁ_handle_gcode_response__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁ_handle_gcode_response__mutmut_orig(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_1(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = None

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_2(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"XXRetries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)XX"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_3(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_4(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"RETRIES:\s*(\d+)/(\d+).*?RANGE:\s*([\d.]+)\s*TOLERANCE:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_5(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_6(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                break

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_7(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list or "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_8(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list or "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_9(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "XXRetries:XX" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_10(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_11(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "RETRIES:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_12(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" not in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_13(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "XXrange:XX" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_14(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "RANGE:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_15(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" not in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_16(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "XXtolerance:XX" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_17(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "TOLERANCE:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_18(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" not in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_19(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = None

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_20(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(None, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_21(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, None)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_22(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_23(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, )

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_24(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = None
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_25(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(None)
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_26(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(None))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_27(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(2))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_28(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = None
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_29(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(None)
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_30(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(None))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_31(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(3))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_32(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = None
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_33(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(None)
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_34(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(None))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_35(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(4))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_36(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = None
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_37(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(None)
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_38(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(None))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_39(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(5))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_40(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done != retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_41(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(None, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_42(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, None)
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_43(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit("")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_44(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, )
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_45(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(True, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_46(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "XXXX")
                        return
                    self.call_load_panel.emit(
                        True,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_47(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        None,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_48(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        None,
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_49(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )

    def xǁControlTabǁ_handle_gcode_response__mutmut_50(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        True,
                        )

    def xǁControlTabǁ_handle_gcode_response__mutmut_51(self, messages: list):
        """Handle gcode response for Z-tilt adjustment"""
        pattern = r"Retries:\s*(\d+)/(\d+).*?range:\s*([\d.]+)\s*tolerance:\s*([\d.]+)"

        for msg_list in messages:
            if not msg_list:
                continue

            if (
                "Retries:" in msg_list
                and "range:" in msg_list
                and "tolerance:" in msg_list
            ):
                match = re.search(pattern, msg_list)

                if match:
                    retries_done = int(match.group(1))
                    retries_total = int(match.group(2))
                    probed_range = float(match.group(3))
                    tolerance = float(match.group(4))
                    if retries_done == retries_total:
                        self.call_load_panel.emit(False, "")
                        return
                    self.call_load_panel.emit(
                        False,
                        f"Retries: {retries_done}/{retries_total} | Range: {probed_range:.6f} | Tolerance: {tolerance:.6f}",
                    )
    
    xǁControlTabǁ_handle_gcode_response__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁ_handle_gcode_response__mutmut_1': xǁControlTabǁ_handle_gcode_response__mutmut_1, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_2': xǁControlTabǁ_handle_gcode_response__mutmut_2, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_3': xǁControlTabǁ_handle_gcode_response__mutmut_3, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_4': xǁControlTabǁ_handle_gcode_response__mutmut_4, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_5': xǁControlTabǁ_handle_gcode_response__mutmut_5, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_6': xǁControlTabǁ_handle_gcode_response__mutmut_6, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_7': xǁControlTabǁ_handle_gcode_response__mutmut_7, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_8': xǁControlTabǁ_handle_gcode_response__mutmut_8, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_9': xǁControlTabǁ_handle_gcode_response__mutmut_9, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_10': xǁControlTabǁ_handle_gcode_response__mutmut_10, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_11': xǁControlTabǁ_handle_gcode_response__mutmut_11, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_12': xǁControlTabǁ_handle_gcode_response__mutmut_12, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_13': xǁControlTabǁ_handle_gcode_response__mutmut_13, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_14': xǁControlTabǁ_handle_gcode_response__mutmut_14, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_15': xǁControlTabǁ_handle_gcode_response__mutmut_15, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_16': xǁControlTabǁ_handle_gcode_response__mutmut_16, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_17': xǁControlTabǁ_handle_gcode_response__mutmut_17, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_18': xǁControlTabǁ_handle_gcode_response__mutmut_18, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_19': xǁControlTabǁ_handle_gcode_response__mutmut_19, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_20': xǁControlTabǁ_handle_gcode_response__mutmut_20, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_21': xǁControlTabǁ_handle_gcode_response__mutmut_21, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_22': xǁControlTabǁ_handle_gcode_response__mutmut_22, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_23': xǁControlTabǁ_handle_gcode_response__mutmut_23, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_24': xǁControlTabǁ_handle_gcode_response__mutmut_24, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_25': xǁControlTabǁ_handle_gcode_response__mutmut_25, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_26': xǁControlTabǁ_handle_gcode_response__mutmut_26, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_27': xǁControlTabǁ_handle_gcode_response__mutmut_27, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_28': xǁControlTabǁ_handle_gcode_response__mutmut_28, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_29': xǁControlTabǁ_handle_gcode_response__mutmut_29, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_30': xǁControlTabǁ_handle_gcode_response__mutmut_30, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_31': xǁControlTabǁ_handle_gcode_response__mutmut_31, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_32': xǁControlTabǁ_handle_gcode_response__mutmut_32, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_33': xǁControlTabǁ_handle_gcode_response__mutmut_33, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_34': xǁControlTabǁ_handle_gcode_response__mutmut_34, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_35': xǁControlTabǁ_handle_gcode_response__mutmut_35, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_36': xǁControlTabǁ_handle_gcode_response__mutmut_36, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_37': xǁControlTabǁ_handle_gcode_response__mutmut_37, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_38': xǁControlTabǁ_handle_gcode_response__mutmut_38, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_39': xǁControlTabǁ_handle_gcode_response__mutmut_39, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_40': xǁControlTabǁ_handle_gcode_response__mutmut_40, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_41': xǁControlTabǁ_handle_gcode_response__mutmut_41, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_42': xǁControlTabǁ_handle_gcode_response__mutmut_42, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_43': xǁControlTabǁ_handle_gcode_response__mutmut_43, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_44': xǁControlTabǁ_handle_gcode_response__mutmut_44, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_45': xǁControlTabǁ_handle_gcode_response__mutmut_45, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_46': xǁControlTabǁ_handle_gcode_response__mutmut_46, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_47': xǁControlTabǁ_handle_gcode_response__mutmut_47, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_48': xǁControlTabǁ_handle_gcode_response__mutmut_48, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_49': xǁControlTabǁ_handle_gcode_response__mutmut_49, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_50': xǁControlTabǁ_handle_gcode_response__mutmut_50, 
        'xǁControlTabǁ_handle_gcode_response__mutmut_51': xǁControlTabǁ_handle_gcode_response__mutmut_51
    }
    xǁControlTabǁ_handle_gcode_response__mutmut_orig.__name__ = 'xǁControlTabǁ_handle_gcode_response'

    @QtCore.pyqtSlot(dict, name="printer_config")
    def on_printer_config(self, config: dict) -> None:
        """Slot that receives the full printer configuration,

        Additionally, this method configures the signal connections
        between controllable heaters and numpad calls
        """
        try:
            self.panel.extruder_temp_display.clicked.disconnect()
            self.panel.bed_temp_display.clicked.disconnect()
        except Exception:
            _logger.debug("Signals were not connected")
        extruder = config.get("extruder", None) or {}
        bed = config.get("heater_bed", None) or {}
        e_min_temp = extruder.get("min_temp", 0)
        e_max_temp = extruder.get("max_temp", 300)
        b_max_temp = bed.get("max_temp", 100)
        b_min_temp = bed.get("min_temp", 0)
        # Configure numpads
        self.panel.extruder_temp_display.clicked.connect(
            lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(
                "Extruder Temperature",
                int(round(float(self.panel.extruder_temp_display.secondary_text))),
                self.on_numpad_change,
                int(e_min_temp),
                int(e_max_temp),
            )
        )
        self.panel.bed_temp_display.clicked.connect(
            lambda: self.request_numpad[str, int, "PyQt_PyObject", int, int].emit(
                "Bed Temperature",
                int(round(float(self.panel.bed_temp_display.secondary_text))),
                self.on_numpad_change,
                int(b_min_temp),
                int(b_max_temp),
            )
        )

    def handle_ztilt(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁhandle_ztilt__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁhandle_ztilt__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁhandle_ztilt__mutmut_orig(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_1(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(None, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_2(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, None)
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_3(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit("Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_4(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, )
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_5(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(False, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_6(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "XXPlease wait, performing Z-axis calibration.XX")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_7(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "please wait, performing z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_8(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "PLEASE WAIT, PERFORMING Z-AXIS CALIBRATION.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_9(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit(None)
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_10(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("XXG28\nM400XX")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_11(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("g28\nm400")
        self.run_gcode_signal.emit("Z_TILT_ADJUST")

    def xǁControlTabǁhandle_ztilt__mutmut_12(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit(None)

    def xǁControlTabǁhandle_ztilt__mutmut_13(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("XXZ_TILT_ADJUSTXX")

    def xǁControlTabǁhandle_ztilt__mutmut_14(self):
        """Handle Z-Tilt Adjustment"""
        self.call_load_panel.emit(True, "Please wait, performing Z-axis calibration.")
        self.run_gcode_signal.emit("G28\nM400")
        self.run_gcode_signal.emit("z_tilt_adjust")
    
    xǁControlTabǁhandle_ztilt__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁhandle_ztilt__mutmut_1': xǁControlTabǁhandle_ztilt__mutmut_1, 
        'xǁControlTabǁhandle_ztilt__mutmut_2': xǁControlTabǁhandle_ztilt__mutmut_2, 
        'xǁControlTabǁhandle_ztilt__mutmut_3': xǁControlTabǁhandle_ztilt__mutmut_3, 
        'xǁControlTabǁhandle_ztilt__mutmut_4': xǁControlTabǁhandle_ztilt__mutmut_4, 
        'xǁControlTabǁhandle_ztilt__mutmut_5': xǁControlTabǁhandle_ztilt__mutmut_5, 
        'xǁControlTabǁhandle_ztilt__mutmut_6': xǁControlTabǁhandle_ztilt__mutmut_6, 
        'xǁControlTabǁhandle_ztilt__mutmut_7': xǁControlTabǁhandle_ztilt__mutmut_7, 
        'xǁControlTabǁhandle_ztilt__mutmut_8': xǁControlTabǁhandle_ztilt__mutmut_8, 
        'xǁControlTabǁhandle_ztilt__mutmut_9': xǁControlTabǁhandle_ztilt__mutmut_9, 
        'xǁControlTabǁhandle_ztilt__mutmut_10': xǁControlTabǁhandle_ztilt__mutmut_10, 
        'xǁControlTabǁhandle_ztilt__mutmut_11': xǁControlTabǁhandle_ztilt__mutmut_11, 
        'xǁControlTabǁhandle_ztilt__mutmut_12': xǁControlTabǁhandle_ztilt__mutmut_12, 
        'xǁControlTabǁhandle_ztilt__mutmut_13': xǁControlTabǁhandle_ztilt__mutmut_13, 
        'xǁControlTabǁhandle_ztilt__mutmut_14': xǁControlTabǁhandle_ztilt__mutmut_14
    }
    xǁControlTabǁhandle_ztilt__mutmut_orig.__name__ = 'xǁControlTabǁhandle_ztilt'

    @QtCore.pyqtSlot(str, name="on-klippy-status")
    def on_klippy_status(self, state: str):
        """Handles incoming klippy status changes"""
        if state.lower() == "ready":
            self.printcores_page.hide()
            self.disable_popups.emit(False)
            return
        if state.lower() == "startup":
            self.printcores_page.setText("Almost done \n be patient")
            return

    def show_swapcore(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁshow_swapcore__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁshow_swapcore__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁshow_swapcore__mutmut_orig(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_1(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit(None)
        self.call_load_panel.emit(True, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_2(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("XXCHANGE_PRINTCORESXX")
        self.call_load_panel.emit(True, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_3(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("change_printcores")
        self.call_load_panel.emit(True, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_4(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(None, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_5(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, None)

    def xǁControlTabǁshow_swapcore__mutmut_6(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit("Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_7(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, )

    def xǁControlTabǁshow_swapcore__mutmut_8(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(False, "Preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_9(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, "XXPreparing to swap print coreXX")

    def xǁControlTabǁshow_swapcore__mutmut_10(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, "preparing to swap print core")

    def xǁControlTabǁshow_swapcore__mutmut_11(self):
        """Show swap printcore"""
        self.run_gcode_signal.emit("CHANGE_PRINTCORES")
        self.call_load_panel.emit(True, "PREPARING TO SWAP PRINT CORE")
    
    xǁControlTabǁshow_swapcore__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁshow_swapcore__mutmut_1': xǁControlTabǁshow_swapcore__mutmut_1, 
        'xǁControlTabǁshow_swapcore__mutmut_2': xǁControlTabǁshow_swapcore__mutmut_2, 
        'xǁControlTabǁshow_swapcore__mutmut_3': xǁControlTabǁshow_swapcore__mutmut_3, 
        'xǁControlTabǁshow_swapcore__mutmut_4': xǁControlTabǁshow_swapcore__mutmut_4, 
        'xǁControlTabǁshow_swapcore__mutmut_5': xǁControlTabǁshow_swapcore__mutmut_5, 
        'xǁControlTabǁshow_swapcore__mutmut_6': xǁControlTabǁshow_swapcore__mutmut_6, 
        'xǁControlTabǁshow_swapcore__mutmut_7': xǁControlTabǁshow_swapcore__mutmut_7, 
        'xǁControlTabǁshow_swapcore__mutmut_8': xǁControlTabǁshow_swapcore__mutmut_8, 
        'xǁControlTabǁshow_swapcore__mutmut_9': xǁControlTabǁshow_swapcore__mutmut_9, 
        'xǁControlTabǁshow_swapcore__mutmut_10': xǁControlTabǁshow_swapcore__mutmut_10, 
        'xǁControlTabǁshow_swapcore__mutmut_11': xǁControlTabǁshow_swapcore__mutmut_11
    }
    xǁControlTabǁshow_swapcore__mutmut_orig.__name__ = 'xǁControlTabǁshow_swapcore'

    def handle_swapcore(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁhandle_swapcore__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁhandle_swapcore__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁhandle_swapcore__mutmut_orig(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("Executing \n Firmware Restart")
        self.run_gcode_signal.emit("FIRMWARE_RESTART")

    def xǁControlTabǁhandle_swapcore__mutmut_1(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText(None)
        self.run_gcode_signal.emit("FIRMWARE_RESTART")

    def xǁControlTabǁhandle_swapcore__mutmut_2(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("XXExecuting \n Firmware RestartXX")
        self.run_gcode_signal.emit("FIRMWARE_RESTART")

    def xǁControlTabǁhandle_swapcore__mutmut_3(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("executing \n firmware restart")
        self.run_gcode_signal.emit("FIRMWARE_RESTART")

    def xǁControlTabǁhandle_swapcore__mutmut_4(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("EXECUTING \n FIRMWARE RESTART")
        self.run_gcode_signal.emit("FIRMWARE_RESTART")

    def xǁControlTabǁhandle_swapcore__mutmut_5(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("Executing \n Firmware Restart")
        self.run_gcode_signal.emit(None)

    def xǁControlTabǁhandle_swapcore__mutmut_6(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("Executing \n Firmware Restart")
        self.run_gcode_signal.emit("XXFIRMWARE_RESTARTXX")

    def xǁControlTabǁhandle_swapcore__mutmut_7(self):
        """Handle swap printcore routine finish"""
        self.printcores_page.setText("Executing \n Firmware Restart")
        self.run_gcode_signal.emit("firmware_restart")
    
    xǁControlTabǁhandle_swapcore__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁhandle_swapcore__mutmut_1': xǁControlTabǁhandle_swapcore__mutmut_1, 
        'xǁControlTabǁhandle_swapcore__mutmut_2': xǁControlTabǁhandle_swapcore__mutmut_2, 
        'xǁControlTabǁhandle_swapcore__mutmut_3': xǁControlTabǁhandle_swapcore__mutmut_3, 
        'xǁControlTabǁhandle_swapcore__mutmut_4': xǁControlTabǁhandle_swapcore__mutmut_4, 
        'xǁControlTabǁhandle_swapcore__mutmut_5': xǁControlTabǁhandle_swapcore__mutmut_5, 
        'xǁControlTabǁhandle_swapcore__mutmut_6': xǁControlTabǁhandle_swapcore__mutmut_6, 
        'xǁControlTabǁhandle_swapcore__mutmut_7': xǁControlTabǁhandle_swapcore__mutmut_7
    }
    xǁControlTabǁhandle_swapcore__mutmut_orig.__name__ = 'xǁControlTabǁhandle_swapcore'

    @QtCore.pyqtSlot(str, int, "PyQt_PyObject", name="on-numpad-request")
    @QtCore.pyqtSlot(str, int, "PyQt_PyObject", int, int, name="on-numpad-request")
    def on_numpad_request(
        self,
        name: str,
        current_value: int,
        callback,
        min_value: int = 0,
        max_value: int = 100,
    ) -> None:
        """Handles numpad widget request"""
        self.numpadPage.value_selected.connect(callback)
        self.numpadPage.set_name(name)
        self.numpadPage.set_value(current_value)
        self.numpadPage.set_min_value(min_value)
        self.numpadPage.set_max_value(max_value)
        self.change_page(self.indexOf(self.numpadPage))

    @QtCore.pyqtSlot(str, int, name="on-numpad-change")
    def on_numpad_change(self, name: str, new_value: int) -> None:
        """Handles inputted numpad values"""
        if "bed" in name.lower():
            name = "heater_bed"
        elif "extruder" in name.lower():
            name = "extruder"
        self.run_gcode_signal.emit(
            f"SET_HEATER_TEMPERATURE HEATER={name} TARGET={new_value}"
        )

    def change_page(self, index):
        args = [index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁchange_page__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁchange_page__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁchange_page__mutmut_orig(self, index):
        """Handles changing page"""
        self.request_change_page.emit(2, index)

    def xǁControlTabǁchange_page__mutmut_1(self, index):
        """Handles changing page"""
        self.request_change_page.emit(None, index)

    def xǁControlTabǁchange_page__mutmut_2(self, index):
        """Handles changing page"""
        self.request_change_page.emit(2, None)

    def xǁControlTabǁchange_page__mutmut_3(self, index):
        """Handles changing page"""
        self.request_change_page.emit(index)

    def xǁControlTabǁchange_page__mutmut_4(self, index):
        """Handles changing page"""
        self.request_change_page.emit(2, )

    def xǁControlTabǁchange_page__mutmut_5(self, index):
        """Handles changing page"""
        self.request_change_page.emit(3, index)
    
    xǁControlTabǁchange_page__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁchange_page__mutmut_1': xǁControlTabǁchange_page__mutmut_1, 
        'xǁControlTabǁchange_page__mutmut_2': xǁControlTabǁchange_page__mutmut_2, 
        'xǁControlTabǁchange_page__mutmut_3': xǁControlTabǁchange_page__mutmut_3, 
        'xǁControlTabǁchange_page__mutmut_4': xǁControlTabǁchange_page__mutmut_4, 
        'xǁControlTabǁchange_page__mutmut_5': xǁControlTabǁchange_page__mutmut_5
    }
    xǁControlTabǁchange_page__mutmut_orig.__name__ = 'xǁControlTabǁchange_page'

    def back_button(self):
        """Handle back button click"""
        self.request_back_button.emit()

    def register_timed_callback(self, time: int, callback: callable) -> None:
        args = [time, callback]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁregister_timed_callback__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁregister_timed_callback__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁregister_timed_callback__mutmut_orig(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(True)
        _timer.timeout.connect(callback)
        _timer.start(int(time))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_1(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = None
        _timer.setSingleShot(True)
        _timer.timeout.connect(callback)
        _timer.start(int(time))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_2(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(None)
        _timer.timeout.connect(callback)
        _timer.start(int(time))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_3(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(False)
        _timer.timeout.connect(callback)
        _timer.start(int(time))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_4(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(True)
        _timer.timeout.connect(None)
        _timer.start(int(time))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_5(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(True)
        _timer.timeout.connect(callback)
        _timer.start(None)
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_6(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(True)
        _timer.timeout.connect(callback)
        _timer.start(int(None))
        self.timers.append(_timer)

    def xǁControlTabǁregister_timed_callback__mutmut_7(self, time: int, callback: callable) -> None:
        """Registers timed callback and starts the timeout"""
        _timer = QtCore.QTimer()
        _timer.setSingleShot(True)
        _timer.timeout.connect(callback)
        _timer.start(int(time))
        self.timers.append(None)
    
    xǁControlTabǁregister_timed_callback__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁregister_timed_callback__mutmut_1': xǁControlTabǁregister_timed_callback__mutmut_1, 
        'xǁControlTabǁregister_timed_callback__mutmut_2': xǁControlTabǁregister_timed_callback__mutmut_2, 
        'xǁControlTabǁregister_timed_callback__mutmut_3': xǁControlTabǁregister_timed_callback__mutmut_3, 
        'xǁControlTabǁregister_timed_callback__mutmut_4': xǁControlTabǁregister_timed_callback__mutmut_4, 
        'xǁControlTabǁregister_timed_callback__mutmut_5': xǁControlTabǁregister_timed_callback__mutmut_5, 
        'xǁControlTabǁregister_timed_callback__mutmut_6': xǁControlTabǁregister_timed_callback__mutmut_6, 
        'xǁControlTabǁregister_timed_callback__mutmut_7': xǁControlTabǁregister_timed_callback__mutmut_7
    }
    xǁControlTabǁregister_timed_callback__mutmut_orig.__name__ = 'xǁControlTabǁregister_timed_callback'

    @QtCore.pyqtSlot(bool, "PyQt_PyObject", int, name="select-extrude-feedrate")
    def handle_toggle_extrude_feedrate(self, checked: bool, caller, value: int) -> None:
        """Slot to change the extruder feedrate, mainly used for toggle buttons

        Args:
            checked (bool): Button checked state
            caller (PyQtObject): The button that called this slot
            value (int): New value for the extruder feedrate
        """
        if value == self.extrude_feedrate:
            return
        self.extrude_feedrate = value

    @QtCore.pyqtSlot(bool, "PyQt_PyObject", int, name="select-extrude-length")
    def handle_toggle_extrude_length(self, checked: bool, caller, value: int) -> None:
        """Slot that changes the extrude length, mainly used for toggle buttons

        Args:
            checked (bool): Button checked state
            caller (PyQtObject): The button that called this slot
            value (int): New value for the extrude length
        """
        if self.extrude_length == value:
            return
        self.extrude_length = value

    @QtCore.pyqtSlot(bool, float, name="handle-select-move-speed")
    def handle_select_move_speed(self, checked: bool, value: float) -> None:
        """Slot that changes the move speed of manual move commands, mainly used
        for toggle buttons

        Args:
            checked (bool): Button checked state
            value (float): New move speed value
        """
        if self.move_speed == value:
            return
        self.move_speed = value

    @QtCore.pyqtSlot(bool, float, name="handle-select-move-length")
    def handle_select_move_length(self, checked: bool, value: float) -> None:
        """Slot that changes the move length of manual move commands,
        mainly used for toggle buttons


        Args:
            checked (bool): Button checked state
            value (float): New length value
        """
        if self.move_length == value:
            return
        self.move_length = value

    @QtCore.pyqtSlot(str, name="handle-extrusion")
    def handle_extrusion(self, extrude: bool) -> None:
        """Slot that requests an extrusion/unextrusion move

        Args:
            extrude (bool): If True extrudes otherwise unextrudes.
        """
        can_extrude = bool(self.printer.heaters_object["extruder"]["can_extrude"])
        if not can_extrude:
            self.extrude_page_message = "Temperature too cold to extrude"
            self.panel.exp_info_label.setText(self.extrude_page_message)
            return
        if extrude:
            self.run_gcode_signal.emit(
                f"M83\nG1 E{self.extrude_length} F{self.extrude_feedrate * 60}\nM82\nM400"
            )
            self.extrude_page_message = "Extruding"
            self.panel.exp_info_label.setText(self.extrude_page_message)
        else:
            self.run_gcode_signal.emit(
                f"M83\nG1 E-{self.extrude_length} F{self.extrude_feedrate * 60}\nM82\nM400"
            )
            self.extrude_page_message = "Retracting"
            self.panel.exp_info_label.setText(self.extrude_page_message)
        # This block of code schedules a method to be called in x amount of milliseconds
        _sch_time_s = float(
            self.extrude_length / self.extrude_feedrate
        )  # calculate the amount of time it'll take for the operation
        self.extrude_page_message = "Ready"
        self.register_timed_callback(
            int(_sch_time_s + 2.0) * 1000,  # In milliseconds
            lambda: self.panel.exp_info_label.setText(self.extrude_page_message),
        )

    @QtCore.pyqtSlot(str, name="handle-move-axis")
    def handle_move_axis(self, axis: str) -> None:
        """Slot that requests manual move command

        Args:
            axis (str): String that contains one of the following axis `
                ['X',
                'X-'
                ,'Y'
                ,'Y-'
                ,'Z'
                ,'Z-']`. [^1]

        ---

        [^1]: The **-** symbol indicates the negative direction for that axis

        """
        if axis not in ["X", "X-", "Y", "Y-", "Z", "Z-"]:
            return
        self.run_gcode_signal.emit(
            f"G91\nG0 {axis}{float(self.move_length)} F{float(self.move_speed * 60)}\nG90\nM400"
        )

    @QtCore.pyqtSlot(str, list, name="on-toolhead-update")
    def on_toolhead_update(self, field: str, values: list) -> None:
        """Handles updated from toolhead printer object"""
        if field == "position":
            self.panel.mva_x_value_label.setText(f"{values[0]:.2f}")
            self.panel.mva_y_value_label.setText(f"{values[1]:.2f}")
            self.panel.mva_z_value_label.setText(f"{values[2]:.3f}")

            if values[0] == "252,50" and values[1] == "250" and values[2] == "50":
                self.call_load_panel.emit(False, "")
        self.toolhead_info.update({f"{field}": values})

    @QtCore.pyqtSlot(str, str, float, name="on-extruder-update")
    def on_extruder_update(
        self, extruder_name: str, field: str, new_value: float
    ) -> None:
        """Handles updates from extruder printer object"""
        if extruder_name == "extruder" and field == "temperature":
            self.panel.extruder_temp_display.setText(f"{new_value:.1f}")
        if extruder_name == "extruder" and field == "target":
            self.panel.extruder_temp_display.secondary_text = f"{new_value:.1f}"
        self.extruder_info.update({f"{extruder_name}": {f"{field}": new_value}})

    @QtCore.pyqtSlot(str, str, float, name="on-heater-bed-update")
    def on_heater_bed_update(self, name: str, field: str, new_value: float) -> None:
        """Handles updated from heater_bed printer object"""
        if field == "temperature":
            self.panel.bed_temp_display.setText(f"{new_value:.1f}")
        if field == "target":
            self.panel.bed_temp_display.secondary_text = f"{new_value:.1f}"
        self.bed_info.update({f"{name}": {f"{field}": new_value}})

    def paintEvent(self, a0: QtGui.QPaintEvent) -> None:
        args = [a0]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁControlTabǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁControlTabǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁControlTabǁpaintEvent__mutmut_orig(self, a0: QtGui.QPaintEvent) -> None:
        """Handles ControlTab Widget painting"""
        if self.panel.extrude_page.isVisible():
            self.panel.exp_info_label.setText(self.extrude_page_message)
        return super().paintEvent(a0)

    def xǁControlTabǁpaintEvent__mutmut_1(self, a0: QtGui.QPaintEvent) -> None:
        """Handles ControlTab Widget painting"""
        if self.panel.extrude_page.isVisible():
            self.panel.exp_info_label.setText(None)
        return super().paintEvent(a0)

    def xǁControlTabǁpaintEvent__mutmut_2(self, a0: QtGui.QPaintEvent) -> None:
        """Handles ControlTab Widget painting"""
        if self.panel.extrude_page.isVisible():
            self.panel.exp_info_label.setText(self.extrude_page_message)
        return super().paintEvent(None)
    
    xǁControlTabǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁControlTabǁpaintEvent__mutmut_1': xǁControlTabǁpaintEvent__mutmut_1, 
        'xǁControlTabǁpaintEvent__mutmut_2': xǁControlTabǁpaintEvent__mutmut_2
    }
    xǁControlTabǁpaintEvent__mutmut_orig.__name__ = 'xǁControlTabǁpaintEvent'
