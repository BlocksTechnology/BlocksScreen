import typing
from dataclasses import dataclass
from enum import Enum, auto
from functools import partial

from lib.moonrakerComm import MoonWebSocket
from lib.panels.widgets.troubleshootPage import TroubleshootPage
from lib.printer import Printer
from lib.ui.utilitiesStackedWidget_ui import Ui_utilitiesStackedWidget
from lib.utils.blocks_button import BlocksCustomButton
from lib.utils.toggleAnimatedButton import ToggleAnimatedButton
from PyQt6 import QtCore, QtGui, QtWidgets

from lib.panels.widgets.optionCardWidget import OptionCard
from lib.panels.widgets.inputshaperPage import InputShaperPage
from lib.panels.widgets.basePopup import BasePopup

import re
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


@dataclass
class LedState:
    """Represents the state of an LED light."""

    led_type: str
    red: int = 0
    green: int = 0
    blue: int = 0
    white: int = 255
    state: str = "on"

    def get_gcode(self, name: str) -> str:
        """Generates the G-code command for the current state."""
        if self.state == "off":
            return f"SET_LED LED={name} RED=0 GREEN=0 BLUE=0 WHITE=0"
        if self.led_type == "white":
            return f"SET_LED LED={name} WHITE={self.white / 255:.2f}"
        # Default to RGB
        return (
            f"SET_LED LED={name} RED={self.red / 255:.2f} "
            f"GREEN={self.green / 255:.2f} BLUE={self.blue / 255:.2f} "
            f"WHITE={self.white / 255:.2f}"
        )


class Process(Enum):
    FAN = auto()
    AXIS = auto()
    BED_HEATER = auto()
    EXTRUDER = auto()
    AXIS_MAINTENANCE = auto()


class UtilitiesTab(QtWidgets.QStackedWidget):
    request_back: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        name="request-back"
    )
    request_change_page: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        int, int, name="request-change-page"
    )
    request_available_objects_signal: typing.ClassVar[QtCore.pyqtSignal] = (
        QtCore.pyqtSignal(name="get-available-objects")
    )
    run_gcode_signal: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        str, name="run-gcode"
    )
    request_numpad_signal: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        int,
        str,
        str,
        "PyQt_PyObject",
        QtWidgets.QStackedWidget,
        name="request-numpad",
    )
    subscribe_config: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        [list, "PyQt_PyObject"],
        [str, "PyQt_PyObject"],
        name="on-subscribe-config",
    )
    on_update_message: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        dict, name="handle-update-message"
    )

    update_available: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        bool, name="update-available"
    )

    show_update_page: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        bool, name="show-update-page"
    )
    call_load_panel = QtCore.pyqtSignal(bool, str, name="call-load-panel")

    def __init__(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        args = [parent, ws, printer]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ__init____mutmut_orig(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_1(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(None)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_2(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = None
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_3(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(None)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_4(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = None
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_5(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = None
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_6(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = None

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_7(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(None)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_8(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = None
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_9(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "XXfansXX": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_10(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "FANS": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_11(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "XXaxisXX": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_12(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "AXIS": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_13(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"XXxXX": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_14(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"X": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_15(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "XXindfXX", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_16(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "INDF", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_17(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "XXyXX": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_18(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "Y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_19(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "XXindfXX", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_20(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "INDF", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_21(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "XXzXX": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_22(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "Z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_23(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "XXindfXX"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_24(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "INDF"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_25(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "XXbheatXX": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_26(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "BHEAT": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_27(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"XXBed_HeaterXX": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_28(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"bed_heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_29(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"BED_HEATER": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_30(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "XXindfXX"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_31(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "INDF"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_32(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "XXextrudeXX": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_33(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "EXTRUDE": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_34(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"XXextruderXX": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_35(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"EXTRUDER": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_36(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "XXindfXX"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_37(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "INDF"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_38(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "XXledsXX": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_39(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "LEDS": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_40(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = None
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_41(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = None

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_42(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = ""
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_43(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = ""
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_44(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = None
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_45(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "XXxXX"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_46(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "X"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_47(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = None
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_48(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 2
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_49(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = None
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_50(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = True
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_51(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = ""
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_52(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = None

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_53(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = True

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_54(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(None)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_55(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            None
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_56(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: None
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_57(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(None)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_58(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(True)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_59(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = None
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_60(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(None)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_61(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(None)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_62(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(None)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_63(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = None
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_64(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(None, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_65(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=None, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_66(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=None)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_67(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_68(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_69(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, )
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_70(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=False, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_71(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=False)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_72(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(None)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_73(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(None)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_74(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(None, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_75(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, None)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_76(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_77(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, )
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_78(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            None, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_79(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, None
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_80(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_81(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_82(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(None, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_83(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, None)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_84(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_85(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, )
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_86(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            None, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_87(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, None
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_88(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_89(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_90(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(None, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_91(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, None)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_92(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_93(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, )

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_94(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(None, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_95(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, None)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_96(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_97(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, )
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_98(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            None, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_99(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, None
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_100(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_101(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_102(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(None)
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_103(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(None, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_104(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, None))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_105(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_106(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, ))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_107(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            None
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_108(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(None, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_109(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, None)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_110(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_111(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, )
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_112(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(None)
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_113(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(None, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_114(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, None))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_115(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_116(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, ))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_117(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(None)
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_118(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(None, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_119(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, None))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_120(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_121(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, ))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_122(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(None)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_123(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(None)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_124(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(None)
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_125(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(None, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_126(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, None))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_127(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial("x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_128(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, ))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_129(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "XXxXX"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_130(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "X"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_131(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(None)
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_132(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(None, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_133(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, None))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_134(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial("y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_135(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, ))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_136(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "XXyXX"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_137(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "Y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_138(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(None)

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_139(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(None, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_140(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, None))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_141(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial("z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_142(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, ))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_143(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "XXzXX"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_144(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "Z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_145(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = None

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_146(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(None)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_147(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(None)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_148(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(None)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_149(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(None)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_150(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            None
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_151(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "XXPyQt_PyObjectXX"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_152(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "pyqt_pyobject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_153(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PYQT_PYOBJECT"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_154(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            None
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_155(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "XXPyQt_PyObjectXX"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_156(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "pyqt_pyobject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_157(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PYQT_PYOBJECT"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_158(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(None)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_159(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(None)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_160(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(None)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_161(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            None
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_162(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(None)
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_163(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap("XX:/system/media/btn_icons/update-software-icon.svgXX")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_164(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/SYSTEM/MEDIA/BTN_ICONS/UPDATE-SOFTWARE-ICON.SVG")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_165(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = None  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_166(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            None,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_167(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            None,
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_168(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            None,
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_169(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            None,
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_170(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_171(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_172(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_173(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_174(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "XXAutomatic\nInput ShaperXX",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_175(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "automatic\ninput shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_176(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "AUTOMATIC\nINPUT SHAPER",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_177(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "XXAutomatic Input ShaperXX",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_178(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "automatic input shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_179(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "AUTOMATIC INPUT SHAPER",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_180(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(None),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_181(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap("XX:/input_shaper/media/btn_icons/input_shaper_auto.svgXX"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_182(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/INPUT_SHAPER/MEDIA/BTN_ICONS/INPUT_SHAPER_AUTO.SVG"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_183(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName(None)
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_184(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("XXAutomatic_IS_CardXX")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_185(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("automatic_is_card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_186(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("AUTOMATIC_IS_CARD")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_187(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            None, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_188(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=None
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_189(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_190(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_191(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            None
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_192(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: None
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_193(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is(None)
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_194(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("XXSHAPER_CALIBRATEXX")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_195(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("shaper_calibrate")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_196(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = None  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_197(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            None,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_198(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            None,
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_199(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            None,
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_200(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            None,
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_201(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_202(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_203(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_204(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_205(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "XXManual\nInput ShaperXX",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_206(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "manual\ninput shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_207(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "MANUAL\nINPUT SHAPER",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_208(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "XXManual Input ShaperXX",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_209(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "manual input shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_210(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "MANUAL INPUT SHAPER",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_211(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(None),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_212(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap("XX:/input_shaper/media/btn_icons/input_shaper_manual.svgXX"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_213(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/INPUT_SHAPER/MEDIA/BTN_ICONS/INPUT_SHAPER_MANUAL.SVG"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_214(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName(None)
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_215(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("XXManual_IS_CardXX")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_216(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("manual_is_card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_217(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("MANUAL_IS_CARD")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_218(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            None, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_219(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=None
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_220(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_221(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_222(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(None)

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_223(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: None)

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_224(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(None))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_225(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is("XXXX"))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_226(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = None
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_227(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = None
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_228(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            None
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_229(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: None
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_230(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is(None)
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_231(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("XXSHAPER_CALIBRATE AXIS=YXX")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_232(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("shaper_calibrate axis=y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_233(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            None
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_234(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: None
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_235(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is(None)
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_236(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("XXSHAPER_CALIBRATE AXIS=XXX")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_237(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("shaper_calibrate axis=x")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(self.panel.input_shaper_page))
        )

    def xǁUtilitiesTabǁ__init____mutmut_238(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            None
        )

    def xǁUtilitiesTabǁ__init____mutmut_239(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: None
        )

    def xǁUtilitiesTabǁ__init____mutmut_240(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(None)
        )

    def xǁUtilitiesTabǁ__init____mutmut_241(
        self, parent: QtWidgets.QWidget, ws: MoonWebSocket, printer: Printer
    ) -> None:
        super().__init__(parent)

        self.panel = Ui_utilitiesStackedWidget()
        self.panel.setupUi(self)

        self.ws = ws
        self.printer: Printer = printer
        self.troubleshoot_page: TroubleshootPage = TroubleshootPage(self)

        # --- State Variables ---
        self.objects: dict = {
            "fans": {},
            "axis": {"x": "indf", "y": "indf", "z": "indf"},
            "bheat": {"Bed_Heater": "indf"},
            "extrude": {"extruder": "indf"},
            "leds": {},
        }
        self.x_inputshaper: dict = {}
        self.stepper_limits: dict = {}

        self.current_object: typing.Optional[str] = None
        self.current_process: typing.Optional[Process] = None
        self.axis_in: str = "x"
        self.amount: int = 1
        self.tb: bool = False
        self.cg = None
        self.aut: bool = False

        # --- UI Setup ---
        self.setLayoutDirection(QtCore.Qt.LayoutDirection.LeftToRight)
        self.panel.update_btn.clicked.connect(
            lambda: self.show_update_page[bool].emit(False)
        )

        self.is_page = InputShaperPage(self)
        self.is_page.call_load_panel.connect(self.call_load_panel)
        self.addWidget(self.is_page)

        self.dialog_page = BasePopup(self, dialog=True, floating=True)
        self.addWidget(self.dialog_page)

        # --- Back Buttons ---
        for button in (
            self.panel.leds_back_btn,
            self.panel.info_back_btn,
            self.panel.leds_slider_back_btn,
            self.panel.input_shaper_back_btn,
            self.panel.routine_check_back_btn,
            self.is_page.update_back_btn,
        ):
            button.clicked.connect(self.back_button)

        # --- Page Navigation ---
        self._connect_page_change(self.panel.utilities_axes_btn, self.panel.axes_page)
        self._connect_page_change(
            self.panel.utilities_input_shaper_btn, self.panel.input_shaper_page
        )
        self._connect_page_change(self.panel.utilities_info_btn, self.panel.info_page)
        self._connect_page_change(
            self.panel.utilities_routine_check_btn, self.panel.routines_page
        )
        self._connect_page_change(self.panel.am_cancel, self.panel.utilities_page)

        self._connect_page_change(self.panel.axes_back_btn, self.panel.utilities_page)
        self._connect_page_change(
            self.troubleshoot_page.tb_back_btn, self.panel.utilities_page
        )

        # --- Routines ---
        self.panel.rc_fans.clicked.connect(partial(self.run_routine, Process.FAN))
        self.panel.rc_bheat.clicked.connect(
            partial(self.run_routine, Process.BED_HEATER)
        )
        self.panel.rc_ext.clicked.connect(partial(self.run_routine, Process.EXTRUDER))
        self.panel.rc_axis.clicked.connect(partial(self.run_routine, Process.AXIS))
        self.panel.rc_no.clicked.connect(self.on_routine_answer)
        self.panel.rc_yes.clicked.connect(self.on_routine_answer)

        # --- Axis Maintenance ---
        self.panel.axis_x_btn.clicked.connect(partial(self.axis_maintenance, "x"))
        self.panel.axis_y_btn.clicked.connect(partial(self.axis_maintenance, "y"))
        self.panel.axis_z_btn.clicked.connect(partial(self.axis_maintenance, "z"))

        self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON

        # --- LEDs ---
        # self.panel.leds_r_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_g_slider.sliderReleased.connect(self.update_led_values)
        # self.panel.leds_b_slider.sliderReleased.connect(self.update_led_values)
        self.panel.leds_w_slider.sliderReleased.connect(self.update_led_values)
        self.panel.toggle_led_button.clicked.connect(self.toggle_led_state)

        # --- Websocket/Printer Signals ---
        self.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.is_page.run_gcode_signal.connect(self.ws.api.run_gcode)
        self.subscribe_config[str, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.subscribe_config[list, "PyQt_PyObject"].connect(
            self.printer.on_subscribe_config
        )
        self.printer.gcode_response.connect(self.handle_gcode_response)

        # --- Initialize Printer Communication ---
        self.printer.printer_config.connect(self.on_printer_config_received)
        self.printer.gcode_move_update.connect(self.on_gcode_move_update)

        self.panel.update_btn.setPixmap(
            QtGui.QPixmap(":/system/media/btn_icons/update-software-icon.svg")
        )

        # ---- Input Shaper ----
        self.automatic_is = OptionCard(
            self,
            "Automatic\nInput Shaper",
            "Automatic Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_auto.svg"),
        )  # type: ignore
        self.automatic_is.setObjectName("Automatic_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.automatic_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.automatic_is.continue_clicked.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE")
        )

        self.manual_is = OptionCard(
            self,
            "Manual\nInput Shaper",
            "Manual Input Shaper",
            QtGui.QPixmap(":/input_shaper/media/btn_icons/input_shaper_manual.svg"),
        )  # type: ignore
        self.manual_is.setObjectName("Manual_IS_Card")
        self.panel.is_content_layout.addWidget(
            self.manual_is, alignment=QtCore.Qt.AlignmentFlag.AlignHCenter
        )
        self.manual_is.continue_clicked.connect(lambda: self.handle_is(""))

        self.is_types: dict = {}
        self.is_aut_types: dict = {}
        self.dialog_page.accepted.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=Y")
        )
        self.dialog_page.rejected.connect(
            lambda: self.handle_is("SHAPER_CALIBRATE AXIS=X")
        )

        self.is_page.action_btn.clicked.connect(
            lambda: self.change_page(self.indexOf(None))
        )
    
    xǁUtilitiesTabǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTab�
        'xǁUtilitiesTabǁ__init____mutmut_238': xǁUtilitiesTabǁ__init____mutmut_238, 
        'xǁUtilitiesTabǁ__init____mutmut_239': xǁUtilitiesTabǁ__init____mutmut_239, 
        'xǁUtilitiesTabǁ__init____mutmut_240': xǁUtilitiesTabǁ__init____mutmut_240, 
        'xǁUtilitiesTabǁ__init____mutmut_241': xǁUtilitiesTabǁ__init____mutmut_241
    }
    xǁUtilitiesTabǁ__init____mutmut_orig.__name__ = 'xǁUtilitiesTabǁ__init__'

    def handle_gcode_response(self, data: list[str]) -> None:
        args = [data]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_gcode_response__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_gcode_response__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_orig(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_1(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 and not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_2(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) and len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_3(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_4(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) == 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_5(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 2 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_6(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_7(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                None
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_8(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = None

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_9(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[1]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_10(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = None
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_11(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            None
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_12(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"XXFitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%XX"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_13(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"fitted shaper '(?p<name>\w+)' frequency = (?p<freq>[\d\.]+) hz \(vibrations = (?p<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_14(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"FITTED SHAPER '(?P<NAME>\w+)' FREQUENCY = (?P<FREQ>[\d\.]+) HZ \(VIBRATIONS = (?P<VIB>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_15(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = None

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_16(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(None)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_17(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = None
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_18(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group(None)
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_19(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("XXnameXX")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_20(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("NAME")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_21(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = None
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_22(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(None)
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_23(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group(None))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_24(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("XXfreqXX"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_25(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("FREQ"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_26(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = None
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_27(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(None)
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_28(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group(None))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_29(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("XXvibXX"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_30(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("VIB"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_31(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = None
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_32(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(None, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_33(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, None)
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_34(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get({})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_35(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, )
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_36(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                None
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_37(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "XXfrequencyXX": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_38(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "FREQUENCY": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_39(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "XXvibrationXX": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_40(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "VIBRATION": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_41(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "XXmax_accelXX": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_42(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "MAX_ACCEL": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_43(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get(None, 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_44(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", None),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_45(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get(0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_46(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", ),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_47(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("XXmax_accelXX", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_48(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("MAX_ACCEL", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_49(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 1.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_50(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = None

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_51(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = None
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_52(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            None
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_53(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"XXTo avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2XX"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_54(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"to avoid too much smoothing with '(?p<name>\w+)', suggested max_accel <= (?p<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_55(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"TO AVOID TOO MUCH SMOOTHING WITH '(?P<NAME>\w+)', SUGGESTED MAX_ACCEL <= (?P<ACCEL>[\d\.]+) MM/SEC\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_56(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = None

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_57(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(None)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_58(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = None
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_59(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group(None)
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_60(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("XXnameXX")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_61(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("NAME")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_62(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = None

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_63(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(None)

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_64(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group(None))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_65(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("XXaccelXX"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_66(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("ACCEL"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_67(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types or isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_68(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name not in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_69(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = None
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_70(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["XXmax_accelXX"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_71(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["MAX_ACCEL"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_72(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = None
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_73(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(None, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_74(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, None)
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_75(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get({})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_76(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, )
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_77(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = None
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_78(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["XXmax_accelXX"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_79(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["MAX_ACCEL"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_80(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = None
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_81(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            None
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_82(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"XXRecommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) HzXX"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_83(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"recommended shaper_type_(?p<axis>[xy]) = (?p<type>\w+), shaper_freq_(?p=axis) = (?p<freq>[\d\.]+) hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_84(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"RECOMMENDED SHAPER_TYPE_(?P<AXIS>[XY]) = (?P<TYPE>\w+), SHAPER_FREQ_(?P=AXIS) = (?P<FREQ>[\d\.]+) HZ"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_85(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = None
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_86(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(None)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_87(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = None
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_88(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group(None)
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_89(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("XXaxisXX")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_90(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("AXIS")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_91(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = None
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_92(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group(None)
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_93(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("XXtypeXX")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_94(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("TYPE")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_95(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = None
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_96(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["XXAxisXX"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_97(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_98(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["AXIS"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_99(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = None
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_100(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) != 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_101(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 3:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_102(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit(None)
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_103(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("XXSAVE_CONFIGXX")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_104(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("save_config")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_105(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(None, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_106(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, None)
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_107(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit("")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_108(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, )
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_109(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(True, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_110(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "XXXX")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_111(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = None
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_112(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = True
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_113(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = None
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_114(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_115(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("XXsuggested_typeXX", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_116(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("SUGGESTED_TYPE", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_117(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "XXAxisXX"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_118(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_119(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "AXIS"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_120(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = None

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_121(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(None)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_122(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = None
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_123(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(None, None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_124(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_125(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), )
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_126(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(None), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_127(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key != first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_128(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(None, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_129(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, None)
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_130(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry("Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_131(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, )
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_132(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "XXRecommended typeXX")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_133(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_134(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "RECOMMENDED TYPE")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_135(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(None)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_136(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(None, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_137(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, None)
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_138(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit("")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_139(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, )
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_140(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(True, "")
            return

    def xǁUtilitiesTabǁhandle_gcode_response__mutmut_141(self, data: list[str]) -> None:
        """
        Parses a Klipper Input Shaper console message and updates self.is_types.
        """

        if not isinstance(data, list) or len(data) != 1 or not isinstance(data[0], str):
            print(
                f"WARNING: Invalid input format. Expected a list with one string. Received: {data}"
            )
            return

        message = data[0]

        pattern_fitted = re.compile(
            r"Fitted shaper '(?P<name>\w+)' frequency = (?P<freq>[\d\.]+) Hz \(vibrations = (?P<vib>[\d\.]+)%"
        )
        match_fitted = pattern_fitted.search(message)

        if match_fitted:
            name = match_fitted.group("name")
            freq = float(match_fitted.group("freq"))
            vib = float(match_fitted.group("vib"))
            current_data = self.is_types.get(name, {})
            current_data.update(
                {
                    "frequency": freq,
                    "vibration": vib,
                    "max_accel": current_data.get("max_accel", 0.0),
                }
            )
            self.is_types[name] = current_data

            return
        pattern_accel = re.compile(
            r"To avoid too much smoothing with '(?P<name>\w+)', suggested max_accel <= (?P<accel>[\d\.]+) mm/sec\^2"
        )
        match_accel = pattern_accel.search(message)

        if match_accel:
            name = match_accel.group("name")
            accel = float(match_accel.group("accel"))

            if name in self.is_types and isinstance(self.is_types[name], dict):
                self.is_types[name]["max_accel"] = accel
            else:
                self.is_types[name] = self.is_types.get(name, {})
                self.is_types[name]["max_accel"] = accel
            return

        pattern_recommended = re.compile(
            r"Recommended shaper_type_(?P<axis>[xy]) = (?P<type>\w+), shaper_freq_(?P=axis) = (?P<freq>[\d\.]+) Hz"
        )
        match_recommended = pattern_recommended.search(message)
        if match_recommended:
            axis = match_recommended.group("axis")
            recommended_type = match_recommended.group("type")
            self.is_types["Axis"] = axis
            if self.aut:
                self.is_aut_types[axis] = recommended_type
                if len(self.is_aut_types) == 2:
                    self.run_gcode_signal.emit("SAVE_CONFIG")
                    self.call_load_panel.emit(False, "")
                    self.aut = False
                    return
                return

            reordered = {recommended_type: self.is_types[recommended_type]}
            for key, value in self.is_types.items():
                if key not in ("suggested_type", recommended_type, "Axis"):
                    reordered[key] = value

            self.is_page.set_type_dictionary(self.is_types)
            first_key = next(iter(reordered.keys()), None)
            for key in reordered.keys():
                if key == first_key:
                    self.is_page.add_type_entry(key, "Recommended type")
                else:
                    self.is_page.add_type_entry(key)

            self.is_page.build_model_list()
            self.call_load_panel.emit(False, "XXXX")
            return
    
    xǁUtilitiesTabǁhandle_gcode_response__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁhandle_gcode_response__mutmut_1': xǁUtilitiesTabǁhandle_gcode_response__mutmut_1, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_2': xǁUtilitiesTabǁhandle_gcode_response__mutmut_2, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_3': xǁUtilitiesTabǁhandle_gcode_response__mutmut_3, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_4': xǁUtilitiesTabǁhandle_gcode_response__mutmut_4, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_5': xǁUtilitiesTabǁhandle_gcode_response__mutmut_5, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_6': xǁUtilitiesTabǁhandle_gcode_response__mutmut_6, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_7': xǁUtilitiesTabǁhandle_gcode_response__mutmut_7, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_8': xǁUtilitiesTabǁhandle_gcode_response__mutmut_8, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_9': xǁUtilitiesTabǁhandle_gcode_response__mutmut_9, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_10': xǁUtilitiesTabǁhandle_gcode_response__mutmut_10, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_11': xǁUtilitiesTabǁhandle_gcode_response__mutmut_11, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_12': xǁUtilitiesTabǁhandle_gcode_response__mutmut_12, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_13': xǁUtilitiesTabǁhandle_gcode_response__mutmut_13, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_14': xǁUtilitiesTabǁhandle_gcode_response__mutmut_14, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_15': xǁUtilitiesTabǁhandle_gcode_response__mutmut_15, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_16': xǁUtilitiesTabǁhandle_gcode_response__mutmut_16, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_17': xǁUtilitiesTabǁhandle_gcode_response__mutmut_17, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_18': xǁUtilitiesTabǁhandle_gcode_response__mutmut_18, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_19': xǁUtilitiesTabǁhandle_gcode_response__mutmut_19, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_20': xǁUtilitiesTabǁhandle_gcode_response__mutmut_20, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_21': xǁUtilitiesTabǁhandle_gcode_response__mutmut_21, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_22': xǁUtilitiesTabǁhandle_gcode_response__mutmut_22, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_23': xǁUtilitiesTabǁhandle_gcode_response__mutmut_23, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_24': xǁUtilitiesTabǁhandle_gcode_response__mutmut_24, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_25': xǁUtilitiesTabǁhandle_gcode_response__mutmut_25, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_26': xǁUtilitiesTabǁhandle_gcode_response__mutmut_26, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_27': xǁUtilitiesTabǁhandle_gcode_response__mutmut_27, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_28': xǁUtilitiesTabǁhandle_gcode_response__mutmut_28, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_29': xǁUtilitiesTabǁhandle_gcode_response__mutmut_29, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_30': xǁUtilitiesTabǁhandle_gcode_response__mutmut_30, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_31': xǁUtilitiesTabǁhandle_gcode_response__mutmut_31, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_32': xǁUtilitiesTabǁhandle_gcode_response__mutmut_32, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_33': xǁUtilitiesTabǁhandle_gcode_response__mutmut_33, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_34': xǁUtilitiesTabǁhandle_gcode_response__mutmut_34, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_35': xǁUtilitiesTabǁhandle_gcode_response__mutmut_35, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_36': xǁUtilitiesTabǁhandle_gcode_response__mutmut_36, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_37': xǁUtilitiesTabǁhandle_gcode_response__mutmut_37, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut
 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_128': xǁUtilitiesTabǁhandle_gcode_response__mutmut_128, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_129': xǁUtilitiesTabǁhandle_gcode_response__mutmut_129, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_130': xǁUtilitiesTabǁhandle_gcode_response__mutmut_130, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_131': xǁUtilitiesTabǁhandle_gcode_response__mutmut_131, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_132': xǁUtilitiesTabǁhandle_gcode_response__mutmut_132, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_133': xǁUtilitiesTabǁhandle_gcode_response__mutmut_133, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_134': xǁUtilitiesTabǁhandle_gcode_response__mutmut_134, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_135': xǁUtilitiesTabǁhandle_gcode_response__mutmut_135, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_136': xǁUtilitiesTabǁhandle_gcode_response__mutmut_136, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_137': xǁUtilitiesTabǁhandle_gcode_response__mutmut_137, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_138': xǁUtilitiesTabǁhandle_gcode_response__mutmut_138, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_139': xǁUtilitiesTabǁhandle_gcode_response__mutmut_139, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_140': xǁUtilitiesTabǁhandle_gcode_response__mutmut_140, 
        'xǁUtilitiesTabǁhandle_gcode_response__mutmut_141': xǁUtilitiesTabǁhandle_gcode_response__mutmut_141
    }
    xǁUtilitiesTabǁhandle_gcode_response__mutmut_orig.__name__ = 'xǁUtilitiesTabǁhandle_gcode_response'

    def handle_is(self, gcode: str) -> None:
        args = [gcode]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_is__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_is__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁhandle_is__mutmut_orig(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_1(self, gcode: str) -> None:
        if gcode != "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_2(self, gcode: str) -> None:
        if gcode == "XXSHAPER_CALIBRATEXX":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_3(self, gcode: str) -> None:
        if gcode == "shaper_calibrate":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_4(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit(None)
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_5(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("XXG28\nM400XX")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_6(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("g28\nm400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_7(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = None
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_8(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = False
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_9(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(None)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_10(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode != "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_11(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "XXXX":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_12(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color(None)
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_13(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("XX#dfdfdfXX")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_14(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#DFDFDF")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_15(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color(None)
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_16(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("XX#dfdfdfXX")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_17(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#DFDFDF")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_18(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color(None)
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_19(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("XX#000000XX")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_20(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color(None)
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_21(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("XX#000000XX")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_22(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text(None)
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_23(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("XXX axisXX")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_24(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("x axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_25(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X AXIS")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_26(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text(None)
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_27(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("XXY axisXX")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_28(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_29(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y AXIS")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_30(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                None
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_31(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "XXSelect the axis you want to execute the input shaper on:XX"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_32(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_33(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "SELECT THE AXIS YOU WANT TO EXECUTE THE INPUT SHAPER ON:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_34(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit(None)
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_35(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("XXG28\nM400XX")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_36(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("g28\nm400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_37(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(None)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_38(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(None)

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_39(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(None))

        self.call_load_panel.emit(True, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_40(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(None, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_41(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, None)

    def xǁUtilitiesTabǁhandle_is__mutmut_42(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit("Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_43(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, )

    def xǁUtilitiesTabǁhandle_is__mutmut_44(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(False, "Running Input Shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_45(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "XXRunning Input Shaper...XX")

    def xǁUtilitiesTabǁhandle_is__mutmut_46(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "running input shaper...")

    def xǁUtilitiesTabǁhandle_is__mutmut_47(self, gcode: str) -> None:
        if gcode == "SHAPER_CALIBRATE":
            self.run_gcode_signal.emit("G28\nM400")
            self.aut = True
            self.run_gcode_signal.emit(gcode)
        elif gcode == "":
            self.dialog_page.confirm_background_color("#dfdfdf")
            self.dialog_page.cancel_background_color("#dfdfdf")
            self.dialog_page.cancel_font_color("#000000")
            self.dialog_page.confirm_font_color("#000000")
            self.dialog_page.cancel_button_text("X axis")
            self.dialog_page.confirm_button_text("Y axis")
            self.dialog_page.set_message(
                "Select the axis you want to execute the input shaper on:"
            )
            self.dialog_page.show()
            return
        else:
            self.run_gcode_signal.emit("G28\nM400")
            self.run_gcode_signal.emit(gcode)
            self.change_page(self.indexOf(self.is_page))

        self.call_load_panel.emit(True, "RUNNING INPUT SHAPER...")
    
    xǁUtilitiesTabǁhandle_is__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁhandle_is__mutmut_1': xǁUtilitiesTabǁhandle_is__mutmut_1, 
        'xǁUtilitiesTabǁhandle_is__mutmut_2': xǁUtilitiesTabǁhandle_is__mutmut_2, 
        'xǁUtilitiesTabǁhandle_is__mutmut_3': xǁUtilitiesTabǁhandle_is__mutmut_3, 
        'xǁUtilitiesTabǁhandle_is__mutmut_4': xǁUtilitiesTabǁhandle_is__mutmut_4, 
        'xǁUtilitiesTabǁhandle_is__mutmut_5': xǁUtilitiesTabǁhandle_is__mutmut_5, 
        'xǁUtilitiesTabǁhandle_is__mutmut_6': xǁUtilitiesTabǁhandle_is__mutmut_6, 
        'xǁUtilitiesTabǁhandle_is__mutmut_7': xǁUtilitiesTabǁhandle_is__mutmut_7, 
        'xǁUtilitiesTabǁhandle_is__mutmut_8': xǁUtilitiesTabǁhandle_is__mutmut_8, 
        'xǁUtilitiesTabǁhandle_is__mutmut_9': xǁUtilitiesTabǁhandle_is__mutmut_9, 
        'xǁUtilitiesTabǁhandle_is__mutmut_10': xǁUtilitiesTabǁhandle_is__mutmut_10, 
        'xǁUtilitiesTabǁhandle_is__mutmut_11': xǁUtilitiesTabǁhandle_is__mutmut_11, 
        'xǁUtilitiesTabǁhandle_is__mutmut_12': xǁUtilitiesTabǁhandle_is__mutmut_12, 
        'xǁUtilitiesTabǁhandle_is__mutmut_13': xǁUtilitiesTabǁhandle_is__mutmut_13, 
        'xǁUtilitiesTabǁhandle_is__mutmut_14': xǁUtilitiesTabǁhandle_is__mutmut_14, 
        'xǁUtilitiesTabǁhandle_is__mutmut_15': xǁUtilitiesTabǁhandle_is__mutmut_15, 
        'xǁUtilitiesTabǁhandle_is__mutmut_16': xǁUtilitiesTabǁhandle_is__mutmut_16, 
        'xǁUtilitiesTabǁhandle_is__mutmut_17': xǁUtilitiesTabǁhandle_is__mutmut_17, 
        'xǁUtilitiesTabǁhandle_is__mutmut_18': xǁUtilitiesTabǁhandle_is__mutmut_18, 
        'xǁUtilitiesTabǁhandle_is__mutmut_19': xǁUtilitiesTabǁhandle_is__mutmut_19, 
        'xǁUtilitiesTabǁhandle_is__mutmut_20': xǁUtilitiesTabǁhandle_is__mutmut_20, 
        'xǁUtilitiesTabǁhandle_is__mutmut_21': xǁUtilitiesTabǁhandle_is__mutmut_21, 
        'xǁUtilitiesTabǁhandle_is__mutmut_22': xǁUtilitiesTabǁhandle_is__mutmut_22, 
        'xǁUtilitiesTabǁhandle_is__mutmut_23': xǁUtilitiesTabǁhandle_is__mutmut_23, 
        'xǁUtilitiesTabǁhandle_is__mutmut_24': xǁUtilitiesTabǁhandle_is__mutmut_24, 
        'xǁUtilitiesTabǁhandle_is__mutmut_25': xǁUtilitiesTabǁhandle_is__mutmut_25, 
        'xǁUtilitiesTabǁhandle_is__mutmut_26': xǁUtilitiesTabǁhandle_is__mutmut_26, 
        'xǁUtilitiesTabǁhandle_is__mutmut_27': xǁUtilitiesTabǁhandle_is__mutmut_27, 
        'xǁUtilitiesTabǁhandle_is__mutmut_28': xǁUtilitiesTabǁhandle_is__mutmut_28, 
        'xǁUtilitiesTabǁhandle_is__mutmut_29': xǁUtilitiesTabǁhandle_is__mutmut_29, 
        'xǁUtilitiesTabǁhandle_is__mutmut_30': xǁUtilitiesTabǁhandle_is__mutmut_30, 
        'xǁUtilitiesTabǁhandle_is__mutmut_31': xǁUtilitiesTabǁhandle_is__mutmut_31, 
        'xǁUtilitiesTabǁhandle_is__mutmut_32': xǁUtilitiesTabǁhandle_is__mutmut_32, 
        'xǁUtilitiesTabǁhandle_is__mutmut_33': xǁUtilitiesTabǁhandle_is__mutmut_33, 
        'xǁUtilitiesTabǁhandle_is__mutmut_34': xǁUtilitiesTabǁhandle_is__mutmut_34, 
        'xǁUtilitiesTabǁhandle_is__mutmut_35': xǁUtilitiesTabǁhandle_is__mutmut_35, 
        'xǁUtilitiesTabǁhandle_is__mutmut_36': xǁUtilitiesTabǁhandle_is__mutmut_36, 
        'xǁUtilitiesTabǁhandle_is__mutmut_37': xǁUtilitiesTabǁhandle_is__mutmut_37, 
        'xǁUtilitiesTabǁhandle_is__mutmut_38': xǁUtilitiesTabǁhandle_is__mutmut_38, 
        'xǁUtilitiesTabǁhandle_is__mutmut_39': xǁUtilitiesTabǁhandle_is__mutmut_39, 
        'xǁUtilitiesTabǁhandle_is__mutmut_40': xǁUtilitiesTabǁhandle_is__mutmut_40, 
        'xǁUtilitiesTabǁhandle_is__mutmut_41': xǁUtilitiesTabǁhandle_is__mutmut_41, 
        'xǁUtilitiesTabǁhandle_is__mutmut_42': xǁUtilitiesTabǁhandle_is__mutmut_42, 
        'xǁUtilitiesTabǁhandle_is__mutmut_43': xǁUtilitiesTabǁhandle_is__mutmut_43, 
        'xǁUtilitiesTabǁhandle_is__mutmut_44': xǁUtilitiesTabǁhandle_is__mutmut_44, 
        'xǁUtilitiesTabǁhandle_is__mutmut_45': xǁUtilitiesTabǁhandle_is__mutmut_45, 
        'xǁUtilitiesTabǁhandle_is__mutmut_46': xǁUtilitiesTabǁhandle_is__mutmut_46, 
        'xǁUtilitiesTabǁhandle_is__mutmut_47': xǁUtilitiesTabǁhandle_is__mutmut_47
    }
    xǁUtilitiesTabǁhandle_is__mutmut_orig.__name__ = 'xǁUtilitiesTabǁhandle_is'

    @QtCore.pyqtSlot(list, name="on_object_list")
    def on_object_list(self, object_list: list) -> None:
        """Handle receiving printer object list"""
        self.cg = object_list
        for obj in self.cg:
            base_name = obj.split()[0]

            # Only accept 'fan_generic' or 'fan'
            if base_name == "fan_generic" or base_name == "fan":
                self.objects["fans"][obj] = "indef"
        self._update_leds_from_config()

    @QtCore.pyqtSlot(dict, name="on_object_config")
    @QtCore.pyqtSlot(list, name="on_object_config")
    def on_object_config(self, config: typing.Union[dict, list]) -> None:
        """Handle receiving printer object configurations"""
        if not config:
            return
        config_items = [config] if isinstance(config, dict) else config
        for item in config_items:
            for key, value in item.items():
                if (
                    key.startswith("stepper_")
                    and isinstance(value, dict)
                    and key not in self.stepper_limits
                ):
                    pos_min = value.get("position_min")
                    pos_max = value.get("position_max")
                    if pos_min is not None or pos_max is not None:
                        self.stepper_limits[key] = {
                            "min": float(pos_min)
                            if pos_min is not None
                            else -float("inf"),
                            "max": float(pos_max)
                            if pos_max is not None
                            else float("inf"),
                        }

    def on_printer_config_received(self, config: dict) -> None:
        args = [config]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁon_printer_config_received__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁon_printer_config_received__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_orig(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_1(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("XXxXX", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_2(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("X", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_3(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "XXyXX", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_4(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "Y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_5(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "XXzXX"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_6(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "Z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_7(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                None, self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_8(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", None
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_9(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_10(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PyQt_PyObject"].emit(
                f"stepper_{axis}", )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_11(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "XXPyQt_PyObjectXX"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_12(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "pyqt_pyobject"].emit(
                f"stepper_{axis}", self.on_object_config
            )

    def xǁUtilitiesTabǁon_printer_config_received__mutmut_13(self, config: dict) -> None:
        """Handle printer configuration"""
        for axis in ("x", "y", "z"):
            self.subscribe_config[str, "PYQT_PYOBJECT"].emit(
                f"stepper_{axis}", self.on_object_config
            )
    
    xǁUtilitiesTabǁon_printer_config_received__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁon_printer_config_received__mutmut_1': xǁUtilitiesTabǁon_printer_config_received__mutmut_1, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_2': xǁUtilitiesTabǁon_printer_config_received__mutmut_2, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_3': xǁUtilitiesTabǁon_printer_config_received__mutmut_3, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_4': xǁUtilitiesTabǁon_printer_config_received__mutmut_4, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_5': xǁUtilitiesTabǁon_printer_config_received__mutmut_5, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_6': xǁUtilitiesTabǁon_printer_config_received__mutmut_6, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_7': xǁUtilitiesTabǁon_printer_config_received__mutmut_7, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_8': xǁUtilitiesTabǁon_printer_config_received__mutmut_8, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_9': xǁUtilitiesTabǁon_printer_config_received__mutmut_9, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_10': xǁUtilitiesTabǁon_printer_config_received__mutmut_10, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_11': xǁUtilitiesTabǁon_printer_config_received__mutmut_11, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_12': xǁUtilitiesTabǁon_printer_config_received__mutmut_12, 
        'xǁUtilitiesTabǁon_printer_config_received__mutmut_13': xǁUtilitiesTabǁon_printer_config_received__mutmut_13
    }
    xǁUtilitiesTabǁon_printer_config_received__mutmut_orig.__name__ = 'xǁUtilitiesTabǁon_printer_config_received'

    @QtCore.pyqtSlot(str, list, name="on_gcode_move_update")
    def on_gcode_move_update(self, name: str, value: list) -> None:
        """Handle gcode move"""
        if not value:
            return
        if name == "gcode_position":
            ...

    def run_routine(self, process: Process):
        args = [process]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁrun_routine__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁrun_routine__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁrun_routine__mutmut_orig(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_1(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = None
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_2(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = None
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_3(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("XXfansXX", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_4(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("FANS", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_5(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "XXfan is spinningXX"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_6(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "FAN IS SPINNING"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_7(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("XXaxisXX", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_8(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("AXIS", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_9(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "XXaxis is movingXX"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_10(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "AXIS IS MOVING"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_11(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("XXbheatXX", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_12(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("BHEAT", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_13(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "XXbed is heatingXX"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_14(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "BED IS HEATING"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_15(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("XXextrudeXX", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_16(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("EXTRUDE", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_17(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "XXextruder is being testedXX"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_18(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "EXTRUDER IS BEING TESTED"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_19(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_20(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = None
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_21(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = None
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_22(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(None)
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_23(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(None, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_24(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, None).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_25(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get({}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_26(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, ).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_27(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_28(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(None):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_29(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = None
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_30(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = True
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_31(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(None)

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_32(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(None))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_33(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process != Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_34(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit(None)
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_35(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("XXM107XX")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_36(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("m107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_37(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = None
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_38(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process != Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_39(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = None
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_40(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process not in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_41(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = None

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_42(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "XXPlease check if the temperature reaches 60°C. \n you may need to wait a few moments.XX"

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_43(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "please check if the temperature reaches 60°c. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_44(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "PLEASE CHECK IF THE TEMPERATURE REACHES 60°C. \n YOU MAY NEED TO WAIT A FEW MOMENTS."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_45(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            None, message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_46(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", None
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_47(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_48(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_49(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            None,
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_50(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            None,
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_51(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            None,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_52(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_53(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_54(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_55(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(None),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_56(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10001 if process == Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_57(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process != Process.AXIS else 0,
        )
        self._send_routine_gcode()

    def xǁUtilitiesTabǁrun_routine__mutmut_58(self, process: Process):
        """Run check routine for available processes"""
        self.current_process = process
        routine_configs = {
            Process.FAN: ("fans", "fan is spinning"),
            Process.AXIS: ("axis", "axis is moving"),
            Process.BED_HEATER: ("bheat", "bed is heating"),
            Process.EXTRUDER: ("extrude", "extruder is being tested"),
        }
        if process not in routine_configs:
            return
        obj_key, message = routine_configs[process]
        obj_list = list(self.objects.get(obj_key, {}).keys())
        if not self._advance_routine_object(obj_list):
            if self.tb:
                self.troubleshoot_request()
                self.tb = False
            else:
                self.change_page(self.indexOf(self.panel.utilities_page))

            if process == Process.FAN:
                self.run_gcode_signal.emit("M107")
            return

        message = f"Please check if the {self.current_object} is functioning correctly."
        if process == Process.AXIS:
            message = f"Please ensure the {self.current_object} axis moves correctly."
        elif process in [Process.BED_HEATER, Process.EXTRUDER]:
            message = "Please check if the temperature reaches 60°C. \n you may need to wait a few moments."

        self.set_routine_check_page(
            f"Running routine for: {self.current_object}", message
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Please check if the {message}",
            10000 if process == Process.AXIS else 1,
        )
        self._send_routine_gcode()
    
    xǁUtilitiesTabǁrun_routine__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTab�
        'xǁUtilitiesTabǁrun_routine__mutmut_57': xǁUtilitiesTabǁrun_routine__mutmut_57, 
        'xǁUtilitiesTabǁrun_routine__mutmut_58': xǁUtilitiesTabǁrun_routine__mutmut_58
    }
    xǁUtilitiesTabǁrun_routine__mutmut_orig.__name__ = 'xǁUtilitiesTabǁrun_routine'

    def _advance_routine_object(self, obj_list: list) -> bool:
        args = [obj_list]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ_advance_routine_object__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ_advance_routine_object__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_orig(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_1(self, obj_list: list) -> bool:
        if obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_2(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_3(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is not None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_4(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = None
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_5(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[1] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_6(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run or obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_7(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "XXdoneXX"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_8(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "DONE"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_9(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_10(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process != Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_11(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit(None)
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_12(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("XXG28XX")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_13(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("g28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_14(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = None
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_15(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[1]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_16(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return False
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_17(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = None
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_18(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(None)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_19(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.rindex(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_20(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index - 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_21(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 2 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_22(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 <= len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_23(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = None
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_24(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index - 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_25(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 2]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_26(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return False
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_27(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = ""
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_28(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return True
        except ValueError:
            self.current_object = obj_list[0]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_29(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = None
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_30(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[1]
            return True

    def xǁUtilitiesTabǁ_advance_routine_object__mutmut_31(self, obj_list: list) -> bool:
        if not obj_list:
            is_first_run = self.current_object is None
            self.current_object = obj_list[0] if is_first_run and obj_list else "done"
            return is_first_run
        if self.current_object not in obj_list:
            if self.current_process == Process.AXIS:
                self.run_gcode_signal.emit("G28")
            self.current_object = obj_list[0]
            return True
        try:
            current_index = obj_list.index(self.current_object)
            if current_index + 1 < len(obj_list):
                self.current_object = obj_list[current_index + 1]
                return True
            else:
                self.current_object = None
                return False
        except ValueError:
            self.current_object = obj_list[0]
            return False
    
    xǁUtilitiesTabǁ_advance_routine_object__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁ_advance_routine_object__mutmut_1': xǁUtilitiesTabǁ_advance_routine_object__mutmut_1, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_2': xǁUtilitiesTabǁ_advance_routine_object__mutmut_2, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_3': xǁUtilitiesTabǁ_advance_routine_object__mutmut_3, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_4': xǁUtilitiesTabǁ_advance_routine_object__mutmut_4, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_5': xǁUtilitiesTabǁ_advance_routine_object__mutmut_5, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_6': xǁUtilitiesTabǁ_advance_routine_object__mutmut_6, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_7': xǁUtilitiesTabǁ_advance_routine_object__mutmut_7, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_8': xǁUtilitiesTabǁ_advance_routine_object__mutmut_8, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_9': xǁUtilitiesTabǁ_advance_routine_object__mutmut_9, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_10': xǁUtilitiesTabǁ_advance_routine_object__mutmut_10, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_11': xǁUtilitiesTabǁ_advance_routine_object__mutmut_11, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_12': xǁUtilitiesTabǁ_advance_routine_object__mutmut_12, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_13': xǁUtilitiesTabǁ_advance_routine_object__mutmut_13, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_14': xǁUtilitiesTabǁ_advance_routine_object__mutmut_14, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_15': xǁUtilitiesTabǁ_advance_routine_object__mutmut_15, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_16': xǁUtilitiesTabǁ_advance_routine_object__mutmut_16, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_17': xǁUtilitiesTabǁ_advance_routine_object__mutmut_17, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_18': xǁUtilitiesTabǁ_advance_routine_object__mutmut_18, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_19': xǁUtilitiesTabǁ_advance_routine_object__mutmut_19, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_20': xǁUtilitiesTabǁ_advance_routine_object__mutmut_20, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_21': xǁUtilitiesTabǁ_advance_routine_object__mutmut_21, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_22': xǁUtilitiesTabǁ_advance_routine_object__mutmut_22, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_23': xǁUtilitiesTabǁ_advance_routine_object__mutmut_23, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_24': xǁUtilitiesTabǁ_advance_routine_object__mutmut_24, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_25': xǁUtilitiesTabǁ_advance_routine_object__mutmut_25, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_26': xǁUtilitiesTabǁ_advance_routine_object__mutmut_26, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_27': xǁUtilitiesTabǁ_advance_routine_object__mutmut_27, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_28': xǁUtilitiesTabǁ_advance_routine_object__mutmut_28, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_29': xǁUtilitiesTabǁ_advance_routine_object__mutmut_29, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_30': xǁUtilitiesTabǁ_advance_routine_object__mutmut_30, 
        'xǁUtilitiesTabǁ_advance_routine_object__mutmut_31': xǁUtilitiesTabǁ_advance_routine_object__mutmut_31
    }
    xǁUtilitiesTabǁ_advance_routine_object__mutmut_orig.__name__ = 'xǁUtilitiesTabǁ_advance_routine_object'

    def on_routine_answer(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁon_routine_answer__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁon_routine_answer__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁon_routine_answer__mutmut_orig(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_1(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None and self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_2(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is not None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_3(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is not None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_4(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() != self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_5(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = None
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_6(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "XXyesXX"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_7(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "YES"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_8(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = None
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_9(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "XXnoXX"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_10(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "NO"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_11(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = None
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_12(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = False
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_13(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = None
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_14(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("XXfansXX", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_15(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("FANS", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_16(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("XXaxisXX", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_17(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("AXIS", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_18(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("XXbheatXX", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_19(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("BHEAT", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_20(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "XXBed_HeaterXX"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_21(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "bed_heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_22(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "BED_HEATER"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_23(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("XXextrudeXX", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_24(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("EXTRUDE", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_25(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "XXextruderXX"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_26(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "EXTRUDER"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_27(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process not in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_28(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = None
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_29(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = None
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_30(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process not in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_31(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit(None)
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_32(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("XXTURN_OFF_HEATERSXX")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_33(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("turn_off_heaters")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_34(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(None)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_35(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process != Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_36(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer != "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_37(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "XXyesXX":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_38(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "YES":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_39(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(None)
            else:
                self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁon_routine_answer__mutmut_40(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(None)

    def xǁUtilitiesTabǁon_routine_answer__mutmut_41(self) -> None:
        """Handle routine ongoing process"""
        if self.current_process is None or self.current_object is None:
            return
        if self.sender() == self.panel.rc_yes:
            answer = "yes"
        else:
            answer = "no"
            self.tb = True
        process_map = {
            Process.FAN: ("fans", self.current_object),
            Process.AXIS: ("axis", self.current_object),
            Process.BED_HEATER: ("bheat", "Bed_Heater"),
            Process.EXTRUDER: ("extrude", "extruder"),
        }
        if self.current_process in process_map:
            obj_key, item_key = process_map[self.current_process]
            self.objects[obj_key][item_key] = answer
            if self.current_process in [Process.BED_HEATER, Process.EXTRUDER]:
                self.run_gcode_signal.emit("TURN_OFF_HEATERS")
            self.run_routine(self.current_process)
        elif self.current_process == Process.AXIS_MAINTENANCE:
            if answer == "yes":
                self._run_axis_maintenance_gcode(self.current_object)
            else:
                self.change_page(self.indexOf(None))
    
    xǁUtilitiesTabǁon_routine_answer__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁon_routine_answer__mutmut_1': xǁUtilitiesTabǁon_routine_answer__mutmut_1, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_2': xǁUtilitiesTabǁon_routine_answer__mutmut_2, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_3': xǁUtilitiesTabǁon_routine_answer__mutmut_3, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_4': xǁUtilitiesTabǁon_routine_answer__mutmut_4, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_5': xǁUtilitiesTabǁon_routine_answer__mutmut_5, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_6': xǁUtilitiesTabǁon_routine_answer__mutmut_6, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_7': xǁUtilitiesTabǁon_routine_answer__mutmut_7, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_8': xǁUtilitiesTabǁon_routine_answer__mutmut_8, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_9': xǁUtilitiesTabǁon_routine_answer__mutmut_9, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_10': xǁUtilitiesTabǁon_routine_answer__mutmut_10, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_11': xǁUtilitiesTabǁon_routine_answer__mutmut_11, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_12': xǁUtilitiesTabǁon_routine_answer__mutmut_12, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_13': xǁUtilitiesTabǁon_routine_answer__mutmut_13, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_14': xǁUtilitiesTabǁon_routine_answer__mutmut_14, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_15': xǁUtilitiesTabǁon_routine_answer__mutmut_15, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_16': xǁUtilitiesTabǁon_routine_answer__mutmut_16, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_17': xǁUtilitiesTabǁon_routine_answer__mutmut_17, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_18': xǁUtilitiesTabǁon_routine_answer__mutmut_18, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_19': xǁUtilitiesTabǁon_routine_answer__mutmut_19, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_20': xǁUtilitiesTabǁon_routine_answer__mutmut_20, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_21': xǁUtilitiesTabǁon_routine_answer__mutmut_21, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_22': xǁUtilitiesTabǁon_routine_answer__mutmut_22, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_23': xǁUtilitiesTabǁon_routine_answer__mutmut_23, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_24': xǁUtilitiesTabǁon_routine_answer__mutmut_24, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_25': xǁUtilitiesTabǁon_routine_answer__mutmut_25, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_26': xǁUtilitiesTabǁon_routine_answer__mutmut_26, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_27': xǁUtilitiesTabǁon_routine_answer__mutmut_27, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_28': xǁUtilitiesTabǁon_routine_answer__mutmut_28, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_29': xǁUtilitiesTabǁon_routine_answer__mutmut_29, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_30': xǁUtilitiesTabǁon_routine_answer__mutmut_30, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_31': xǁUtilitiesTabǁon_routine_answer__mutmut_31, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_32': xǁUtilitiesTabǁon_routine_answer__mutmut_32, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_33': xǁUtilitiesTabǁon_routine_answer__mutmut_33, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_34': xǁUtilitiesTabǁon_routine_answer__mutmut_34, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_35': xǁUtilitiesTabǁon_routine_answer__mutmut_35, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_36': xǁUtilitiesTabǁon_routine_answer__mutmut_36, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_37': xǁUtilitiesTabǁon_routine_answer__mutmut_37, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_38': xǁUtilitiesTabǁon_routine_answer__mutmut_38, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_39': xǁUtilitiesTabǁon_routine_answer__mutmut_39, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_40': xǁUtilitiesTabǁon_routine_answer__mutmut_40, 
        'xǁUtilitiesTabǁon_routine_answer__mutmut_41': xǁUtilitiesTabǁon_routine_answer__mutmut_41
    }
    xǁUtilitiesTabǁon_routine_answer__mutmut_orig.__name__ = 'xǁUtilitiesTabǁon_routine_answer'

    def _send_routine_gcode(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_orig(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_1(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process != Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_2(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = None
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_3(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object and next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_4(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(None, None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_5(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_6(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), )
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_7(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(None), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_8(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["XXfansXX"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_9(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["FANS"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_10(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name != "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_11(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "XXfanXX":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_12(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "FAN":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_13(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit(None)
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_14(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("XXM106 S255\nM400XX")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_15(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("m106 s255\nm400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_16(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        None
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_17(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = None

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_18(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "XXSET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60XX",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_19(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "set_heater_temperature heater=heater_bed target=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_20(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=HEATER_BED TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_21(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "XXSET_HEATER_TEMPERATURE HEATER=extruder TARGET=60XX",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_22(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "set_heater_temperature heater=extruder target=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_23(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=EXTRUDER TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_24(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "XXxXX"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_25(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "X"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_26(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "XXG91\nG1 X50 F700\nG1 X-50 F700XX",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_27(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "g91\ng1 x50 f700\ng1 x-50 f700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_28(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "XXyXX"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_29(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "Y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_30(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "XXG91\nG1 Y50 F700\nG1 Y-50 F700XX",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_31(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "g91\ng1 y50 f700\ng1 y-50 f700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_32(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "XXzXX"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_33(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "Z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_34(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "XXG91\nG1 Z50 F600\nG1 Z-50 F600XX",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_35(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "g91\ng1 z50 f600\ng1 z-50 f600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_36(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = None

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_37(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process != Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_38(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(None):
            self.run_gcode_signal.emit(f"{gcode}\nM400")

    def xǁUtilitiesTabǁ_send_routine_gcode__mutmut_39(self):
        """Send the correct G-code for the current process and object."""
        if self.current_process == Process.FAN:
            fan_name = self.current_object or next(iter(self.objects["fans"]), None)
            if fan_name:
                if fan_name == "fan":
                    self.run_gcode_signal.emit("M106 S255\nM400")
                else:
                    self.run_gcode_signal.emit(
                        f"SET_FAN_SPEED FAN={fan_name} SPEED=0.8\nM400"
                    )

            return

        gcode_map = {
            Process.BED_HEATER: "SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60",
            Process.EXTRUDER: "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=60",
            (Process.AXIS, "x"): "G91\nG1 X50 F700\nG1 X-50 F700",
            (Process.AXIS, "y"): "G91\nG1 Y50 F700\nG1 Y-50 F700",
            (Process.AXIS, "z"): "G91\nG1 Z50 F600\nG1 Z-50 F600",
        }

        key = (
            (self.current_process, self.current_object)
            if self.current_process == Process.AXIS
            else self.current_process
        )

        if gcode := gcode_map.get(key):
            self.run_gcode_signal.emit(None)
    
    xǁUtilitiesTabǁ_send_routine_gcode__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_1': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_1, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_2': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_2, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_3': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_3, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_4': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_4, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_5': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_5, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_6': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_6, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_7': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_7, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_8': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_8, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_9': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_9, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_10': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_10, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_11': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_11, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_12': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_12, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_13': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_13, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_14': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_14, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_15': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_15, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_16': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_16, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_17': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_17, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_18': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_18, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_19': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_19, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_20': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_20, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_21': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_21, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_22': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_22, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_23': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_23, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_24': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_24, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_25': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_25, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_26': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_26, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_27': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_27, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_28': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_28, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_29': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_29, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_30': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_30, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_31': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_31, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_32': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_32, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_33': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_33, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_34': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_34, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_35': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_35, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_36': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_36, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_37': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_37, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_38': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_38, 
        'xǁUtilitiesTabǁ_send_routine_gcode__mutmut_39': xǁUtilitiesTabǁ_send_routine_gcode__mutmut_39
    }
    xǁUtilitiesTabǁ_send_routine_gcode__mutmut_orig.__name__ = 'xǁUtilitiesTabǁ_send_routine_gcode'

    def set_routine_check_page(self, title: str, label: str):
        args = [title, label]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁset_routine_check_page__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁset_routine_check_page__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁset_routine_check_page__mutmut_orig(self, title: str, label: str):
        """Set text on routine page"""
        self.panel.rc_tittle.setText(title)
        self.panel.rc_label.setText(label)

    def xǁUtilitiesTabǁset_routine_check_page__mutmut_1(self, title: str, label: str):
        """Set text on routine page"""
        self.panel.rc_tittle.setText(None)
        self.panel.rc_label.setText(label)

    def xǁUtilitiesTabǁset_routine_check_page__mutmut_2(self, title: str, label: str):
        """Set text on routine page"""
        self.panel.rc_tittle.setText(title)
        self.panel.rc_label.setText(None)
    
    xǁUtilitiesTabǁset_routine_check_page__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁset_routine_check_page__mutmut_1': xǁUtilitiesTabǁset_routine_check_page__mutmut_1, 
        'xǁUtilitiesTabǁset_routine_check_page__mutmut_2': xǁUtilitiesTabǁset_routine_check_page__mutmut_2
    }
    xǁUtilitiesTabǁset_routine_check_page__mutmut_orig.__name__ = 'xǁUtilitiesTabǁset_routine_check_page'

    def update_led_values(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁupdate_led_values__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁupdate_led_values__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁupdate_led_values__mutmut_orig(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_1(self) -> None:
        """Update led state and color values"""
        if self.current_object in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_2(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["XXledsXX"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_3(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["LEDS"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_4(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = None
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_5(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["XXledsXX"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_6(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["LEDS"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_7(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = None
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_8(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(None)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_9(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 * 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_10(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() / 255 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_11(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 256 / 100)
        self.save_led_state()

    def xǁUtilitiesTabǁupdate_led_values__mutmut_12(self) -> None:
        """Update led state and color values"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        led_state.white = int(self.panel.leds_w_slider.value() * 255 / 101)
        self.save_led_state()
    
    xǁUtilitiesTabǁupdate_led_values__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁupdate_led_values__mutmut_1': xǁUtilitiesTabǁupdate_led_values__mutmut_1, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_2': xǁUtilitiesTabǁupdate_led_values__mutmut_2, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_3': xǁUtilitiesTabǁupdate_led_values__mutmut_3, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_4': xǁUtilitiesTabǁupdate_led_values__mutmut_4, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_5': xǁUtilitiesTabǁupdate_led_values__mutmut_5, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_6': xǁUtilitiesTabǁupdate_led_values__mutmut_6, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_7': xǁUtilitiesTabǁupdate_led_values__mutmut_7, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_8': xǁUtilitiesTabǁupdate_led_values__mutmut_8, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_9': xǁUtilitiesTabǁupdate_led_values__mutmut_9, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_10': xǁUtilitiesTabǁupdate_led_values__mutmut_10, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_11': xǁUtilitiesTabǁupdate_led_values__mutmut_11, 
        'xǁUtilitiesTabǁupdate_led_values__mutmut_12': xǁUtilitiesTabǁupdate_led_values__mutmut_12
    }
    xǁUtilitiesTabǁupdate_led_values__mutmut_orig.__name__ = 'xǁUtilitiesTabǁupdate_led_values'

    def _update_leds_from_config(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_orig(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_1(self):
        layout = None

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_2(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) or child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_3(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(None)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_4(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(1)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_5(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = None
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_6(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_7(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "XXledXX" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_8(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "LED" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_9(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" not in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_10(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = None
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_11(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[2]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_12(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(None)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_13(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = None
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_14(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["XXledsXX"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_15(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["LEDS"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_16(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type=None)
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_17(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="XXwhiteXX")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_18(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="WHITE")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_19(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = None
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_20(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 4
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_21(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = None  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_22(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(None):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_23(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = None
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_24(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(None, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_25(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, None)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_26(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_27(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, )
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_28(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(201, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_29(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 71)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_30(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(None)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_31(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty(None, "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_32(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", None)
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_33(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_34(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", )
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_35(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("XXclassXX", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_36(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("CLASS", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_37(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "XXmenu_btnXX")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_38(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "MENU_BTN")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_39(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(None)
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_40(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(None))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_41(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap("XX:/ui/media/btn_icons/LEDs.svgXX"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_42(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/leds.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_43(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/UI/MEDIA/BTN_ICONS/LEDS.SVG"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_44(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = None
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_45(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(None, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_46(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, None)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_47(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_48(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, )
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_49(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(None, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_50(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, None, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_51(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, None)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_52(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_53(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_54(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, )
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_55(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(None)
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_56(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(None, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_57(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, None))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_58(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_59(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, ))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_60(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(None)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_61(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) != 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_62(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 2:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_63(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                None
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_64(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(None, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_65(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, None)
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_66(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_67(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, )
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_68(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[1])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_69(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                None, self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_70(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, None
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_71(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.leds_page
            )

    def xǁUtilitiesTabǁ_update_leds_from_config__mutmut_72(self):
        layout = self.panel.leds_content_layout

        while layout.count():
            if (child := layout.takeAt(0)) and child.widget():
                child.widget().deleteLater()  # type: ignore

        led_names = []
        if not self.cg:
            return

        # Collect LED names
        for obj in self.cg:
            if "led" in obj:
                try:
                    name = obj.split()[1]
                    led_names.append(name)
                    self.objects["leds"][name] = LedState(led_type="white")
                except IndexError:
                    pass

        max_columns = 3
        buttons = []  # store references to created buttons

        # Create LED buttons
        for i, name in enumerate(led_names):
            if self.panel.leds_widget:
                button = BlocksCustomButton()
                button.setFixedSize(200, 70)
                button.setText(name)
                button.setProperty("class", "menu_btn")
                button.setPixmap(QtGui.QPixmap(":/ui/media/btn_icons/LEDs.svg"))
                row, col = divmod(i, max_columns)
                layout.addWidget(button, row, col)
                button.clicked.connect(partial(self.handle_led_button, name))
                buttons.append(button)

        if len(buttons) == 1:
            self.panel.utilities_leds_btn.clicked.connect(
                partial(self.handle_led_button, led_names[0])
            )
        else:
            self._connect_page_change(
                self.panel.utilities_leds_btn, )
    
    xǁUtilitiesTabǁ_update_leds_from_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_1': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_1, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_2': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_2, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_3': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_3, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_4': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_4, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_5': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_5, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_6': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_6, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_7': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_7, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_8': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_8, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_9': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_9, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_10': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_10, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_11': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_11, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_12': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_12, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_13': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_13, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_14': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_14, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_15': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_15, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_16': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_16, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_17': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_17, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_18': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_18, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_19': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_19, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_20': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_20, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_21': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_21, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_22': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_22, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_23': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_23, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_24': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_24, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_25': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_25, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_26': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_26, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_27': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_27, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_28': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_28, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_29': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_29, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_30': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_30, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_31': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_31, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_32': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_32, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_33': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_33, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_34': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_34, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_35': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_35, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_36': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_36, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_37': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_37, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_38': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_38, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_39': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_39, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_40': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_40, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_41': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_41, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_42': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_42, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_43': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_43, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_44': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_44, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_45': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_45, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_46': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_46, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_47': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_47, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_48': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_48, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_49': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_49, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_50': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_50, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_51': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_51, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_52': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_52, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_53': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_53, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_54': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_54, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_55': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_55, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_56': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_56, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_57': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_57, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_58': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_58, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_59': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_59, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_60': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_60, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_61': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_61, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_62': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_62, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_63': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_63, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_64': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_64, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_65': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_65, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_66': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_66, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_67': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_67, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_68': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_68, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_69': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_69, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_70': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_70, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_71': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_71, 
        'xǁUtilitiesTabǁ_update_leds_from_config__mutmut_72': xǁUtilitiesTabǁ_update_leds_from_config__mutmut_72
    }
    xǁUtilitiesTabǁ_update_leds_from_config__mutmut_orig.__name__ = 'xǁUtilitiesTabǁ_update_leds_from_config'

    def toggle_led_state(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁtoggle_led_state__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁtoggle_led_state__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_orig(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_1(self) -> None:
        """Toggle leds"""
        if self.current_object in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_2(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["XXledsXX"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_3(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["LEDS"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_4(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = None
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_5(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["XXledsXX"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_6(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["LEDS"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_7(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state != "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_8(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "XXoffXX":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_9(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "OFF":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_10(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = None
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_11(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "XXonXX"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_12(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "ON"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_13(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = None
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_14(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = None
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_15(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "XXoffXX"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_16(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "OFF"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.OFF
        self.save_led_state()

    def xǁUtilitiesTabǁtoggle_led_state__mutmut_17(self) -> None:
        """Toggle leds"""
        if self.current_object not in self.objects["leds"]:
            return
        led_state: LedState = self.objects["leds"][self.current_object]
        if led_state.state == "off":
            led_state.state = "on"
            self.panel.toggle_led_button.state = ToggleAnimatedButton.State.ON
        else:
            led_state.state = "off"
            self.panel.toggle_led_button.state = None
        self.save_led_state()
    
    xǁUtilitiesTabǁtoggle_led_state__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁtoggle_led_state__mutmut_1': xǁUtilitiesTabǁtoggle_led_state__mutmut_1, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_2': xǁUtilitiesTabǁtoggle_led_state__mutmut_2, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_3': xǁUtilitiesTabǁtoggle_led_state__mutmut_3, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_4': xǁUtilitiesTabǁtoggle_led_state__mutmut_4, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_5': xǁUtilitiesTabǁtoggle_led_state__mutmut_5, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_6': xǁUtilitiesTabǁtoggle_led_state__mutmut_6, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_7': xǁUtilitiesTabǁtoggle_led_state__mutmut_7, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_8': xǁUtilitiesTabǁtoggle_led_state__mutmut_8, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_9': xǁUtilitiesTabǁtoggle_led_state__mutmut_9, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_10': xǁUtilitiesTabǁtoggle_led_state__mutmut_10, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_11': xǁUtilitiesTabǁtoggle_led_state__mutmut_11, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_12': xǁUtilitiesTabǁtoggle_led_state__mutmut_12, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_13': xǁUtilitiesTabǁtoggle_led_state__mutmut_13, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_14': xǁUtilitiesTabǁtoggle_led_state__mutmut_14, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_15': xǁUtilitiesTabǁtoggle_led_state__mutmut_15, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_16': xǁUtilitiesTabǁtoggle_led_state__mutmut_16, 
        'xǁUtilitiesTabǁtoggle_led_state__mutmut_17': xǁUtilitiesTabǁtoggle_led_state__mutmut_17
    }
    xǁUtilitiesTabǁtoggle_led_state__mutmut_orig.__name__ = 'xǁUtilitiesTabǁtoggle_led_state'

    def handle_led_button(self, name: str) -> None:
        args = [name]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_led_button__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁhandle_led_button__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁhandle_led_button__mutmut_orig(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_1(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = None
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_2(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = None
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_3(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(None)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_4(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["XXledsXX"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_5(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["LEDS"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_6(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_7(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = None
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_8(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type != "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_9(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "XXrgbXX"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_10(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "RGB"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_11(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(None)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_12(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_13(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(None)
        self.change_page(self.indexOf(self.panel.leds_slider_page))

    def xǁUtilitiesTabǁhandle_led_button__mutmut_14(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(None)

    def xǁUtilitiesTabǁhandle_led_button__mutmut_15(self, name: str) -> None:
        """Handle led button clicked"""
        self.current_object = name
        led_state: LedState = self.objects["leds"].get(name)
        if not led_state:
            return
        is_rgb = led_state.led_type == "rgb"
        self.panel.leds_w_slider.setVisible(not is_rgb)
        self.panel.leds_w_slider.setValue(led_state.white)
        self.change_page(self.indexOf(None))
    
    xǁUtilitiesTabǁhandle_led_button__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁhandle_led_button__mutmut_1': xǁUtilitiesTabǁhandle_led_button__mutmut_1, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_2': xǁUtilitiesTabǁhandle_led_button__mutmut_2, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_3': xǁUtilitiesTabǁhandle_led_button__mutmut_3, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_4': xǁUtilitiesTabǁhandle_led_button__mutmut_4, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_5': xǁUtilitiesTabǁhandle_led_button__mutmut_5, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_6': xǁUtilitiesTabǁhandle_led_button__mutmut_6, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_7': xǁUtilitiesTabǁhandle_led_button__mutmut_7, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_8': xǁUtilitiesTabǁhandle_led_button__mutmut_8, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_9': xǁUtilitiesTabǁhandle_led_button__mutmut_9, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_10': xǁUtilitiesTabǁhandle_led_button__mutmut_10, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_11': xǁUtilitiesTabǁhandle_led_button__mutmut_11, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_12': xǁUtilitiesTabǁhandle_led_button__mutmut_12, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_13': xǁUtilitiesTabǁhandle_led_button__mutmut_13, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_14': xǁUtilitiesTabǁhandle_led_button__mutmut_14, 
        'xǁUtilitiesTabǁhandle_led_button__mutmut_15': xǁUtilitiesTabǁhandle_led_button__mutmut_15
    }
    xǁUtilitiesTabǁhandle_led_button__mutmut_orig.__name__ = 'xǁUtilitiesTabǁhandle_led_button'

    def save_led_state(self):
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁsave_led_state__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁsave_led_state__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁsave_led_state__mutmut_orig(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_1(self):
        """Save led state"""
        if self.current_object:
            if self.current_object not in self.objects["leds"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_2(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["XXledsXX"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_3(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["LEDS"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_4(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = None
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_5(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = self.objects["XXledsXX"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_6(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = self.objects["LEDS"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(self.current_object))

    def xǁUtilitiesTabǁsave_led_state__mutmut_7(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(None)

    def xǁUtilitiesTabǁsave_led_state__mutmut_8(self):
        """Save led state"""
        if self.current_object:
            if self.current_object in self.objects["leds"]:
                led_state: LedState = self.objects["leds"][self.current_object]
                self.run_gcode_signal.emit(led_state.get_gcode(None))
    
    xǁUtilitiesTabǁsave_led_state__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁsave_led_state__mutmut_1': xǁUtilitiesTabǁsave_led_state__mutmut_1, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_2': xǁUtilitiesTabǁsave_led_state__mutmut_2, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_3': xǁUtilitiesTabǁsave_led_state__mutmut_3, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_4': xǁUtilitiesTabǁsave_led_state__mutmut_4, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_5': xǁUtilitiesTabǁsave_led_state__mutmut_5, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_6': xǁUtilitiesTabǁsave_led_state__mutmut_6, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_7': xǁUtilitiesTabǁsave_led_state__mutmut_7, 
        'xǁUtilitiesTabǁsave_led_state__mutmut_8': xǁUtilitiesTabǁsave_led_state__mutmut_8
    }
    xǁUtilitiesTabǁsave_led_state__mutmut_orig.__name__ = 'xǁUtilitiesTabǁsave_led_state'

    def axis_maintenance(self, axis: str) -> None:
        args = [axis]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁaxis_maintenance__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁaxis_maintenance__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_orig(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_1(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = None
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_2(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = None
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_3(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(None)
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_4(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.lower()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_5(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            None,
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_6(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            None,
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_7(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_8(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_9(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "XXAxis MaintenanceXX",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_10(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "axis maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_11(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "AXIS MAINTENANCE",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_12(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.lower()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_13(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            None,
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_14(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            None,
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_15(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            None,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_16(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_17(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_18(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_19(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(None),
            f"Homing {axis.upper()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_20(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.lower()} axis...",
            5000,
        )

    def xǁUtilitiesTabǁaxis_maintenance__mutmut_21(self, axis: str) -> None:
        """Routine, checks axis movement for printer debugging"""
        self.current_process = Process.AXIS_MAINTENANCE
        self.current_object = axis
        self.run_gcode_signal.emit(f"G28 {axis.upper()}\nM400")
        self.set_routine_check_page(
            "Axis Maintenance",
            f"Insert oil on the {axis.upper()} axis before confirming.",
        )
        self.show_waiting_page(
            self.indexOf(self.panel.rc_page),
            f"Homing {axis.upper()} axis...",
            5001,
        )
    
    xǁUtilitiesTabǁaxis_maintenance__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁaxis_maintenance__mutmut_1': xǁUtilitiesTabǁaxis_maintenance__mutmut_1, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_2': xǁUtilitiesTabǁaxis_maintenance__mutmut_2, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_3': xǁUtilitiesTabǁaxis_maintenance__mutmut_3, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_4': xǁUtilitiesTabǁaxis_maintenance__mutmut_4, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_5': xǁUtilitiesTabǁaxis_maintenance__mutmut_5, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_6': xǁUtilitiesTabǁaxis_maintenance__mutmut_6, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_7': xǁUtilitiesTabǁaxis_maintenance__mutmut_7, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_8': xǁUtilitiesTabǁaxis_maintenance__mutmut_8, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_9': xǁUtilitiesTabǁaxis_maintenance__mutmut_9, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_10': xǁUtilitiesTabǁaxis_maintenance__mutmut_10, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_11': xǁUtilitiesTabǁaxis_maintenance__mutmut_11, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_12': xǁUtilitiesTabǁaxis_maintenance__mutmut_12, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_13': xǁUtilitiesTabǁaxis_maintenance__mutmut_13, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_14': xǁUtilitiesTabǁaxis_maintenance__mutmut_14, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_15': xǁUtilitiesTabǁaxis_maintenance__mutmut_15, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_16': xǁUtilitiesTabǁaxis_maintenance__mutmut_16, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_17': xǁUtilitiesTabǁaxis_maintenance__mutmut_17, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_18': xǁUtilitiesTabǁaxis_maintenance__mutmut_18, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_19': xǁUtilitiesTabǁaxis_maintenance__mutmut_19, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_20': xǁUtilitiesTabǁaxis_maintenance__mutmut_20, 
        'xǁUtilitiesTabǁaxis_maintenance__mutmut_21': xǁUtilitiesTabǁaxis_maintenance__mutmut_21
    }
    xǁUtilitiesTabǁaxis_maintenance__mutmut_orig.__name__ = 'xǁUtilitiesTabǁaxis_maintenance'

    def _run_axis_maintenance_gcode(self, axis: str):
        args = [axis]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_orig(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_1(self, axis: str):
        stepper_key = None
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_2(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key not in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_3(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = None
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_4(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get(None, 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_5(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", None)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_6(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get(20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_7(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", )
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_8(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("XXmaxXX", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_9(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("MAX", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_10(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 21)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_11(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = None
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_12(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) + 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_13(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(None) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_14(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 21
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_15(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                None
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_16(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.lower()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_17(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.lower()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_18(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                None,
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_19(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                None,
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_20(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                None,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_21(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_22(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_23(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_24(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(None),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_25(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.lower()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_26(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5001,
            )
        else:
            self.change_page(self.indexOf(self.panel.axes_page))

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_27(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(None)

    def xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_28(self, axis: str):
        stepper_key = f"stepper_{axis}"
        if stepper_key in self.stepper_limits:
            max_pos = self.stepper_limits[stepper_key].get("max", 20)
            distance = int(max_pos) - 20
            self.run_gcode_signal.emit(
                f"G1 {axis.upper()}{distance} F3000\nM400\nG28 {axis.upper()}\nM400"
            )
            self.show_waiting_page(
                self.indexOf(self.panel.axes_page),
                f"Running maintenance cycle on {axis.upper()} axis...",
                5000,
            )
        else:
            self.change_page(self.indexOf(None))
    
    xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_1': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_1, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_2': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_2, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_3': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_3, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_4': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_4, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_5': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_5, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_6': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_6, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_7': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_7, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_8': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_8, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_9': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_9, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_10': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_10, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_11': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_11, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_12': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_12, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_13': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_13, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_14': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_14, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_15': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_15, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_16': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_16, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_17': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_17, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_18': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_18, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_19': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_19, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_20': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_20, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_21': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_21, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_22': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_22, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_23': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_23, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_24': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_24, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_25': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_25, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_26': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_26, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_27': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_27, 
        'xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_28': xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_28
    }
    xǁUtilitiesTabǁ_run_axis_maintenance_gcode__mutmut_orig.__name__ = 'xǁUtilitiesTabǁ_run_axis_maintenance_gcode'

    def troubleshoot_request(self) -> None:
        """Show troubleshoot page"""
        self.troubleshoot_page.show()

    def show_waiting_page(self, page_to_go_to: int, label: str, time_ms: int):
        args = [page_to_go_to, label, time_ms]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁshow_waiting_page__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁshow_waiting_page__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_orig(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_1(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(None, label)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_2(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, None)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_3(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(label)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_4(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, )
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_5(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(False, label)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_6(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(None, lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_7(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(time_ms, None)

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_8(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(lambda: self.change_page(page_to_go_to))

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_9(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(time_ms, )

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_10(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(time_ms, lambda: None)

    def xǁUtilitiesTabǁshow_waiting_page__mutmut_11(self, page_to_go_to: int, label: str, time_ms: int):
        """Show placeholder page"""
        self.call_load_panel.emit(True, label)
        QtCore.QTimer.singleShot(time_ms, lambda: self.change_page(None))
    
    xǁUtilitiesTabǁshow_waiting_page__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁshow_waiting_page__mutmut_1': xǁUtilitiesTabǁshow_waiting_page__mutmut_1, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_2': xǁUtilitiesTabǁshow_waiting_page__mutmut_2, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_3': xǁUtilitiesTabǁshow_waiting_page__mutmut_3, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_4': xǁUtilitiesTabǁshow_waiting_page__mutmut_4, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_5': xǁUtilitiesTabǁshow_waiting_page__mutmut_5, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_6': xǁUtilitiesTabǁshow_waiting_page__mutmut_6, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_7': xǁUtilitiesTabǁshow_waiting_page__mutmut_7, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_8': xǁUtilitiesTabǁshow_waiting_page__mutmut_8, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_9': xǁUtilitiesTabǁshow_waiting_page__mutmut_9, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_10': xǁUtilitiesTabǁshow_waiting_page__mutmut_10, 
        'xǁUtilitiesTabǁshow_waiting_page__mutmut_11': xǁUtilitiesTabǁshow_waiting_page__mutmut_11
    }
    xǁUtilitiesTabǁshow_waiting_page__mutmut_orig.__name__ = 'xǁUtilitiesTabǁshow_waiting_page'

    def _connect_page_change(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        args = [button, page]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁ_connect_page_change__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁ_connect_page_change__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁ_connect_page_change__mutmut_orig(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        if isinstance(button, QtWidgets.QAbstractButton):
            button.clicked.connect(lambda: self.change_page(self.indexOf(page)))

    def xǁUtilitiesTabǁ_connect_page_change__mutmut_1(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        if isinstance(button, QtWidgets.QAbstractButton):
            button.clicked.connect(None)

    def xǁUtilitiesTabǁ_connect_page_change__mutmut_2(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        if isinstance(button, QtWidgets.QAbstractButton):
            button.clicked.connect(lambda: None)

    def xǁUtilitiesTabǁ_connect_page_change__mutmut_3(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        if isinstance(button, QtWidgets.QAbstractButton):
            button.clicked.connect(lambda: self.change_page(None))

    def xǁUtilitiesTabǁ_connect_page_change__mutmut_4(self, button: QtWidgets.QWidget, page: QtWidgets.QWidget):
        if isinstance(button, QtWidgets.QAbstractButton):
            button.clicked.connect(lambda: self.change_page(self.indexOf(None)))
    
    xǁUtilitiesTabǁ_connect_page_change__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁ_connect_page_change__mutmut_1': xǁUtilitiesTabǁ_connect_page_change__mutmut_1, 
        'xǁUtilitiesTabǁ_connect_page_change__mutmut_2': xǁUtilitiesTabǁ_connect_page_change__mutmut_2, 
        'xǁUtilitiesTabǁ_connect_page_change__mutmut_3': xǁUtilitiesTabǁ_connect_page_change__mutmut_3, 
        'xǁUtilitiesTabǁ_connect_page_change__mutmut_4': xǁUtilitiesTabǁ_connect_page_change__mutmut_4
    }
    xǁUtilitiesTabǁ_connect_page_change__mutmut_orig.__name__ = 'xǁUtilitiesTabǁ_connect_page_change'

    def change_page(self, index: int):
        args = [index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁUtilitiesTabǁchange_page__mutmut_orig'), object.__getattribute__(self, 'xǁUtilitiesTabǁchange_page__mutmut_mutants'), args, kwargs, self)

    def xǁUtilitiesTabǁchange_page__mutmut_orig(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_1(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(None, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_2(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, None)
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_3(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit("")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_4(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, )
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_5(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(True, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_6(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "XXXX")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_7(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index <= self.count():
            self.request_change_page.emit(3, index)

    def xǁUtilitiesTabǁchange_page__mutmut_8(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(None, index)

    def xǁUtilitiesTabǁchange_page__mutmut_9(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, None)

    def xǁUtilitiesTabǁchange_page__mutmut_10(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(index)

    def xǁUtilitiesTabǁchange_page__mutmut_11(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(3, )

    def xǁUtilitiesTabǁchange_page__mutmut_12(self, index: int):
        """Request change page by index"""
        self.call_load_panel.emit(False, "")
        self.troubleshoot_page.hide()
        if index < self.count():
            self.request_change_page.emit(4, index)
    
    xǁUtilitiesTabǁchange_page__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁUtilitiesTabǁchange_page__mutmut_1': xǁUtilitiesTabǁchange_page__mutmut_1, 
        'xǁUtilitiesTabǁchange_page__mutmut_2': xǁUtilitiesTabǁchange_page__mutmut_2, 
        'xǁUtilitiesTabǁchange_page__mutmut_3': xǁUtilitiesTabǁchange_page__mutmut_3, 
        'xǁUtilitiesTabǁchange_page__mutmut_4': xǁUtilitiesTabǁchange_page__mutmut_4, 
        'xǁUtilitiesTabǁchange_page__mutmut_5': xǁUtilitiesTabǁchange_page__mutmut_5, 
        'xǁUtilitiesTabǁchange_page__mutmut_6': xǁUtilitiesTabǁchange_page__mutmut_6, 
        'xǁUtilitiesTabǁchange_page__mutmut_7': xǁUtilitiesTabǁchange_page__mutmut_7, 
        'xǁUtilitiesTabǁchange_page__mutmut_8': xǁUtilitiesTabǁchange_page__mutmut_8, 
        'xǁUtilitiesTabǁchange_page__mutmut_9': xǁUtilitiesTabǁchange_page__mutmut_9, 
        'xǁUtilitiesTabǁchange_page__mutmut_10': xǁUtilitiesTabǁchange_page__mutmut_10, 
        'xǁUtilitiesTabǁchange_page__mutmut_11': xǁUtilitiesTabǁchange_page__mutmut_11, 
        'xǁUtilitiesTabǁchange_page__mutmut_12': xǁUtilitiesTabǁchange_page__mutmut_12
    }
    xǁUtilitiesTabǁchange_page__mutmut_orig.__name__ = 'xǁUtilitiesTabǁchange_page'

    @QtCore.pyqtSlot(name="request-back")
    def back_button(self) -> None:
        """Request back"""
        self.request_back.emit()
